# Analysis Log

| # | Timestamp | Aspect | Duration | Key Findings |
|---|-----------|--------|----------|--------------|
| 1 | 2026-02-28 | `graph-api-mentions-tags` | 1 run | `/tags` polls photo-tagged feed posts (no creator opt-in, brand token); `mentioned_media` is webhook-first (requires `media_id`). Both use brand's own token. Stories/Reels NOT covered. 200 calls/hr/brand rate limit. `instagram_manage_comments` requires App Review. Two distinct webhook systems (Graph API `changes` vs Messaging API `messaging`). |
| 2 | 2026-02-28 | `webhooks-mentions` | 1 run | Graph API `mentions` (plural, `changes[]` array) = caption/comment @mentions; Messaging API `mention` (singular, `messaging[]` array) = Story mentions — completely separate systems. Caption vs comment distinguished by `comment_id` null/present. No polling fallback for @mentions — webhook is mandatory. Shares callback URL + page token with IG DM integration. Incremental effort is tiny if IG DM infra already built. At-least-once delivery; no replay API; missed events are unrecoverable. Private account posts invisible. |
| 3 | 2026-02-28 | `hashtag-monitoring` | 1 run | 30-hashtag/7-day rolling window is the binding constraint (not rate limits). Only catches public feed photos/image carousels — Reels excluded (major gap), Stories excluded, video carousels excluded. 24-hr lookback forces polling every 2–4 hrs. ~25 posts max per query. `instagram_public_content_access` requires separate App Review. NO architectural overlap with IG DM/mentions webhook — purely polling-based Temporal cron workflow. Unique value: catches branded-hashtag content without @mention or photo-tag (5–25% of UGC). Multi-brand-account workaround gives 30 slots per account. Medium effort due to brand config UI + new App Review. |
| 4 | 2026-02-28 | `story-mention-capture` | 1 run | Story @mentions arrive via Messaging API `mention` event in `messaging[]` array — SAME webhook as IG DMs. Subscribe via `mention` field (singular). Payload contains `media_id` + `media_type: "story"`. Fetch CDN URL via `mentioned_media` endpoint. CDN URL expires in 24h — must download within window for permanent archive. Meta TOS explicitly prohibits downloading Story media; but Archive.com and entire UGC industry appears to download anyway (grey area — industry treats Story @mentions as consent-implied content). Private account Stories only fire if brand follows creator. Stories without @mention NOT captured (require AI detection). HIGHEST overlap with IG DM integration: shared endpoint, handler, subscription, permissions, token. Incremental effort is Small–Medium if DM infra exists; webhook downtime = permanent content loss (no polling fallback). |
| 5 | 2026-02-28 | `ai-visual-detection` | 1 run | Three complementary sub-approaches: (1) YOLO logo detection — fine-tuned per-brand, 75-85% mAP, requires 200-500 labeled images/brand; (2) CLIP product recognition — zero-shot via embedding similarity, no brand-specific training needed, 65-75% top-1; (3) OCR — PaddleOCR v3.0 best for scene text, detects brand names as visible text. Video requires frame extraction (2 FPS practical; ~120 frames for 60s Reel). Build-vs-buy: Cloud Vision API ($1.50/1K images, works out-of-box for known brands) vs Roboflow managed ($49-299/mo) vs self-hosted YOLO+CLIP (lowest $/image at scale, highest setup cost). Archive Radar uses all three + audio. KEY INSIGHT: AI detection is the easy part — candidate content discovery is the real bottleneck (see `ai-candidate-discovery`). Google Cloud Video API logo detection ($0.60/min) is 3.3× more expensive than frame extraction + image API; avoid for video. |
| 6 | 2026-02-28 | `ai-audio-detection` | 1 run | Four-stage pipeline: ffmpeg audio extraction (16kHz mono WAV) → STT transcription → brand mention extraction (NLP) → confidence scoring. STT options: Whisper (self-hosted, ~$0.0017/min), GPT-4o Mini Transcribe ($0.003/min cheapest API), AssemblyAI ($0.0042/min, LeMUR LLM extraction, 30% fewer hallucinations). Brand extraction: keyword/regex (zero cost, ~90% exact match) → fuzzy match (rapidfuzz, ~80% recall) → Claude Haiku NLP ($0.0003/transcript, handles homonyms). CRITICAL CHALLENGE: Background music degrades Whisper accuracy from ~20% WER to ~30-40% WER; Spleeter/Demucs audio source separation mitigates this. Audio detection is the CHEAPEST AI detection layer at scale ($0.003/video API vs $1.50/1K images for visual). Archive Radar confirmed to use audio STT + NLP for spoken brand mentions. No new infra for API path — Whisper API or GPT-4o Mini Transcribe is a simple API call. Self-hosted shares GPU with visual detection worker. Small effort overall (1 sprint for API path). |
| 7 | 2026-02-28 | `ai-candidate-discovery` | 1 run | Six strategies mapped: (1) Business Discovery API — fetch recent media from any public Business/Creator account using brand token; 200 calls/hr supports ~400-500 creator polls/day; CRITICAL LIMIT: personal accounts (~80% of Instagram) are invisible. (2) Historical tag graph — auto-add every prior tagger to watchlist; self-reinforcing growth. (3) Hashtag expansion — extract usernames from hashtag results to discover new creators; conflicts with hashtag UGC capture budget (30-slot limit). (4) Engagement mining (commenters) — weak signal, low quality. (5) Third-party creator DBs (Modash $299-999/mo, HypeAuditor) — expand beyond organic discovery. (6) Apify personal-account scraping — only way to reach personal accounts; TOS risk same as existing Cheerful Apify usage. COVERAGE CEILING: 30-50% untagged UGC reachable via official API only; 60-75% with Apify personal accounts. Caption pre-filter eliminates cheap: skip posts with @mention (already captured), flag text-match, queue remainder for full AI. New tables: `ugc_creator_monitoring`, `ugc_ai_analysis_queue`. Pool is self-reinforcing — each new tagger auto-added to watchlist. Archive's "4× more UGC" likely means within known creator universe, not cold universe. |
| 8 | 2026-02-28 | `third-party-ugc-platforms` | 1 run | Surveyed 7 platforms: Archive, Pixlee/Emplifi, TINT/TrueLoyal, Bazaarvoice, Dash Hudson, Stackla/Nosto, Juicer. CRITICAL FINDING: No platform offers a white-label/reseller API program for UGC capture infrastructure. All are direct-to-brand SaaS. Archive's Partner API (GraphQL, `app.archive.com/api/v2`) is the only programmatic data layer — Enterprise-tier only, opaque pricing, direct competitor. Archive API supports: items/creators/transcriptions queries, filtering by POST/REEL/STORY/TIKTOK item type, virality score, rights status; webhooks for new capture events. Stackla/Nosto has REST API + 11 webhook types (TILE_INGESTED most relevant) — technically usable but enterprise-priced and had past Meta ToS violations. STRATEGIC CONCLUSION: No platform is viable as capture infrastructure for Cheerful's multi-brand SaaS. Archive's capabilities map 1:1 to what Cheerful can build natively using official Meta APIs + Temporal. Best third-party strategy: build native Layer 1, offer optional read-only Archive integration for brands that already subscribe. Archive Story capture = same Messaging API `story_mention` event Cheerful builds for IG DMs — Archive's Story advantage evaporates when IG DM infra is built. |
| 9 | 2026-03-01 | `unofficial-scraping` | 1 run | Three approaches assessed: (1) Apify actors — VIABLE as supplemental layer; Cheerful already has `APIFY_API_TOKEN` in prod + `ApifyService` + existing `test_16_apify_scrape_instagram_mentions.py` test script using `resultsType='mentions'`; main advantage over official API = unlimited hashtag monitoring (vs 30/7-day cap); no Story capture, no untagged detection; ~$0.50–5/1K results; lowest-friction extension of existing infra. (2) instagrapi (private API) — NOT RECOMMENDED; requires brand account credentials; account ban risk = catastrophic (brand locked out of Instagram); offers no unique capability over official APIs + Apify; TOS + CFAA exposure. (3) Browser automation (Playwright/Puppeteer) — NOT RECOMMENDED; higher complexity than Apify with no unique capability; Instagram bot detection is sophisticated; high maintenance burden. KEY STRATEGIC INSIGHT: Apify's only unique advantage over official Graph API is no hashtag cap — everything else (mentions, photo tags, feed posts) official API handles better (real-time webhooks, free, TOS-compliant). Recommended role: Apify = hashtag monitoring supplement + polling fallback; official API = primary capture path. |
| 10 | 2026-03-01 | `current-post-tracking` | 1 run | Cheerful already has a full post tracking system (launched Feb 2026): `creator_post` table, `post-media` Supabase Storage bucket (50MB, public), `PostTrackingWorkflow` (24h scheduler via continue_as_new), Apify `instagram-profile-scraper` ingestion (10 posts/creator), two-phase matching (caption text match → Claude Sonnet vision). FULLY REUSABLE for UGC: media_storage.py, analyzer.py (caption_matches + analyze_image_with_llm), scheduler pattern. NEEDS MODIFICATION: `creator_post` has NOT NULL `campaign_creator_id` FK — zero-signup UGC needs nullable or separate table; UNIQUE constraint is `(campaign_creator_id, instagram_post_id)` — UGC needs `(brand_ig_user_id, instagram_post_id)`; `match_method` only has 'caption'/'llm' — needs 'webhook_mention', 'story_mention', 'hashtag', 'ai_radar'. CURRENT GAPS: no Stories (Apify profile scraper limitation), no @mention monitoring, no photo-tag monitoring, no hashtag monitoring, no audio STT. Recommend new `ugc_content` table (separate from campaign-scoped `creator_post`) with nullable campaign FK and `capture_source` column. Stories gap is explicitly known — CDN URL expiry problem already documented in design doc. |
| 14 | 2026-03-01 | `option-official-api-capture` | 1 run | Full official API integration design. Four capture mechanisms: (1) Graph API `mentions` webhook → `FeedMentionWorkflow` (caption/comment @mentions); (2) `/tags` polling → `UGCTagPollingWorkflow` (photo-tagged posts, 10–15min cadence); (3) Messaging API `mention` event → `StoryMentionWorkflow` (Story @mentions, 24h download deadline); (4) Hashtag API → `HashtagMonitoringWorkflow` (2–4hr cadence, 30-slot budget). IG DM overlap is maximal: webhook endpoint, HMAC, IGSID resolution, token storage, media download service, App Review (instagram_manage_messages) all shared. Net-new: FeedMentionWorkflow (3–5 days), StoryMentionWorkflow (4–5 days), UGCTagPollingWorkflow (3–5 days), HashtagMonitoringWorkflow + UI (4–6 days), ugc_content table + attribution algorithm (4–5 days). Two App Review gates: instagram_manage_comments (can combine with DM review) + instagram_public_content_access (separate; hashtag-only). Coverage estimate: 60–75% of public brand UGC. Key gaps: Reels photo-tags (API gap), untagged content (AI Radar required), private accounts. Cost: free API + $10–200/month storage. Total effort (DM infra exists): ~4–6 engineer-weeks phased as 2wk + 1wk + 2wk. Partial index needed for brand-level dedup (NULL campaign_id). No polling fallback for @mentions — webhook-only, no recovery on downtime. |
| 16 | 2026-03-01 | `option-third-party-service` | 1 run | Three variants assessed: (A) Archive as primary engine — commercially unviable: per-brand pricing (~$1,000–5,000/month Enterprise) makes wholesale model cost-prohibitive at scale; Archive is a direct competitor and likely to refuse reseller arrangement. (B) Additive Archive integration — most viable path: brands that already pay Archive connect it to Cheerful; Cheerful polls Archive GraphQL API (`app.archive.com/api/v2`) + registers for `item_captured` webhooks; 2–3 engineer-weeks to build; Cheerful adds campaign attribution on top of Archive's capture. (C) Stackla/Nosto — technically viable (TILE_INGESTED webhook, REST API) but worse than Archive due to ToS violation history + Nosto's eCommerce-first focus. KEY INSIGHT: IG DM integration eliminates Archive's primary advantage — Story capture arrives via same Messaging API `story_mention` event Cheerful already builds for DMs; once DM infra exists, buy-vs-build tilts heavily toward build. Shared component with native capture: `attribute_ugc_to_campaign_activity` is reused regardless of capture source (Archive webhook → same attribution algorithm). New tables: `archive_integration` (credential store; tiny). No Meta App Review required for Variant B (Archive handles Meta API; Cheerful needs no new permissions). Competitive risk: Archive can see Cheerful's brands' UGC data; can terminate API access at will (Variant A). Data ownership: Cheerful mirrors media to Supabase Storage at ingestion; retains historical content if Archive integration is terminated. Build is preferred at scale: native Official API Capture is free to operate vs $100K+/month Archive at 100 brands. |
| 15 | 2026-03-01 | `option-ai-radar` | 1 run | AI Radar: three-stage pipeline — (1) Candidate Discovery via Business Discovery API polling of `ugc_creator_monitoring` watchlist (self-reinforcing: each tagged post adds creator to watchlist); (2) AI Analysis with three parallel sub-pipelines: visual (logo/CLIP/OCR on extracted frames), audio (ffmpeg → Whisper/GPT-4o Mini STT → brand NER), caption pre-filter (free); (3) Result Processing: confidence aggregation, deduplication vs `ugc_content`, auto-capture vs human review queue. MVP path reuses Claude vision (already in `analyzer.py`) + adds GPT-4o Mini Transcribe API (no new infrastructure). Coverage: +15–30% beyond Official API Capture (Business Discovery only); +25–40% with Apify personal accounts. Key constraint: Business Discovery API excludes ~80% personal-account creators. GPU worker only needed at Phase AI-3 (>100K analyses/month); API-based Phase AI-1 runs on CPU. IG DM overlap is lower (~30% shared) than Official API Capture — mainly reuses `user_ig_dm_account` credentials and media download service; candidate discovery, AI inference, review queue all net-new. Break-even vs Archive.com ($308/brand/mo): 3–12 months depending on brand count. New tables: `ugc_creator_monitoring`, `ugc_ai_analysis_queue`, `ugc_review_queue`. Effort: ~5–7 engineer-weeks (Phase AI-1); +2–3 for AI-2 (Cloud Vision); +4–6 for AI-3 (GPU self-hosted). |
| 13 | 2026-03-01 | `current-campaign-ugc-link` | 1 run | No explicit `brand` entity exists — brand = user (user_id) in current schema. Attribution algorithm: lookup incoming IG handle against `campaign_creator.social_media_handles` (JSONB, no index — GIN index or separate `creator_social_handle` table required at scale). Multi-campaign attribution is the norm: same post can match N campaigns, creates N ugc_content rows (consistent with existing `creator_post` UNIQUE key design). Two-tier UGC structure: campaign-level (campaign_id NOT NULL) and brand-level (campaign_id IS NULL) — brand-level for creators not in any campaign. `user_ig_dm_account` from IG DM spec is the correct credential anchor for brand Instagram account (avoid creating duplicate table — add 2-3 UGC columns to IG DM table via ALTER). `ugc_content` separate table confirmed correct (cannot extend `creator_post` — NOT NULL FKs, UNIQUE constraints, semantic coupling make it impossible without breaking migration). Three new tables: `ugc_content` (with nullable campaign FK + creator global registry FK), `ugc_hashtag_config`. UNIQUE dedup requires partial index for brand-level rows (PostgreSQL NULLs don't conflict in UNIQUE — partial index WHERE campaign_id IS NULL needed). Handle-based matching is fragile (case, @ prefix, handle changes) — store `creator_ig_user_id` (stable numeric ID) on campaign_creator for robust attribution. Retroactive promotion workflow needed: brand-level UGC → assign to campaign when creator is later added. |
| 12 | 2026-03-01 | `current-media-storage` | 1 run | 6 active Supabase Storage buckets mapped. KEY: `post-media` bucket (50MB limit, public, MP4/image MIME types) already exists and is designed for Instagram post media — 100% reusable for UGC with only a path organization change (`ugc/{brand_ig_id}/{post_id}/media.{ext}`). `StorageService` is bucket-agnostic, fully reusable. `brand-logos` bucket (wordmark/symbol/icon) doubles as AI Radar reference images for CLIP/logo detection. Volume risk: Reel-heavy UGC could generate 1–12 TB/month at 100-brand scale (~$190+/month Supabase overage). Three key gaps: (1) single `media_storage_path` TEXT needs to be JSONB array for carousel posts; (2) no content-hash dedup (same post captured twice = duplicate storage cost); (3) no TTL/archival job. Story media download is latency-sensitive: 24h CDN expiry means Temporal activity with 20-hour deadline needed for burst handling. All download pipeline patterns (async + `asyncio.run` sync wrapper) are directly reusable. | 11 | 2026-03-01 | `current-ig-dm-overlap` | 1 run | IG DM spec (50% complete) already defines exact shared infrastructure. FULLY SHARED components: `POST /webhooks/instagram/` endpoint + HMAC verification (`ig_dm_webhook.py`), `user_ig_dm_account` table (tokens, FB Page IDs), `ig_igsid_cache` table + `resolve_igsid_activity`, `post-media` Supabase Storage bucket, `download_and_store_media` service, FB Page OAuth flow, `instagram_manage_messages` App Review (covers `mention` field too). INCREMENTAL for Story capture: add `"mention"` to page subscription fields (1 line), add `elif "mention" in event:` routing branch (~10 lines), new `StoryMentionWorkflow` (Medium), new `fetch_story_mentioned_media_activity` + `download_ig_story_media_activity` (Small). Graph API `mentions` (plural) webhook arrives at SAME URL but via `entry.changes[]` — different subscription mechanism but shares endpoint and HMAC verification. KEY TIMING INSIGHT: subscribe to `mention` field at initial account connection (before StoryMentionWorkflow is built) — Story events arrive silently until workflow is ready, avoiding backfill complexity. No additional App Review items — `instagram_manage_messages` covers both DMs and Story `mention` field. Total incremental effort assuming DM infra complete: 4–7 engineering days. |
| 17 | 2026-03-01 | `option-hybrid-layered` | 1 run | Three independently deployable layers: Layer 1 (Official API: Story mentions + feed @mentions + photo-tag polling, 60–75% coverage, ~free), Layer 2 (Hashtag monitoring, +5–25%, free API), Layer 3 (AI Radar: visual/audio/OCR + candidate pool, +10–20%, $50–500/month). All three layers write to the same `ugc_content` table (differentiated by `capture_source`); `ON CONFLICT DO NOTHING` deduplication ensures first-capture-wins across layers. CRITICAL LAYERING INSIGHT: Layer 1A (Story capture) bundles with IG DM integration for near-zero incremental cost (1 line + 4–7 days). Layers are independently pauseable/deployable — App Review rejection for one layer doesn't block others. Natural product tier mapping: Basic=Layer 1, Pro=Layer 1+2, Enterprise=Layer 1+2+3. Submit all three App Reviews simultaneously at IG DM launch to run review timelines in parallel with engineering. Data flywheel: Layer 1 tag events auto-populate Layer 3 candidate pool (`ugc_creator_monitoring`); over 6–12 months pool reaches thousands of brand-adjacent accounts. Caption pre-filter eliminates 20–40% of AI calls (skip already-captured content). Cost: Layers 1+2 essentially free ($10–200/month storage); Layer 3 MVP $10–200/month (API-based); Layer 3 full $100–300/month self-hosted GPU. Native build at 100 brands: ~$300–1,500/month vs Archive $30,800/month. Total effort (DM infra exists): Layer 1 (~4–6 weeks), Layer 2 (+1–2 weeks), Layer 3 MVP (+2–3 weeks), Layer 3 full (+3–6 weeks). |
| 18 | 2026-03-01 | `synthesis-options-catalog` | 1 run | Master options catalog synthesizing all 17 Wave 1–3 analysis files. CAPABILITY MATRIX: 6 capture methods × 6 content types mapped — mentions webhook (feed @mention + comment), `/tags` polling (photo-tags; Reels excluded = confirmed API gap), Messaging API `story_mention` (Story mentions), Hashtag API (branded hashtag posts, 30-slot limit), AI Radar (untagged visual/audio), Apify (hashtag unlimited). COVERAGE: Layer 1 (official API) = 60–75%; +Layer 2 (hashtag) = 65–90%; +Layer 3 (AI Radar) = 75–95%. IG DM OVERLAP: 7/8 major infra components shared with Story capture (webhook endpoint, HMAC, `user_ig_dm_account`, IGSID, storage bucket, download service, App Review) — highest-leverage integration in the system. COMBINATION MATRIX: all official API methods are fully complementary; Apify + hashtag API overlap (Apify preferred if no slot constraint); AI Radar is synergistic (Layer 1 tag events auto-populate AI Radar watchlist). CONSTRAINTS: 30-hashtag/7-day cap (binding), Reels photo-tag API gap (permanent), 24h CDN expiry for Stories, Business Discovery covers only ~20% of IG (personal accounts excluded), no polling fallback for mentions webhook. EFFORT (DM infra exists): L1A=4–7d, L1B+1C=6–10d, L2=1–2wk, L3 MVP=5–7wk. COST: Layers 1+2 ~$10–200/month; Layer 3 $10–500/month; Archive at 100 brands = $100K+/month (60–300× more expensive). BUY-VS-BUILD: build native at any scale ≥3 brands; Archive Variant B additive only for brands already paying Archive. PRODUCT TIERS: Basic=L1, Pro=L1+L2, Enterprise=L1+L2+L3. 7 new tables, 1 ALTER, 13 existing components reused. |
