# Stage 9 — Heading Consistency

## Problem
Pages use a mix of `var(--text-h1)`, `text-3xl`, and `text-[2rem]` for headings. Inconsistent.

## Instructions
1. Standardize ALL page `<h1>` elements to use:
   ```
   className="font-display text-foreground" style={{ fontSize: 'var(--text-h1)', lineHeight: 'var(--text-h1-lh)' }}
   ```
2. Standardize ALL `<h2>` section headings to use `var(--text-h2)` or `var(--text-h3)` consistently
3. Files to check and update:
   - `routes/dashboard.tsx`
   - `routes/index.tsx`
   - `routes/computations/index.tsx`
   - `routes/computations/new.tsx`
   - `routes/computations/$compId.tsx`
   - `routes/clients/index.tsx`
   - `routes/clients/new.tsx`
   - `routes/clients/$clientId.tsx`
   - `routes/deadlines.tsx`
   - `routes/settings/index.tsx`
   - `routes/settings/team.tsx`

## Verify
- `npx vite build` passes

## Commit
`taxklaro(qa): stage 9 - standardize heading sizes`
