# Stage 8 — Landing Hero Proportions

## Problem
Hero section uses `min-h-[70vh]` — too tall, pushes feature cards below fold. Heading feels small relative to whitespace.

## Instructions
1. Read `routes/index.tsx`
2. Change `min-h-[70vh]` to `py-16 sm:py-24` (padding instead of min-height)
3. Ensure feature cards are visible without scrolling on most viewports
4. Footer should feel connected, not floating far below
5. Verify heading size feels proportionate to the space

## Verify
- `npx vite build` passes

## Commit
`taxklaro(qa): stage 8 - landing hero proportions`
