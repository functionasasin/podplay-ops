# Stage 5 — Console Errors

## Problem
1 console error appears on every authenticated page load.

## Instructions
1. Start dev server: `cd apps/taxklaro/frontend && npx vite --port 5175`
2. Open browser, sign in, check console for errors
3. Identify the error source (likely failed Supabase query, missing column, or React strict mode)
4. Fix the root cause
5. Verify zero console errors after fix

## Verify
- `npx vite build` passes
- `npx vitest run` passes
- No console errors on page load

## Commit
`taxklaro(qa): stage 5 - fix console errors`
