# Stage 7 — Dashboard Max-Width

## Problem
Dashboard content stretches edge-to-edge on wide screens. Needs max-width constraint.

## Instructions
1. Read `routes/dashboard.tsx` (created in Stage 1)
2. Add `max-w-5xl mx-auto` to the dashboard wrapper div
3. Ensure consistent padding with other authenticated pages

## Verify
- `npx vite build` passes

## Commit
`taxklaro(qa): stage 7 - dashboard max-width constraint`
