# Stage 6 — Client Detail Fields

## Problem
Client detail page may still reference wrong columns after Stage 2 fix. Ensure it displays all real DB fields.

## Instructions
1. Read `routes/clients/$clientId.tsx` and `components/clients/ClientInfoCard.tsx`
2. Verify the Supabase query selects: `id, full_name, email, phone, tin, notes`
3. Verify the `ClientDetail` interface has: `fullName, email, phone, tin, notes`
4. Verify `ClientInfoCard` renders all fields with correct labels
5. Fix any remaining mismatches

## DB Schema Reference
```sql
clients: id, org_id, full_name, email, phone, tin, notes, status, created_by, created_at, updated_at
```

## Verify
- `npx vite build` passes
- `npx vitest run` passes

## Commit
`taxklaro(qa): stage 6 - fix client detail fields`
