# Stage 27 — Flow QA: Auth Flow

## Viewport
1280x800

## Instructions
Execute this flow using Playwright, verifying each step:

1. Navigate to `/` → verify landing page renders with hero + CTA
2. Click "Get Started — It's Free" → verify redirect to `/auth`
3. Fill email: `armorlaruan@gmail.com`, password: `testpassword123!`
4. Click "Sign In" → verify redirect to `/dashboard`
5. Verify sidebar is visible with all nav links
6. Click each sidebar link → verify each page loads without errors
7. Click "Sign out" → verify redirect back to landing page

Screenshot any failures → `screenshots/flow-auth-{step}.png`

## Pass criteria
- Complete auth flow works end-to-end
- All sidebar navigation works
- Sign out works
