# Stage 17 — Desktop Verify: Clients

## Viewport
1280x800

## Instructions
1. Sign in, navigate to `/clients` via sidebar
2. Screenshot → `screenshots/desktop-clients.png`
3. Verify: page heading, empty state or client list, "New Client" button
4. Navigate to `/clients/new`
5. Screenshot → `screenshots/desktop-clients-new.png`
6. Verify: form with Client Name, TIN, Email, Phone, Notes fields
7. Check console for errors — especially no "column does not exist" errors

## Pass criteria
- Client list renders (empty state or table)
- New client form has correct fields matching DB schema
- No column mismatch errors in console
- No console errors
