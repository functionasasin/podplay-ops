# Stage 30 — Flow QA: Cross-Entity

## Viewport
1280x800

## Instructions
1. Sign in
2. Create a new computation — if wizard has client selection, assign the client from Stage 29
3. Verify computation shows client association (if applicable)
4. If share toggle exists on computation detail: enable sharing
5. Copy share URL, open in new context (or navigate directly)
6. Verify shared computation view renders results correctly
7. Check any remaining cross-entity features (deadlines, notes)

Screenshot key steps → `screenshots/flow-cross-{step}.png`

## Pass criteria
- Cross-entity references work
- Share link (if implemented) works for anonymous users
- No broken references or missing data
