# Stage 32 — Discovery: Supabase Query Audit

## Instructions
1. Grep all `.from('` calls in `src/`
2. For each table query, check `.select()`, `.insert()`, `.update()` columns against the actual schema:

```sql
-- organizations: id, name, slug, settings, created_at, updated_at
-- organization_members: id, org_id, user_id, role, created_at
-- organization_invitations: id, org_id, email, role, token, expires_at, created_at
-- user_profiles: id, display_name, avatar_url, full_name, phone, bir_tin, rdo_code, bir_registration_date, created_at, updated_at
-- clients: id, org_id, full_name, email, phone, tin, notes, status, created_by, created_at, updated_at
-- computations: id, org_id, client_id, created_by, title, tax_year, status, input_json, output_json, regime_selected, share_token, share_enabled, created_at, updated_at
-- computation_notes: id, computation_id, author_id, content, created_at
-- computation_deadlines: id, computation_id, label, due_date, status, created_at, updated_at
```

3. Fix any column mismatches
4. Build + test

## Pass criteria
- Every Supabase query uses real column names
- No "column does not exist" possible at runtime
