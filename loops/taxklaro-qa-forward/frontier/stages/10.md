# Stage 10 — Mobile Responsive Audit

## Instructions
1. Check every route file for mobile breakpoints:
   - Form inputs should be full-width on mobile
   - Cards should stack to single column on mobile (`grid-cols-1 sm:grid-cols-2`)
   - Tables should have `overflow-x-auto` wrapper on mobile
   - Landing page should have mobile-appropriate padding and font sizes
2. Check AppLayout: mobile hamburger works, sidebar hidden on small screens
3. Fix any missing responsive classes

## Files to audit
- All route files in `routes/`
- `components/layout/AppLayout.tsx`
- `components/clients/ClientsTable.tsx`
- `components/results/RegimeComparisonTable.tsx`

## Verify
- `npx vite build` passes

## Commit
`taxklaro(qa): stage 10 - mobile responsive audit`
