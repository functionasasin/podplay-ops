# Temporary logs
/tmp/ralph-github-profile-reverse-iter-*.log

# Large raw API responses (keep the processed ones)
raw/*.json.bak

# Cloned private repos (context only, never commit)
input/decision-orchestrator/
input/cheerful/
