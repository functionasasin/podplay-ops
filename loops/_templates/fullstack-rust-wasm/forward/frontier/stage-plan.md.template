# Stage Plan — {{PRODUCT_NAME}}

## Phase Overview

| Phase | Name | Workspace | Stages | Status |
|-------|------|-----------|--------|--------|
| 1 | Engine Core | engine/ (cargo test) | 0 — N | pending |
| 2 | WASM Bridge | engine/ → frontend/ (wasm-pack) | P2-1, P2-2 | pending |
| 3 | Frontend Foundation | frontend/ (vitest + real WASM) | P3-1, P3-2, P3-3 | pending |
| 4 | Frontend Components | frontend/ (vitest + real WASM) | P4-1 — P4-N | pending |
| 5 | UI Polish | frontend/ (vitest + CSS check) | P5-1, P5-2, P5-3 | pending |

## Current Stage

Not started. Run `./forward-ralph.sh auto` to begin.

## Stage Details

<!-- Phase 1 stages: one per engine pipeline step -->
<!-- Phase 2 stages: WASM export + build (always 2 stages) -->
<!-- Phase 3 stages: scaffold + types/schemas + bridge (always 3 stages) -->
<!-- Phase 4 stages: one per UI component group -->
<!-- Phase 5 stages: design system + restyle + responsive (always 3 stages) -->

## Completed Stages

(None yet)

## Notes

- Phase 1 tests: `cargo test {filter}` in engine/
- Phase 2 tests: `cargo check --target wasm32` + `wasm-pack build` + artifact verification
- Phase 3-5 tests: `npx vitest run {filter}` in frontend/ — all hit real WASM engine
- No mocks anywhere. Phase 3 Stage P3-3 wires real WASM before any components are built.
- CSS bundle size check in Phase 5: `npm run build` must produce CSS > 20KB
