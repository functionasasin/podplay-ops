# Analysis Log

| # | Timestamp | Aspect | Duration | Key Findings |
|---|-----------|--------|----------|--------------|
| 1 | 2026-02-25 | supabase-schema | 1 run | 50+ migration files; ~35 tables across email (Gmail+SMTP), campaign, creator, team, Discord, and infra domains; pgvector for email reply examples; pg_trgm for search; extensive RLS; trigger-maintained denormalized views |
| 2 | 2026-02-25 | backend-api-surface | 1 run | 26 route files; ~110 endpoints; 3 auth tiers (JWT/API-key/Slack-sig/public); key domains: campaigns, threads, creators, drafts, email, Shopify, YouTube, Slack, teams, dashboard, internal service API; async via Temporal workflows |
| 3 | 2026-02-25 | backend-services | 1 run | 12 service domains (campaign, creator, email, post-tracking, external, storage, processing, CSV, workflow, tools, composio, utils); 27+ repositories; idempotency-first via ON CONFLICT DO NOTHING; append-only thread state; two-tier content storage (DB capped + Supabase Storage full); enrichment waterfall (cache→Apify→bio crawl→Influencer Club); personalization at queue time not send time |
| 4 | 2026-02-25 | temporal-workflows | 1 run | 24 workflows across 2 task queues; central `ThreadProcessingCoordinatorWorkflow` with 20+ activities/child-workflows per thread; 2 infinite-loop schedulers via `continue_as_new`; Gmail and SMTP dual-path ingestion; AI draft generation with staleness/dedup guards; bulk edit, enrichment fan-out, post tracking, Slack digest; fire-and-forget ABANDON pattern for processing pipeline; Google Sheets isolated to dedicated queue |
| 5 | 2026-02-25 | temporal-activities | 1 run | 44 activity files, ~75 functions across 16 domains; universal `Candidate` dataclass for Gmail/SMTP polymorphism; 4 rate-limited send activities (one email/account/execution + crash recovery); 3 draft generation strategies (base LLM, RAG via pgvector, correction-injection via JSONL); `execute_campaign_workflows_activity` is most complex (Claude Agent SDK + MCP tools + schema-validated outputs); NO_RETRY pattern for all LLM activities; `ThreadProcessingCoordinatorWorkflow` calls 20+ distinct activities |
| 6 | 2026-02-25 | ai-features | 1 run | 14 AI features; LlmService is provider-agnostic (OpenAI/Anthropic routing by model name prefix); 3 draft strategies (base, RAG+Opus4.5, corrections+JSONL); 4-step RAG pipeline (summarize→embed→pgvector search→XML inject); 20+ Langfuse prompts with label versioning; ClaudeAgentService runs autonomous tool-calling Claude for user-defined campaign workflows; 10 model variants across features from gpt-4.1-nano to claude-opus-4-5; every feature @langfuse.observe for full tracing |
| 7 | 2026-02-25 | webapp-routing | 1 run | 4 layout groups: auth-pages, (mail) with 66px icon sidebar, onboarding linear funnel, standalone; 31 total routes; middleware handles all auth gates (cookie-first, DB fallback); 5 sidebar nav items (Mail, Search, Lists, Campaigns, Dashboard); onboarding is 7-step sequential funnel with Zustand persistence; /onboarding/connect-email uniquely accessible post-onboarding; PostHog recording paused on /settings; GrowthBook+PostHog dual feature flag systems |
| 8 | 2026-02-25 | webapp-campaign-wizard | 1 run | 7-step wizard (+ landing step 0 + paid-only step 3b) with Zustand store for all state; 4 campaign types (seeding/paid/sales/creator) with different step paths; AI assists at every step (email gen, FAQ gen, tracking rules, product scraping, signature rewrite); campaign submission via POST /api/campaigns/launch with FormData; draft load/save supported; creators via CSV+embedded-search+lists; 3 integrations (Google Sheets, Shopify/GoAffPro, Slack); automation level (manual vs semi-automated); folder numbering ≠ step numbering |
| 9 | 2026-02-25 | webapp-mail-inbox | 1 run | 3-panel split layout (account filter, thread list, thread detail); 5 views (Pending/Sent/Ignored/Drafts/Creators); batch account fetching reduces N×2 requests to 2; TipTap rich text editor with merge tags; "Cheerify" AI improvement via SSE streaming; auto-generated rule suggestions after manual edits; 7-day local draft persistence; silent background refresh post-mutation; flag system (wants_paid/has_question/has_issue) for triage; demo email onboarding; bulk edit support |
| 10 | 2026-02-25 | webapp-state-stores | 1 run | 12 Zustand stores across 4 directories; 3-layer state architecture (Zustand ephemeral, Zustand+persist, TanStack Query); campaign wizard store is largest (5 sections, 70+ fields, selector hooks via useShallow); mail store uses persist middleware with session-reset on user change; enrichment store runs polling loop outside React lifecycle; search store has client-side page cache (Map); useEmailSend has owner vs team-member dual send paths; bulk edit classifies edits post-send for campaign-wide application; inter-store calls via getState() for non-React contexts |
| 11 | 2026-02-25 | context-engine-core | 1 run | FCIS architecture; Slack bot via Socket Mode; 38 tools across 7 platforms (Slack/Fly/Clarify/Onyx/ACP/PostHog/Cheerful); meta-tool pattern (discover_tools+execute_tool) wraps full catalog as 2 SDK tools; claude-opus-4-6 with bypassPermissions; keyword→platform→tool selection (currently disabled); thread context persisted to DB for additive tool merging; 2-session DB pattern avoids idle connection timeouts during LLM execution; live status updater with rate-limited Slack message updates; FCIS enforced via CLAUDE.md layer rules |
| 12 | 2026-02-25 | context-engine-mcp-tools | 1 run | 38 tools across 7 platforms; @tool decorator auto-extracts Pydantic input_model; ToolRegistry validates+dispatches; 3 injected contexts (ToolContext credentials, DatabaseContext, RequestContext user identity); XML output for all tools; 34 READ-only / 4 WRITE (Fly session lifecycle + template CRUD); ACP tools enable Claude-to-Claude multi-agent via Fly sessions; Cheerful tools expose campaign/email/creator data to Slack; PostHog tools cover analytics queries (HogQL), sessions, insights, feature flags; meta-tool pattern defers full catalog until Anthropic Tool Search Tool available |
| 13 | 2026-02-25 | infra-deploy | 1 run | 3-environment topology (local/staging/prd); backend + context-engine on Fly.io, webapp on Vercel, Supabase cloud + Temporal cloud; backend: 2-process Fly app (web shared-cpu-4x/2GB + worker shared-cpu-8x/8GB); 9 GitHub Actions workflows (deploy-backend, deploy-context-engine, test, worker-heartbeat, prd-cherry-pick, ralph-loops, bugbot-triage, backfill-messages); Claude Code CLI baked into both Docker images; GitHub Variables (not Secrets) as secret source of truth; migration-before-deploy ordering; external heartbeat for worker alerting |
| 14 | 2026-02-26 | user-journeys | 1 run | 14 distinct journeys mapped end-to-end across 4 personas (Brand Manager, Campaign Operator, Team Admin, Creator); 5 cross-cutting patterns identified (AI-assisted human-confirmed, async enrichment, event-sourced thread state, personalization at queue time, multi-model strategy); full stack traces from UI → API → service → Temporal → DB → external APIs; dependency map shows journey prerequisites and enablement chain |
| 15 | 2026-02-26 | data-flow-map | 1 run | 6 major entities traced (campaign, creator, email thread, email message, product, draft); 9 lifecycle phases for email thread (detection → state creation → processing pipeline → operator review → follow-up → completion); 5 cross-cutting patterns (two-tier storage, append-only state with trigger-materialized view, idempotency via ON CONFLICT, fire-and-forget ABANDON, personalization at queue time); complete ASCII data flow diagram; entity lifecycle summary table with creation/mutation/read owners |
| 16 | 2026-02-26 | integration-points | 1 run | 15 integrations across 5 categories; Gmail OAuth (refresh-token per user, crypto-encrypted, RFC 2047 subject encoding); SMTP/IMAP (per-user credentials, APPENDUID draft lifecycle, dual TLS modes); Apify Instagram (lookalike+keyword, two-actor with different field mappings); Apify YouTube (4-actor pipeline: scraper→LLM keywords→channel finder→email extractor, parallelized email extraction); Bio Link Scraper (free HTTP, mailto+NEXT_DATA+regex, 5 platforms, image suffix filtering); Influencer Club (paid final fallback, email-in-sibling key quirk, httpx sync); Firecrawl (product scraping, 4h cache, 5-category error classification); GSheets (service account, HeaderToColumnMap normalization, gifting address guard, isolated task queue); Shopify via GoAffPro (GraphQL proxy, GID format, country_code unquoted enum); Slack ops (Block Kit order digest with approve/edit/skip, views.open modal); PostHog (webapp client-side + 9 context-engine MCP read tools); Langfuse (prompt versioning by env label, full LLM tracing with session/user attribution); Anthropic+OpenAI (model-prefix routing, structured outputs beta, tenacity retry); Composio (schema adapter, MCP wrapper, never raises); 7 design patterns (error classification, credential encryption, service-account vs OAuth, GoAffPro proxy, enrichment waterfall cost ordering, read-only context engine, sync activity boundary) |
| 17 | 2026-02-26 | auth-permissions | 1 run | Dual-layer auth (JWT + RLS); Supabase Auth with HS256/ES256 JWT verification + Google OAuth + email/password + invite flow; 3 Supabase client tiers (user/service/admin); 2-tier permission model (ownership + team assignment); team→campaign_member_assignment granular sharing; SECURITY DEFINER functions break RLS recursion; sensitive credential tables (gmail/smtp accounts) inaccessible to team members via RLS—service_role used at API layer instead; CSRF on all mutations; dev-only impersonation; onboarding gate with cookie caching; permission matrix documented for campaigns and teams |
| 18 | 2026-02-26 | campaign-lifecycle | 1 run | 8-phase lifecycle: wizard+draft → launch → outreach sending → reply ingestion+thread processing → automation+draft review → follow-up scheduling → post opt-in processing (GoAffPro/Shopify/Slack) → reporting+post tracking; 13-step atomic launch transaction; 4 campaign type divergence matrix (gifting/paid/sales/creator); 2 follow-up mechanisms (queue-level + thread-level); 3 automation levels (manual/semi/full); complete gifting pipeline (discount code → Shopify order → Slack approval → post tracking); `campaign_creator.gifting_status` tracks fulfillment; 20+ tables created across lifecycle; every phase mapped to user problem solved |
