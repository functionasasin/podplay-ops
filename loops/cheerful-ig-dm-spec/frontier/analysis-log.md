# Analysis Log

| # | Timestamp | Aspect | Duration | Key Findings |
|---|-----------|--------|----------|--------------|
| 1 | 2026-02-28 | `audit-db-schemas` | 1 run | 13 tables fully audited. GmailThreadStatus is Python StrEnum (8 values), not a PG ENUM. 5 mutual-exclusivity CHECK constraints found (all 2-way, need 3-way expansion). campaign_creator/smtp_message/smtp_thread_state/thread_flag have NO RLS. email_reply_example is the RAG table (vector(1536), HNSW index). |
| 2 | 2026-02-28 | `audit-backend-services` | 1 run | 21 files audited. GmailService/SmtpEmailService: @classmethod for_user() factory pattern, crypto_service.decrypt() for credentials. Candidate model in src/models/temporal/gmail_thread_state.py with 12 fields (needs ig_dm_account_id + ig_conversation_id). EmailLoaderService produces XML thread context. LlmService/RagService/EmbeddingService are channel-agnostic — only prompt name and RAG table change for IG DM. No processing service class; coordinator is a Temporal workflow. |
| 3 | 2026-02-28 | `audit-temporal-workflows` | 1 run | 25 workflows registered, 67 activities. ThreadProcessingCoordinatorWorkflow uses `is_smtp = candidate.smtp_account_id is not None` as top-level discriminator. IG DM adds `is_ig_dm = candidate.ig_dm_account_id is not None` branch. Task queue is "main". AllPollHistoryWorkflow loops via continue_as_new (no Temporal Schedule). SMTP pipeline: AllSmtpInboxSyncWorkflow → BatchSmtpInboxSyncWorkflow → SmtpThreadSyncWorkflow → ThreadProcessingCoordinatorWorkflow. update_state_status_activity already dispatches to SMTP repo via smtp_account_id discriminator — extend same pattern for IG DM. |
| 4 | 2026-02-28 | `audit-api-routes` | 1 run | 17 files audited. All routes mounted under /api. Auth via get_current_user (Supabase JWT Bearer). Service-to-service auth via X-Service-Api-Key header. Slack webhook pattern = reference for Meta webhook: HMAC-SHA256 verify + BackgroundTasks + immediate 200. SMTP account CRUD at /api/v1/smtp-accounts is the exact model for IG DM account CRUD. Gmail accounts are read-only from API (OAuth-managed). Key models: ThreadSummary, ThreadWithMessages, MessageInThread, DraftResponse — all need IG DM parallels. Webhook should mount at /webhooks/instagram/ directly on app (not under /api). Missing routes identified: 11 new routes needed across 3 new router files. |
| 5 | 2026-02-28 | `audit-frontend-types` | 1 run | Two-layer type system: hand-authored gmail-types.ts (GmailThread, GmailMessage) + orval-generated schemas. No explicit channel discriminator — presence of gmail_thread_state_id vs smtp_thread_state_id is implicit. SmtpAccountResponse is direct template for IgDmAccountResponse. usePendingMails/useSentMails/useIgnoredMails all call fetchAndEnrichThreads → /api/threads/list → transformThreadWithMessagesToGmailThread. Need: IgDmThread type (ig_handle, windowExpiresAt, igConversationId instead of email/subject fields), IgDmMessage (no HTML/cc/to/subject), channel discriminator added to GmailThread, igDmKeys query factory, useIgDmThreads/useIgDmMessages hooks. |
| 6 | 2026-02-28 | `audit-frontend-components` | 1 run | 9 files audited. Mail component (mail.tsx) is top-level orchestrator — 3 views (Pending/Sent/Ignored), all GmailThread[]. MailList: thread items render senderName, subject (bold, always shown, must be hidden for DM), snippet. getConversationPartnerInfo() is email-centric. MailDisplay: header has partnerName + <partnerEmail> + subject subtitle — all email fields. Reply composer uses EmailRichTextEditor (contenteditable HTML, not textarea) — must be replaced by DmComposer for DM threads. 14 email-hardcoded elements inventoried. Settings page has "Email" + "Team" tabs, EmailSettingsCard for Gmail CRUD — no IG section exists. SenderAccountsSection in campaigns is the pattern for per-campaign IG account assignment. |
