export interface PrintHeaderProps {
  firmName: string;
  caseTitle: string;
}

/**
 * Header component shown only in print media.
 * Displays firm name, case title, and page number.
 */
export function PrintHeader(_props: PrintHeaderProps) {
  // Stub — implementation in next iteration
  return null;
}
