# Analysis Log

| # | Timestamp | Aspect | Duration | Key Findings |
|---|-----------|--------|----------|--------------|
| 1 | 2026-02-28 | `tiktok-login-kit` | 1 run | OAuth 2.0 foundation for all TikTok APIs. Two token types: user (`act.`, 24h, refreshable via refresh_token for 365d) and client-credentials (`clt.`, 2h, no user needed). Non-standard `client_key` param instead of `client_id`. 2024 breaking change split `user.info.basic` into basic/profile/stats scopes. App review required (3–4 days, manual). Login Kit gates Display API, Content Posting, Data Portability. Client credentials gates Research API + Commercial Content API. |
| 2 | 2026-02-28 | `tiktok-display-api` | 1 run | 3 endpoints: /v2/user/info/, /v2/video/list/, /v2/video/query/. User-context only — requires OAuth per creator (cannot look up arbitrary creators). User fields: open_id, union_id, avatar, display_name, bio, username, is_verified, follower_count, following_count, likes_count, video_count. Video fields: id, title, description, duration, cover_image_url (expires), embed_link, share_url, view_count, like_count, comment_count, share_count, favorites_count. Rate limit: 600 req/min per endpoint. No audience demographics, no watch time, no revenue data. Key distinction vs Research API: Display = own authorized users only; Research = any public content. |
| 3 | 2026-02-28 | `tiktok-content-posting-api` | 1 run | Two modes: Direct Post (`video.publish` scope — posts live to profile) and Inbox Upload (`video.upload` scope — sends to creator's inbox as draft). Five endpoints: /v2/post/publish/video/init/, /v2/post/publish/inbox/video/init/, /v2/post/publish/content/init/ (photos), /v2/post/publish/creator_info/query/, /v2/post/publish/status/fetch/. Chunked file upload (5–64 MB chunks, max 1000) or PULL_FROM_URL. Photo/carousel posts support up to 35 images via URL-pull only. No native scheduling. Rate limits: 6 req/min init, 30 req/min status check, ~15 posts/day per creator. Unaudited apps limited to 5 users + SELF_ONLY posts. Audit required for public posting. Webhooks: video.upload.failed + authorization.removed. For Cheerful: inbox mode is easiest integration; requires creator OAuth connection. |
| 4 | 2026-02-28 | `tiktok-research-api` | 1 run | Academic/commercial research API. FREE but restricted: US/EU/UK/CH academic institutions only, ~4 week approval, EXPLICITLY prohibits commercial use — Cheerful cannot use directly. Client credentials auth (`clt.` token, 2h lifetime). 8 endpoints: Video Query (search by hashtag/keyword/region/view_count, 30-day window max), User Info (any public creator profile: followers, bio, verified, likes, video_count, bio_url), Video Comments (with PII redaction), User Followers/Following (2M records/day quota), User Liked/Reposted/Pinned Videos. Rate limits: 1,000 req/day, 100 records/req, 100K records/day (video+comments); 20K calls/day for followers. Historical data to ~2018 but quality degrades; pre-July 2024 metrics were bugged (fixed). VCE track for non-academic NGOs (sandboxed, TikTok manually reviews all outputs). Key differentiator vs Display API: Research = any public creator; Display = own authorized users only. Recommendation: not viable for Cheerful production; evaluate Wave 2 third-party providers who package this data commercially. |
| 5 | 2026-02-28 | `tiktok-shop-api` | 1 run | TikTok Shop Open Platform (Partner API v2). Base URL: open-api.tiktokglobalshop.com. Available in 17 countries (US, UK, DE, FR, IT, ES, IE, JP, SG, ID, MY, TH, VN, PH, MX, BR). Three shop types: Seller, Creator, Affiliate. Auth: OAuth 2.0 + mandatory HmacSHA256 request signing (non-negotiable). Tokens: 24h access, 1yr refresh. Six API groups: Products (CRUD, inventory, categories), Orders (full lifecycle, affiliate attribution), Fulfillment (shipping, waybills, returns), Affiliate Seller (campaign creation, creator search by GMV/keywords/demographics, creator invite, performance tracking, affiliate link generation), Finance (settlement, fees), Shop Management. Webhooks: orders, products, shop, affiliate, logistics events — at-least-once delivery. Rate limits: 50 req/sec per endpoint; 100 products/day (probation) → 1,000/day post-probation. HIGHEST CHEERFUL VALUE: Affiliate Seller API — only surface that provides creator GMV data (not in any other TikTok API), plus programmatic campaign creation and revenue attribution. Requires seller account per client; app review for affiliate scope. |
| 6 | 2026-02-28 | `tiktok-ads-marketing-api` | 1 run | Marketing API (API for Business). Base URL: business-api.tiktok.com/open_api/v1.3/. Auth: OAuth 2.0 long-lived tokens (no expiry unless revoked). Free API access — cost is ad spend only. App review: 5–7 business days. Scopes: Ads Management, Reporting, Creative Management, Audience Management. Full campaign hierarchy: campaign/adgroup/ad CRUD. Key endpoints: /campaign/create+get+update, /adgroup/create+get+update, /ad/create+get+update, /report/integrated/get/, /dmp/custom_audience/* (custom audiences + lookalike), /pixel/* (tracking). SPARK ADS: highest Cheerful value — programmatically authorize creator post (/tt_video/authorize/ via auth code) then create paid campaign boosting that organic content. Engagement attributed to creator's post. 142% higher engagement, 43% higher conversion vs standard ads. Creator must generate auth code in-app (7–365 day window, up to 20 at once). Smart+ (Oct 2025 update): AI-automated campaigns, Symphony generative AI for creatives, catalog ads (36% lower CPA), modular automation controls. Reporting: highly configurable — all metrics (impressions, clicks, video views, completions, follows, conversions, spend) × dimensions. Commercial Content API (/v2/research/adlib/): read-only access to public ad library — useful for competitive intel. Regional: globally available; US-only for household income/spending power targeting; EU compliance requirements post-Jan 2026. Critical dependency: brand clients need own TikTok Ads Manager accounts; Cheerful acts as Business Manager. |
| 7 | 2026-02-28 | `tiktok-creator-marketplace-api` | 1 run | TCM/TikTok One API (now under Organic API suite). Auth: OAuth 2.0 via API for Business portal. Pool: ~2M TTCM-registered creators (US: 10K+ follower min). Five organic APIs: TTO (creator discovery/orders, Open Beta), Discovery (trending hashtags/music), Mentions (brand mention tracking), Accounts (video publishing/insights), Spark Ads Recommendation (boost candidate identification). Unique first-party data: completion rate, audience retention graphs, precise demographics — not available from any scraper or third-party. Order-based workflow: keyword detection NOT allowed; brands create orders → send invite links → creators upload via TikTok app → auto-tracked. Campaign code for retroactive linking. Spark Ads authorization bundled with order acceptance — seamless organic-to-paid amplification. Webhooks: order lifecycle events. Rate limit: 20 req/min per user token. App review: 5–7 days. Access constrained: TTO API still in global beta (contact TikTok rep). Commercial Content API (separate) = EU-only ad transparency research tool. Cheerful applicability: HIGH for enrichment (unique metrics) and campaign tracking (compliant method); constrained discovery pool vs Display API/Apify for long-tail creators. |
| 8 | 2026-02-28 | `tiktok-messaging-comments-api` | 1 run | Six distinct API surfaces. CRITICAL GAP: no official API to proactively DM creators for outreach. Comment reads: Research API (POST /v2/research/video/comment/list/, academic-only, prohibited for commercial use); VCE task-based bulk export (100K records/day). Business API Comment suite (Comment List, Comment Post/reply, Comment Status Update hide/unhide, Comment Delete, Blockedword CRUD) — for ad content + owned business account; NOT for arbitrary creator content. Organic API Mentions (Open Beta, March 2025) — brand @mention tracking in captions/comments/replies; brand perspective only. Business Messaging API: B2C only (consumer DMs to brand), non-US/EEA/CH/UK only, 48h window, user must initiate, max 10 msgs/window, no video/voice/stickers. Data Portability API: DM archive export only (not real-time). Webhooks: only 4 events exist (authorization.removed, video.upload.failed, video.publish.completed, portability.download.ready) — NO comment or DM webhooks. Cheerful: creator DM outreach via TikTok API is impossible; must use email (Gmail API) or TCM order-based flow; comment enrichment needs Wave 2 third-party providers. |
| 9 | 2026-02-28 | `tiktok-live-api` | 1 run | NO official public Live API exists on developers.tiktok.com. RTMP streaming is creator-facing (in-app RTMP credentials, session-specific key, agency/follower gated), not a developer API. Real-time live event data (chat, gifts, viewer count, battles, follows, polls, 100+ proto events) only accessible via unofficial reverse-engineered WebSocket libraries: TikTok-Live-Connector (Node.js) and TikTokLive (Python) — both connect to TikTok's internal Webcast push service without credentials for public streams. Risk: HIGH TOS violation for commercial use, API breakage common. Third-party: Euler Stream provides commercial enterprise wrapper, free tier exists. Post-live analytics: Display API covers video recording metrics only (24-48h lag); peak viewers, gift revenue, live duration are NEVER exposed via any API. Live commerce handled via TikTok Shop API (product tagging, affiliate links, post-live order reporting) — separate integration path from live data. Gift/revenue data is a hard permanent gap across all access paths. Cheerful: low-relevance for core workflows; post-live recordings trackable via Display API; unofficial libs only for live enrichment (is-creator-live? signal); live commerce is distinct Shop API workflow. |
| 10 | 2026-02-28 | `tiktok-webhooks-events` | 1 run | THREE distinct webhook systems: (1) Developer Platform (developers.tiktok.com) — only 4 events: authorization.removed, video.upload.failed, video.publish.completed, portability.download.ready. HMAC-SHA256 verification via `TikTok-Signature` header (format: `t={ts},s={sig}`), signed payload = `{timestamp}.{body}`, key = client_secret. At-least-once delivery, 72h retry via exponential backoff, no selective subscription (all-or-nothing). (2) TikTok Shop — separate system in Partner Center; selective subscription by event_type; ORDER_STATUS_CHANGE and RETURN_STATUS_CHANGE confirmed; other commerce events exist but gated. (3) Events API — NOT inbound webhooks; this is outbound server-to-server conversion data sent TO TikTok for ad attribution; 13 standard events (Purchase, ViewContent, AddToCart, etc.) with hashed PII match keys. Business API adds: lead generation events, ad review status, TCM order lifecycle (all same infrastructure, Business account required). CRITICAL GAP: no webhooks for comments, followers, video metric updates, new posts, or live events — those require polling. Cheerful integration: thin FastAPI receiver → HMAC verify → 200 immediately → enqueue to Temporal workflow. |
| 11 | 2026-02-28 | `tiktok-embed-oembed` | 1 run | Zero-auth integration surface. Video oEmbed: GET https://www.tiktok.com/oembed?url={video_url} — returns version, type="video", title (caption), author_name (username), author_url (profile URL), thumbnail_url (720×1280), thumbnail_width, thumbnail_height, html (blockquote + embed.js script). No API key required. Creator Profile oEmbed: same endpoint with profile URL → type="rich", shows follower/following/likes/10 recent videos when rendered. Embed Player: https://www.tiktok.com/player/v1/{video_id} — customizable iframe (autoplay, muted, loop, controls, music_info, description, progress_bar etc.); bidirectional postMessage API: commands (play/pause/seekTo/mute/navigateTo) + events (onPlayerReady, onStateChange, onCurrentTime, onVolumeChange, onImageChange, onPlayerError). Data extractable without auth: caption, username, profile URL, thumbnail URL, video ID (parsed from html). Follower/like counts visible only in rendered DOM (not JSON) → needs headless browser. Private + underage accounts cannot be embedded. No documented rate limits. Commercial use technically requires TikTok written consent. Cheerful value: embedded video player for campaign review UI (HIGH), zero-auth thumbnail fetch (MEDIUM), no use for discovery or metrics tracking. |
| 12 | 2026-02-28 | `third-party-apify-tiktok` | 1 run | 15+ TikTok actors across three publishers (clockworks, apidojo, novi). Cheerful already uses Apify for Instagram (`apify.py`) and YouTube (`youtube_apify.py`) — TikTok is a drop-in extension using the same `ApifyClient.actor().call()` + dataset pattern. TOP ACTORS: `clockworks/tiktok-profile-scraper` ($0.30/1K posts, 40+ fields, 425 posts/sec — PRIMARY enrichment), `clockworks/tiktok-user-search-scraper` (keyword creator discovery, mirrors existing Instagram search), `clockworks/tiktok-video-scraper` (URL-based metric polling for post tracking), `clockworks/tiktok-trends-scraper` (trending hashtags/sounds/creators by region+industry), `clockworks/tiktok-ads-scraper` (unique: audience age/interest/country data at hashtag level). PRICING: Most clockworks = $0.005/item; profile scrapers = $0.30/1K posts; apidojo API = $0.006/query. KEY GAP: no actor provides creator-level audience demographics or email — those require TCM/TikTok One and manual outreach respectively. RISK: Medium TOS exposure, acceptable for commercial influencer platform use via Apify infrastructure. NEW ACTOR DESIGNS: `TikTokApifyService` class + 5 new config actor ID settings needed. |
| 13 | 2026-02-28 | `third-party-phantombuster-tiktok` | 1 run | PhantomBuster has effectively sunset TikTok support. Platform officially lists LinkedIn, Instagram, Twitter/X, Facebook, Reddit, Google Maps — TikTok NOT in current platform docs or support pages. No named TikTok phantoms (e.g., "TikTok Profile Scraper") appear in indexed documentation; third-party review sites cite generic capabilities not verifiable in official sources. Pricing: Start $56/mo (20 exec hrs), Grow $128/mo (80 hrs), Scale $352/mo (300 hrs) — execution-time model is expensive for batch data extraction (est. $300–400 for 10K profiles vs $3–30 on Apify PPR). Risk: HIGH — automation runs as connected account (vs Apify's anonymous proxies), meaning TikTok bans the brand's account, not just an IP. No write actions, no audience demographics. VERDICT: Do not build PhantomBuster TikTok integration for Cheerful. Apify is categorically superior on all dimensions. |
| 14 | 2026-02-28 | `third-party-platform-connectors` | 1 run | Four iPaaS platforms assessed. Composio: 12 TikTok tools (5 content posting, 3 user data, 4 ads/business) — Cheerful already uses it; lowest-friction TikTok integration path for connected-creator data and posting; Free tier 5K–20K calls, Starter $99/mo. Zapier: two apps — TikTok Lead Generation (trigger: New Lead from Instant Form) + TikTok Conversions (action: Send Lead Event, free up to 100K); ads-only, no influencer use. Make.com: four modules — Campaign Management (full CRUD triggers/actions/searches), Audiences (6 custom audience types), Lead Forms (New Lead trigger), Conversions; ads-focused; $10.59–34.12/mo plans; requires API access approval. n8n: NO official TikTok node; community node `@igabm/n8n-nodes-tiktok` covers video/photo post + user profile; not in Cheerful stack. CROSS-PLATFORM GAP: none solve creator discovery for arbitrary public profiles — all require creator OAuth. VERDICT: Only Composio relevant for Cheerful (existing infrastructure); skip Zapier/Make/n8n as dependencies. |
| 15 | 2026-02-28 | `third-party-data-providers` | 1 run | Seven providers surveyed. Modash: 350M+ TikTokers, Discovery API $16,200/yr or platform $199–$599/mo; unique for collaboration history + email unlock; Raw API $10,000/yr for live data. HypeAuditor: custom pricing, 35+ TikTok metrics, UNIQUE fake follower/fraud score — no other path provides this. Pentos: TikTok-only, no API (custom export only), $99–$299/mo — dashboard trend tool only. Socialinsider: API on $333/mo+ Advanced plan, requires TikTok account connection — useless for discovery. EchoTik: TikTok Shop-specific, 40+ endpoints, platform $9.9–$57/mo, API pricing custom — creator GMV data without brand seller account. Phyllo: creator-permissioned via official TikTok API, ~$199/mo+, UNIQUE audience demographics (gender/age/location not available via any scraper) — post-acceptance enrichment only. Data365: public data scraping, custom pricing, backup to Apify. KEY INSIGHT: Apify vs Modash is the core discovery cost tradeoff ($0.005/item raw vs $16,200/yr for collaboration history + emails); Phyllo fills the demographics-after-onboarding gap that no other provider covers. |
| 16 | 2026-02-28 | `third-party-social-management` | 1 run | Nine platforms surveyed (Hootsuite, Sprout Social, Later, Buffer, Planoly, Loomly, SocialBee, Metricool, Agorapulse). VERDICT: none solve creator discovery — all require creator OAuth to the platform; they are account management tools, not data access APIs. THREE have developer APIs: Hootsuite (gated, enterprise), Sprout Social (Advanced plan, $399/seat), Metricool (Advanced plan, $53/mo — most accessible). Metricool standout: unique TikTok metrics (view source breakdown For You/Following/Profile/Search, daily follower gain/loss, full video completion %), music integration for Business accounts, public documented API. Sprout Social: unlimited historical data storage (TikTok native = 60 days only). Hootsuite: only platform with partial DM support (Business accounts, non-EEA/CH/UK — still not proactive outreach). Agorapulse: ad comment monitoring for TikTok Spark Ads campaigns; 27 documented TikTok API limitations (most transparent). SocialBee: unique evergreen content recycling. Direct Cheerful integration: not recommended for any platform — Apify/Modash/direct API superior for all core workflows. |
| 17 | 2026-02-28 | `unofficial-scraping-methods` | 1 run | Six method tiers assessed. DIY libraries: `TikTokApi` (davidteather, v7.2.2, Playwright-based, actively maintained) — full public profile/video/hashtag/comment access; breaks every 4–8 weeks when TikTok rotates X-Bogus/X-Gnarly signatures; residential proxies required; production risk HIGH. `pyktok` (hidden JSON extraction, ~30 items/page, lighter footprint, low-volume research only). `TikTokLive` (WebSocket reverse-engineering, 100+ real-time live events: comments, gifts, viewer count, battles; signature server dependency via Euler Stream; multiple language SDKs). Managed unofficial APIs: ScrapeCreators (pay-per-credit, audience demographics 26 credits/call — unique capability gap filler, real-time, handles proxy rotation internally). EnsembleData ($100+/mo, multi-platform, unique transcript/speech-to-text capability). Legal: ToS explicitly prohibits all scraping; U.S. courts (hiQ, Van Buren, Meta v. Bright Data 2024, X v. Bright Data 2024) protect public-data scraping from CFAA liability but not ToS breach; GDPR/CCPA exposure for EU/CA user PII. Anti-bot: X-Bogus + X-Gnarly required signatures, browser fingerprinting, behavioral analysis, CAPTCHA, rate limiting, IP blocking. Cheerful verdict: Managed APIs (ScrapeCreators for demographics, EnsembleData for transcripts) as Tier 2 supplements to Apify; DIY only for prototyping; never for login-walled or PII-heavy data. |
| 18 | 2026-02-28 | `cheerful-creator-discovery-pipeline` | 1 run | Two platforms supported today (Instagram via Apify lookalike/keyword search; YouTube via 4-step Apify pipeline with LLM keyword extraction). Data model: Creator table is platform-agnostic (platform TEXT + handle TEXT unique key, profile_data JSONB for platform-specific fields) — no schema changes needed for TikTok basic integration. Discovery flow: CampaignDiscoveryWorkflow → discover_creators_for_campaign_activity (currently Instagram-only, needs platform dispatch). Enrichment waterfall: cache → Apify scrape → bio-link crawl → Influencer Club (bio_link_apify.py already handles Linktree/Beacons, reusable for TikTok). Key TikTok integration points: (1) new tiktok_apify.py service using clockworks actors, (2) save_creator_from_tiktok() in creator_service.py, (3) tiktok elif branch in enrichment_service.py, (4) platform dispatch in campaign_discovery_activity.py. Email discovery bottleneck: TikTok creators rarely expose email in bio — bio-link crawl is primary path. Official Display API path requires creator OAuth (Login Kit) — deep integration, not quick win. Recommended order: Pydantic models → TikTokApifyService → storage function → enrichment branch → discovery dispatch → LLM lookalike pipeline → Display API OAuth. |
| 19 | 2026-02-28 | `cheerful-content-tracking-model` | 1 run | Post tracking system is deeply Instagram-specific across 9 files: creator_post table has `instagram_post_id` column (not `platform_post_id`), PostType only has "post"/"story"/"reel", TrackableCreator model has `instagram_handle` field, get_trackable_creators_activity skips creators without Instagram handles, fetch_instagram_posts() uses apify/instagram-profile-scraper, analyzer prompt says "Instagram", CreatorPostResponse exposes `instagram_post_id` in API. Schema migration required: add `platform` TEXT + `platform_post_id` TEXT + `platform_metrics` JSONB columns; backfill from instagram_post_id; drop old column. campaign_creator table is already platform-agnostic (social_media_handles JSONB already stores {platform:"tiktok",...}, tracking date fields are platform-neutral). TikTok-unique metrics requiring storage: share_count, collect_count, hashtags[], music_id, duet_from_id, stitch_from_id — all go in platform_metrics JSONB. New opportunities: hashtag campaign monitoring (distinct from per-creator polling), duet/stitch detection (if campaign has a hero TikTok video), sound tracking. Effort: ~6.5 days total across 9 files + 2-phase migration. |
| 20 | 2026-02-28 | `cheerful-campaign-workflow-touchpoints` | 1 run | Mapped 9 distinct campaign lifecycle touchpoints to TikTok integration opportunities across Cheerful's 24 Temporal workflows. Key findings: (1) Discovery: add TikTok platform branch to CampaignDiscoveryWorkflow using Apify keyword/hashtag actors — no new infrastructure needed. (2) Enrichment: bio-link crawl is immediately reusable; Apify profile rescrape is quick add; Display API requires Login Kit OAuth first. (3) Outreach: no official TikTok DM API — email remains sole channel for now. (4) Thread processing: add TikTok handle extraction and Spark Ads code flag to LLM extraction activities. (5) Opt-in: Spark Ads workflow (new) extracts creator code → Marketing API → boosts video; Login Kit OAuth unlocks Display API tier; TikTok Shop enables affiliate tracking. (6) Content tracking: Apify video polling (48h, same cadence) + new hashtag/sound tracking activities + duet/stitch attribution. (7) Reporting: new Sheets columns (plays, shares, saves, share rate, save rate) + Spark Ads performance reporting. (8) Lookalike: TikTok branch for generate_lookalikes_for_opt_in using Apify keyword search + LLM (mirrors YouTube pattern). 4 net-new workflows identified: TikTokHashtagTrackingWorkflow, TikTokCreatorAuthWorkflow, SparkAdsWorkflow, TikTokShopOrderWorkflow. |
| 21 | 2026-02-28 | `cheerful-data-model-extensions` | 1 run | Exhaustive blueprint of all database changes for TikTok. 4 existing tables assessed, 2 new tables designed. Key findings: (1) `creator` table — NO migration needed; `platform='tiktok'` and `profile_data JSONB` already accommodate all TikTok-specific fields. (2) `creator_post` — REQUIRES 2-phase migration: Phase 1 adds `platform`, `platform_post_id`, `platform_metrics` columns + backfills from `instagram_post_id`; Phase 2 drops old column. `PostType` extended with "video"/"photo"/"live"/"duet"/"stitch". `MatchMethod` extended with "duet"/"stitch"/"hashtag"/"sound". `platform_metrics` JSONB stores share_count, collect_count, hashtags[], music metadata. (3) `campaign_creator` — NO migration; `social_media_handles` JSONB already supports `"tiktok"` platform; optional `tiktok_affiliate_link TEXT` deferrable to Phase 2. (4) `campaign` — NO migration; `discovery_config` JSONB extended for TikTok; new optional `tiktok_config JSONB` column (campaign_hashtag, hero_video_id, ad_account_id, etc.). (5) NEW `creator_social_auth` table — stores TikTok Login Kit OAuth tokens (access_token 24h, refresh_token 365d, UNIQUE per creator+platform). (6) NEW `tiktok_shop_order` table — TikTok Shop affiliate orders with full status enum and attribution chain. Total: 8 migrations across 3 phases, ~4-5 weeks effort. |
