# Gap Matrix — Mama Sita's Campaign

> Consolidated list of every missing feature discovered during stage analysis.
> Sorted by build priority: P0 = blocks campaign, P1 = degrades experience, P2 = nice to have.

| # | Gap | Stage | Impact | Workaround | Priority | Effort | Spec Source |
|---|-----|-------|--------|------------|----------|--------|-------------|
| 1 | `cheerful_connect_ig_account` CE tool not built | Stage 1: Campaign Setup | Blocks connecting IG account — entire IG DM flow cannot start | Direct API call during development; no production workaround | P0 | M (CE tool build after spec-ce-ig-dm-tools complete) | `loops/cheerful-ig-dm-spec/PROMPT.md` (spec-ce-ig-dm-tools pending) |
| 2 | `CampaignSenderCreate` schema not extended for `ig_dm_account_id` | Stage 1: Campaign Setup | `cheerful_create_campaign` rejects IG account as sender with 422 error | Create campaign without sender; insert `campaign_sender` row with `ig_dm_account_id` via direct DB or separate endpoint | P0 | S (schema change + validation update) | `loops/cheerful-ig-dm-spec/analysis/spec/db-migrations.md` §Section 1 |
| 3 | Meta App Review required for Advanced Access permissions | Stage 1: Campaign Setup | Outbound DMs and inbound webhooks blocked for non-test accounts in Development Mode | Test with App Role accounts; submit App Review before campaign launch (budget 1-2 weeks) | P0 (timeline) | External — Meta process | `loops/cheerful-ig-dm-spec/analysis/spec/meta-oauth.md` §1.3 |
| 4 | `cheerful_list_ig_accounts` CE tool not built | Stage 1: Campaign Setup | Cannot verify IG connection status via CE | Query `user_ig_dm_account` table directly | P0 | S (simple CE tool) | `loops/cheerful-ig-dm-spec/PROMPT.md` (spec-ce-ig-dm-tools pending) |
| 5 | No IG DM message template field on campaign model | Stage 1: Campaign Setup | DM outreach message must be stored in `body_template` (email field) — misleading and no DM-specific placeholder docs | Use `body_template` as DM template; document convention; `{name}` placeholder works | P1 | M (add `ig_dm_template` field to campaign model + CE tools) | None (design gap) |
| 6 | `subject_template` required by `cheerful_create_campaign` for IG-only campaigns | Stage 1: Campaign Setup | Must pass meaningless placeholder value; confuses CE interaction | Pass `"[IG DM Campaign]"` as placeholder | P1 | S (make optional, or skip for gifting+IG campaigns) | `loops/cheerful-ce-parity-reverse/specs/campaigns.md` |
| 7 | `cheerful_create_product` and `cheerful_list_products` not built yet | Stage 1: Campaign Setup | Cannot create product records via CE | Direct API call to `POST /products/` or webapp product UI | P1 | S (CE tool build — spec complete) | `loops/cheerful-ce-parity-reverse/specs/campaigns.md` §Products |
| 8 | `cheerful_search_creators_by_keyword` CE tool not built | Stage 2: Creator Discovery | Cannot run keyword creator search via CE — primary discovery mechanism blocked | Direct API call to existing main route `POST /v1/creator-search/keyword` with Bearer token | P0 | M (new service route + CE tool; spec complete in creators.md) | `loops/cheerful-ce-parity-reverse/specs/creators.md` §Creator Search & Discovery |
| 9 | `cheerful_search_similar_creators` CE tool not built | Stage 2: Creator Discovery | Cannot run similarity-based discovery via CE | Direct API call to `POST /v1/creator-search/similar` with Bearer token | P0 | M (new service route + CE tool; spec complete in creators.md) | `loops/cheerful-ce-parity-reverse/specs/creators.md` §Creator Search & Discovery |
| 10 | `cheerful_create_creator_list` and `cheerful_list_creator_lists` CE tools not built | Stage 2: Creator Discovery | Cannot create or view creator lists via CE — no way to stage discovery results | Direct API calls to `POST /v1/lists/` and `GET /v1/lists/` | P0 | M (4 new service routes + CE tools; spec complete in creators.md) | `loops/cheerful-ce-parity-reverse/specs/creators.md` §Creator Lists — CRUD |
| 11 | `cheerful_add_search_creators_to_list` CE tool not built | Stage 2: Creator Discovery | Cannot add IC search results to creator list via CE — discovery results cannot be persisted | Direct API call to `POST /v1/lists/{list_id}/creators/from-search` with Bearer token | P0 | M (new service route + CE tool; spec complete in creators.md) | `loops/cheerful-ce-parity-reverse/specs/creators.md` §Creator List Items |
| 12 | `INFLUENCER_CLUB_API_KEY` env var must be provisioned | Stage 2: Creator Discovery | All IC discovery tools return 503 if key not configured — discovery completely blocked | Configure env var from IC dashboard | P0 (infra/config) | XS (env var config only) | `loops/cheerful-ce-parity-reverse/specs/creators.md` §Creator Search & Discovery |
| 13 | No hashtag-based creator discovery tool | Stage 2: Creator Discovery | Cannot find creators by #SinigangRecipe, #LutongPinoy, #FilipinoFood — high-precision discovery vector lost | Manual: search hashtags on Instagram → note handles → add manually via `cheerful_add_creators_to_list` (also not built) | P1 | L (requires IG Graph API hashtag search + new CE tool + new service route) | No existing loop spec covers this |
| 14 | IC coverage may be limited for Philippine micro-creators (5K-15K followers) | Stage 2: Creator Discovery | Smaller Filipino food creators may not be indexed in IC — discovery pool under-populated | Supplement with manual Instagram search + hashtag browsing; add creators by handle manually | P1 | L (third-party data gap — not fully solvable by Cheerful alone) | External data quality issue |
| 15 | All 8 creator list/vetting CE tools not built (`cheerful_list_creator_list_items`, `cheerful_start_creator_enrichment`, `cheerful_get_enrichment_workflow_status`, `cheerful_enrich_creator`, `cheerful_remove_creator_from_list`, `cheerful_add_creators_to_list`, `cheerful_add_list_creators_to_campaign`, `cheerful_get_creator_profile`) | Stage 3: Creator Vetting | Entire vetting stage is manual without these tools — no CE-mediated filtering, enrichment, or campaign transfer | Direct API calls to `/v1/` main routes with Bearer token during development | P0 | L (8 new service routes + CE tools; all specs complete in creators.md) | `loops/cheerful-ce-parity-reverse/specs/creators.md` |
| 16 | Engagement rate not stored on Creator record from `cheerful_add_search_creators_to_list` import | Stage 3: Creator Vetting | Primary vetting criterion (ER ≥ 3.0%) unavailable without re-fetching IC data per creator — 112 extra IC API calls or manual Apify computation | Re-fetch via `cheerful_enrich_creator` spot-checks; compute ER manually from `cheerful_get_creator_profile` post metrics | P1 | S (add `engagement_rate` to `CreatorFromSearchData` schema and Creator UPSERT; one-line fix) | `loops/cheerful-ce-parity-reverse/specs/creators.md` §Creator List Items |
| 17 | No bulk remove from creator list | Stage 3: Creator Vetting | Removing ~50 rejected creators = 50 individual `cheerful_remove_creator_from_list` calls — slow and tedious in Slack | Bypass pool cleanup: build shortlist directly from approvals without removing from pool; or call single-remove in sequence | P1 | S (add `DELETE /api/service/lists/{list_id}/creators/bulk` accepting `creator_ids[]`) | `loops/cheerful-ce-parity-reverse/specs/creators.md` §Creator List Items |
| 18 | No bulk filter on creator list by ER, follower count, last-post date, or content category | Stage 3: Creator Vetting | Cannot query "all creators with ER < 3%" from the list — must review all 112 profiles individually to find rejects | Manual review; CE agent iterates and presents sorted results by follower count or ER (computed on-the-fly) | P1 | M (add filter params to `GET /api/service/lists/{list_id}/creators`: `min_er`, `max_followers`, `active_since`) | No existing spec |
| 19 | No AI content relevance analysis for creator posts | Stage 3: Creator Vetting | Cannot automatically detect food content vs. restaurant reviews — requires human to read each creator's latest post captions | Brand rep manually scans captions from `cheerful_get_creator_profile` response for Filipino food keywords | P1 | L (requires LLM classification tool over latest posts + CE tool; similar to `cheerful_list_posts` match_method='llm') | `loops/cheerful-reverse/` (LLM vision analysis exists for post verification — could be adapted) |
| 20 | No batch Apify profile fetch | Stage 3: Creator Vetting | Fetching 112 creator profiles = 112 sequential Apify runs (~5-10 minutes); no parallelization supported | Accept as-is; CE agent runs sequentially; 24h cache makes repeat checks free | P2 | M (add batch variant: `POST /api/service/creator-search/profiles` accepting `handles[]`) | No existing spec |
| 21 | Apify INSTAGRAM_SCRAPER token must be provisioned | Stage 3: Creator Vetting | `cheerful_get_creator_profile` returns 500 for Instagram handles if Apify token not configured | Configure `APIFY_API_TOKEN` in backend env | P0 (infra/config) | XS (env var config only) | `loops/cheerful-ce-parity-reverse/specs/creators.md` §Creator Search & Discovery |
