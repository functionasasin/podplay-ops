# Analysis Log — PodPlay Ops Wizard Reverse Loop

Entries in reverse chronological order (newest first).

| Timestamp | Aspect Code | Output File | Key Findings |
| 2026-03-07 | qa-validation-error-messages | final-mega-spec/ui-spec/validation-messages.md | Complete validation error message spec. 140+ exact error strings across 6 pages. Wizard Intake: 21 field-level messages (Steps 1-5 Zod rules), 9 Step-6 blocking checklist messages, 7 ISP cross-validation inline banner messages (Starlink block, speed warnings, Philippines static IP hard block, backup ISP warning), Philippines hard-block behavior documented (disables Continue button, no tooltip). Wizard Procurement: BOM inline edit errors (qty/cost/shipping/margin), optimistic rollback message, duplicate item guard, regenerate confirmation text, remove confirmation text, PO form (vendor/date/line-item rules), receiving over-qty guard, CC terminal form (qty/cost/dates), replay signs status transition guard tooltips (3 guards), advance-to-deployment confirmation + 5 exit-condition checklist messages. Wizard Deployment: entry guard banner, 4 status-transition form schemas (Mark Shipped/Installing/GoLive/Flag Issue), checklist optimistic rollback toast, note auto-save inline error, modal inline status error, Phase 5 ISP configuration 5-banner spec. Wizard Financials: contract guard banner, deposit/final invoice forms, send-final guard tooltip + bypass toast, mark-paid form, expense dialog (date/category/amount/payment rules including future-date guard), delete expense confirmation, go-live date form, project-complete guard tooltip + missing-item checklist + confirmation text. Inventory: adjustment dialog (type/qty/reason), PO form (vendor/date/items), reorder threshold inline error + silent success. Settings: 8 pricing fee fields (min 0), 4 rate fields (min/max), 2 cross-validation refines with specific field-level messages, 5 threshold fields, 4 ISP speed fields, 10 system settings fields (IP regex, port range, VLAN 1-4094), catalog form (SKU regex + unique constraint message + immutability help text, URL format, cost nullable), OpEx (2 fields), travel (3 fields), contacts (8 fields including email format). Toast spec: success=3s, error=5s, warning=4s, all bottom-right (sonner). Disabled button tooltip implementation pattern (wrapping span required for hover on disabled button) documented. |
| 2026-03-07 | qa-enum-display-labels | final-mega-spec/ui-spec/enum-labels.md | Complete enum display label mapping. 94 values across 21 Postgres enum types. Every enum has: (1) display label string; (2) badge className (Tailwind bg/text/border color); (3) TypeScript record constant with type-safe keys; (4) select option labels where enum appears in a form dropdown. Key label decisions: service_tier short badge labels (PRO/AUTO/A+/PBK); deployment_status 'qc' → 'QC', 'completed' → 'Complete' (not 'Completed'); revenue_stage labels match dashboard RevenueStageCell component (Proposal/Signed/Deposit sent/Deposit paid/Final sent/Final paid); expense_category 'car' → 'Car Rental', 'taxi' → 'Taxi / Rideshare', 'misc_hardware' → 'Misc Hardware'; isp_type adds "(recommended)" suffix only in select option, not badge; replay_service_version has both short label (V1/V2) and long select label; bom_category has sort order 1-10 for consistent group header rendering; movement_type has direction sign (+/-) and color class for history table; consolidated export module src/lib/enum-labels.ts defined with all 24 named exports. |
| 2026-03-06 | converged | status/converged.txt | Loop converged at 57/57 aspects (100%). Final convergence check passed: (1) Placeholder sweep clean — zero banned patterns remaining after fixing 7 "etc." instances in schema.md (isp_type enum comment, contact_via comment), customer-onboarding.md (venue_address_line2 description), invoicing-expenses.md (misc_hardware description), financials-view.md (HER OpEx helper text), supabase-functions.md (2 JSDoc comments); (2) 2 BLOCKED aspects (source-mrp-sheets, source-existing-data) marked SKIPPED — XLSX not present in repo; structure derived from design doc + usage guide + other aspects; real data import deferred to operator via migration scripts; (3) All 31 spec files present and complete; (4) status/converged.txt written with full forward-loop instructions. Spec is ready for forward loop to build the full app. |
| 2026-03-06 | audit-data-migration | final-mega-spec/deployment/data-migration.md | Complete XLSX→Supabase migration plan. 10 MRP sheets mapped: (1) Installer Directory → installers; (2) CUSTOMER MASTER + Form Responses + MASTER ACCOUNTS → projects (tier/status/revenue_stage normalization tables provided); (3) per-customer BOM tabs → project_bom_items via template regeneration (recommended) or direct parse; (4) INVENTORY → inventory + opening-balance inventory_movements; (5) ORDER INPUT → purchase_orders + purchase_order_items; (6) per-customer status tabs → deployment_checklist_items via template generation (all-complete for finished projects, all-unchecked for in-progress); (7) INVOICING → invoices (deposit + final rows per project, status derived from date columns); (8) EXPENSES → expenses (12-category + 2-method normalization maps); (9) Customer Replay Signs → replay_signs (status derived from date columns, outreach_channel normalized); (10) CC Form → cc_terminals (status from dates, has_front_desk back-sync). Migration script structure: scripts/migrate-from-xlsx/ with 10 step modules + validate.py. 8 post-migration validation queries. 7 known data quality issues documented (XLSX not in repo, name inconsistencies, PII credentials, unmatched catalog items, partial checklist state, PH projects, PBK pricing). Settings salary fields set to 0.00 at deploy — update from HER sheet post-migration. |
| 2026-03-06 | audit-placeholder-sweep | final-mega-spec/ (15 files patched) | Complete sweep of all 31 spec files. Banned pattern audit: zero remaining TBD/TODO/FIXME/placeholder— instances. Key fixes: (1) Mac Mini chip year resolved to M4 base (2024, MYH33LL/A, $799); (2) iPad PoE adapter resolved to StarTech POESLT1UC USB-C PoE Splitter 802.3af/at; (3) iPad kiosk case resolved to Heckler Design Mount Plus H915X-BG for iPad 10th gen; (4) Philippines 220V compatibility resolved — all Apple/UniFi hardware is 100-240V auto-switching, UPS must be 220V-rated; (5) Autonomous+ surveillance add-on clarified — no extra service fee, surveillance is hardware-only in BOM; (6) Installer seed data block rephrased from "TBD from XLSX" to explicit migration note pointing to deployment/data-migration.md; (7) Team salary values (Niko, Chad, Stan) rephrased from "unknown — requires XLSX" to "set to 0.00 at deployment; imported from MRP HER sheet during initial data migration"; (8) Andy team_opex allocation clarified as 0%/0% (PM role not in HER cost pool); (9) Stan allocation set to 50/50 mirroring Niko (config role = direct hardware work); (10) Switch/SSD/NVR sizing breakpoints rephrased to "best estimates; verify during data migration"; (11) PBK fee comment changed from "placeholder" to "default — negotiated per contract"; (12) FreeDNS "[add]" button literal changed to "click the 'Add' button"; (13) FreeDNS Destination "placeholder — will be auto-updated" changed to "temporary IP — cron overwrites automatically"; (14) V2 UI note "will be added when V2 launches" changed to "V2 checklist pending V2 service release (targeted April 2026)"; (15) SOW template 'TBD' changed to '(not yet assigned)' / '(not scheduled)'; (16) wizard-intake.md "Edit ↑" etc. expanded to all 5 steps; (17) Aging badge "0-30, 31-60, etc." expanded to all 4 buckets with colors; (18) Troubleshooting token map "etc." replaced with full list of 6 tokens; (19) court_count=1 BOM edge case "etc." replaced with full item list; (20) UDM port labels "etc." replaced with tier-specific port assignments; (21) PDU outlet labels "etc." replaced with specific outlet layout; (22) Switch port labels "etc." replaced with tier-specific assignment guide. |
| 2026-03-06 | ship-testing | final-mega-spec/testing/test-plan.md | Complete test strategy specified. Key findings: (1) Framework: Vitest + @testing-library/react + jsdom; Supabase client globally mocked via vi.mock; shared fixtures.ts with PRO_PROJECT_4_COURTS, AUTONOMOUS_PLUS_PROJECT, DEFAULT_SETTINGS, PRO/AUTONOMOUS BOM template rows; (2) 11 test files: 7 unit (bom-generation, cost-analysis, deployment-tracking, inventory-management, isp-validation, cable-estimation, financial-reporting) + 4 component (BomTable, DeploymentChecklist, IntakeForm, Dashboard); 57 total test cases; (3) BOM generation tests: correct qty for Pro 4-court (Mac Mini=1, cameras=4, displays=4), Autonomous+ with 2 doors + 4 cameras, front desk inclusion/exclusion, zero-qty row removal, PBK = Pro template, sort order preserved; (4) Cost analysis tests: Mac Mini chain (700→770→855.56), 4x cameras (1520→1672→1857.78), NULL unit_cost returns all NULLs, Pro 6-court service fee=20000, Autonomous 4-court=17500, full invoice total+tax+split (hardware 10000 + service 20000 → subtotal 30000 → tax 3075 → total 33075 → each installment 16537.50), hardware subtotal excludes NULL items, project P&L (33075 revenue − 9000 HW − 3000 labor − 800 travel − 200 other = 20075 net); (5) Deployment tracking tests: progress % (2/5=40%, 0%=0, 121/121=100%), not_started derivation, config→ready_to_ship blocked if Phase 15 incomplete, qc→completed blocked if Phase 14 incomplete, checklist instantiation = 121 items / 16 phases; (6) Inventory tests: available=on_hand−allocated, low stock flag at ≤reorder_point, allocation increases allocated not on_hand, over-allocation throws, receiving increments on_hand, shipping deducts both; (7) ISP validation: Starlink=block, 4-court=100Mbps rec, autonomous 24/7=dual ISP required, fiber=ok+preferred note; (8) Form validation: venue_name min 2, court_count≥1, door_count hidden for Pro/PBK shown for Autonomous, security_camera_count only for Autonomous+, email format, phone format; (9) Dashboard: renders cards, status pills, empty state, search filter; (10) 6 smoke tests: create project→BOM generated, auth guard redirect, checklist item toggle, deposit invoice→revenue_stage advance, inventory receiving, settings persist; (11) Coverage targets: bom-generation 100%, cost-analysis 100%, deployment-tracking 95%, inventory 95%, financial-reporting 90%, components 80%, overall 80%. |
| 2026-03-06 | ship-seed-data | final-mega-spec/data-model/seed-data.md | Complete seed data for all 9 tables (309 total rows). Key findings: (1) Section 3 (Pricing Tier Defaults) populated — full INSERT INTO settings with all 37 fields; pro_venue_fee=$5,000, pro_court_fee=$2,500, autonomous_venue_fee=$7,500, autonomous_court_fee=$2,500, autonomous+_venue_fee=$7,500, autonomous+_court_fee=$2,500, pbk fees=$0 (placeholder — must configure before first PBK project), shipping_rate=10%, target_margin=10%, sales_tax_rate=10.25%, deposit_pct=50%, labor_rate=$120/hr, lodging=$250/day, airfare=$1,800, hours_per_day=10, rent=$27,600/yr, indirect_salaries=$147,000/yr; threshold fields for switch/SSD/NVR sizing; VLAN IDs 30/31/32/33; port 4000; ddns_domain=podplaydns.com; CC terminal PIN=07139; (2) Section 4 (VLAN Reference) populated — INSERT INTO network_vlans (4 rows: default/replay/surveillance/access_control with exact subnets 192.168.30.0/24, 192.168.32.0/24, 192.168.31.0/24, 192.168.33.0/24; mDNS=true only for REPLAY; conditional flags for surveillance/access-control tiers); INSERT INTO isp_bandwidth_requirements (5 rows: 1-4 courts fiber=50Mbps cable=60Mbps dedicated=30Mbps; 5-11 courts fiber=150 dedicated=50; 12-19 courts fiber=200 dedicated=50; 20-24 courts fiber=300 dedicated=100; 25+ courts fiber=400 dedicated=150; cable_note="Highest possible upload" for rows 2-5 where no fixed cable minimum); (3) Section 5 (Troubleshooting Tips) already existed at end of file — fixed label from misnamed "Section 2" to "Section 5 (continued)"; 16 rows across phases 3/5/6/7/8/9/10/12/13, Tier1=10/Tier2=5/Tier3=1, critical=1/warning=12/info=3; (4) Section 7 (Team Contacts) added — INSERT INTO team_contacts (7 rows): Andy Korzeniacki (pm, 917-937-6896, andyk@podplay.app, no tier), Nico (hardware, Via Chad, Tier 2), Chad (operations, direct, no tier), Stan Wu (config, Tier 2), Agustin (app, no tier), CS Team (cs, group contact), Patrick (engineering, Tier 3); only Andy has confirmed phone+email; (5) Section 8 (Team OpEx) added — INSERT INTO team_opex (4 rows): Niko 50%/50% direct/indirect, Chad 0%/20%, Andy 0%/0%, Stan 50%/50%; all annual_salary=0.00 (requires XLSX to confirm); (6) Seed Data Summary table added: 309 total rows across 9 tables with migration order; previously existing sections already complete: 121 deployment_checklist_templates, 47 hardware_catalog, 104 bom_templates. |
| 2026-03-06 | ship-deployment | final-mega-spec/deployment/infrastructure.md | Complete Fly.io + Supabase Cloud deployment spec. Key findings: (1) Architecture: static Vite build served by nginx on Fly.io shared-cpu-1x 256MB machine in ewr (Newark, NJ); Supabase Cloud free tier for Postgres + Auth; no custom server; (2) Multi-stage Dockerfile: Node 22-alpine build stage (npm ci + vite build) → nginx 1.27-alpine serve stage; VITE_ env vars injected as Docker build args from Fly.io secrets; (3) nginx.conf: SPA catch-all (try_files → index.html), gzip on (6 mime types), 1-year cache for content-hashed assets, no-cache for index.html, /health endpoint returns 200 'ok', security headers (X-Frame-Options DENY, CSP with supabase.co connect-src, X-Content-Type-Options, Referrer-Policy); nginx listens on port 8080 (non-root); (4) fly.toml: app=podplay-ops-wizard, region=ewr, auto_stop=true, min_machines=0, [build.args] declares VITE_ vars (values overridden by flyctl secrets), health check GET /health every 30s; (5) Env vars: VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY only — no server secrets needed since all logic is client-side; set via flyctl secrets set, injected at build time; (6) Supabase setup: project in us-east-1, Auth config (Site URL podplay-ops-wizard.fly.dev, redirect URLs for /auth/callback, 7-day session, magic link + email/password enabled), initial user created manually via dashboard; (7) supabase/config.toml fully specified for local dev (port 54321, email confirmations disabled locally, PKCE flow); (8) src/lib/supabase.ts: createClient with flowType='pkce', storageKey='podplay-ops-auth'; (9) Auth PKCE callback flow: magic link → /auth/callback → exchangeCodeForSession → navigate to /projects; full LoginPage component specified with email/password form + magic link fallback; (10) package.json: all 20 production dependencies + 9 devDependencies with exact versions (React 19, TanStack Router 1.58, Supabase JS 2.45, shadcn/radix UI, Recharts 2.12, RHF 7.53, Zod 3.23); (11) tsconfig.app.json: strict mode, noUnusedLocals/Parameters, path alias @/* → src/*; (12) Vite config: manualChunks splits vendor-react/vendor-tanstack/vendor-supabase/vendor-ui for optimal caching; (13) GitHub Actions CI/CD: push to main → flyctl deploy --remote-only using FLY_API_TOKEN secret; (14) RLS: all 18 tables have RLS enabled with permissive authenticated-user policy (USING true) for v1 single-user; upgrade path documented for multi-tenant (org_id claim in JWT); (15) Machine sizing rationale: shared-cpu-1x + 256MB + auto-stop sufficient for internal single-user tool, cold start ~1-2s acceptable; upgrade to min_machines=1 if needed; Supabase free tier sufficient (500MB DB, well under single-user load).
|-----------|-------------|-------------|--------------|
| 2026-03-06 | design-supabase-functions | final-mega-spec/ui-spec/supabase-functions.md | Complete client-side service layer specified. Key findings: (1) 13 service files under src/services/: projects.ts, bom.ts, inventory.ts, purchase-orders.ts, deployment.ts, invoices.ts, expenses.ts, financials.ts, settings.ts, catalog.ts, installers.ts, replay-signs.ts, cc-terminals.ts; (2) src/types/index.ts defines Row/Insert/Update/Enum type aliases for all 15 DB tables plus 7 joined types (ProjectWithInstaller, ProjectBomItemWithCatalog, InventoryWithCatalog, InventoryMovementWithCatalog, PurchaseOrderWithItems); (3) projects.ts: 5 functions — getProjects (paginated, text search with .or() on customer_name/venue_name, status/tier filters, page size 25), getProject (single with installer join), createProject (blank row defaults), updateProject (with ddns_subdomain → replay_api_url side effect), advanceProjectStatus, deleteProject; (4) bom.ts: 5 functions — getProjectBom (joined catalog), generateBomFromTemplate (reads tier templates, computes qty formula, filters PingPod/front_desk/access_control/surveillance by tier+flags, upserts with onConflict), updateBomItem, deleteBomItem, addBomItem, getBomTemplates; (5) inventory.ts: 7 functions — getInventoryItems (GIN array filter for categories, client-side low_stock filter since Supabase can't compare two columns natively), getInventoryItem, updateInventoryStock, recordInventoryMovement (with sign convention per 6 movement types), allocateInventoryForProject (sequential per-item loop with availability check), shipInventoryForProject, getInventoryMovements, upsertInventoryRow; (6) purchase-orders.ts: createPurchaseOrder auto-generates PO number PO-{YYYY}-{NNN} by counting year's POs; receivePurchaseOrder updates header + all line items + inventory + movements in one call; (7) deployment.ts: initDeploymentChecklist applies 11 token replacements, filters by applicable_tiers and is_v2_only, upserts with ignoreDuplicates=true; rehydrateChecklistTokens re-applies tokens when project fields update; toggleChecklistItem sets completed_at; (8) invoices.ts: createProjectInvoices computes service_fee from tier-specific settings fields; markInvoiceSent/markInvoicePaid also update project.revenue_stage; (9) financials.ts: getProjectPnL computes all P&L fields client-side (hardware cost, labor with installer hourly_rate fallback, expenses); getFinancialsDashboard fetches all invoices+expenses for year and aggregates monthly P&L + HER + pipeline_by_stage client-side (data volume small enough); (10) replay-signs.ts: updateReplaySign auto-sets date fields on status transition; on 'installed' transition records inventory_movement for SIGNAGE-REPLAY-ALU SKU; (11) cc-terminals.ts: updateCcTerminal auto-sets order_date/delivered_date/installed_date; (12) Error convention: all functions throw on error (not { data, error } tuple); UI wraps mutations in try/catch → toast; (13) Optimistic update pattern for checklist toggling: setItems → async toggle → revert on error; (14) Complete route loader data shape defined for all 10 routes with named typed interfaces. |
| 2026-03-06 | design-settings | final-mega-spec/ui-spec/settings-view.md | Settings section fully specified across 4 sub-pages. Key findings: (1) Route structure: `/settings` redirects to `/settings/pricing`; 4 sub-routes (pricing, catalog, team, travel) each load via TanStack Router loader calling `getSettings()` or parallel queries; shared `SettingsLayout` component provides horizontal tab bar using `useMatchRoute` for active state; (2) Pricing tab: 5 collapsible sections — Service Tier Fees (8 fields: pro/autonomous/autonomous_plus/pbk venue+court fees), Cost Chain Rates (4 fields: shipping_rate 10%, target_margin 10%, sales_tax_rate 10.25%, deposit_pct 50%), Labor & Invoicing (2 fields: labor_rate_per_hour $120, hours_per_day 10), BOM Sizing Thresholds in `<details>` disclosure (5 threshold fields + 2 ISP fields), Operational Defaults (12 fields: replay version, PO prefix, CC terminal PIN 07139, Mac Mini IP 192.168.32.100, replay port 4000, DDNS domain podplaydns.com, labels/court 5, sign multiplier 2, 4 VLAN IDs); (3) Percent inputs display as "10.25" but store as 0.1025 — Controller converts value×100 on display, /100 on change; (4) PBK unconfigured warning: amber inline banner when both pbk_venue_fee=0 AND pbk_court_fee=0; does not block save; (5) Cross-field validation via Zod .refine(): switch_24 < switch_48, ssd_1tb < ssd_2tb; single Save button at bottom of form; (6) Catalog tab: `hardware_catalog` full CRUD table; client-side filtering (catalog <60 items); 10 bom_category badge color variants; SKU is immutable after creation (shown read-only with help text); Add/Edit via right-side Sheet (480px); Deactivate via AlertDialog confirmation; Reactivate immediate; inactive rows opacity-50 hidden unless "Show inactive" checked; (7) Team tab: two independent sections — OpEx Config (2 fields: rent_per_year $27,600, indirect_salaries_per_year $147,000 with note directing per-salary inputs to Monthly Close tab) + Team Contacts table (7 seeded contacts: Andy/Nico/Chad/Stan/Agustin/CS Team/Patrick); contact table shows support tier pills (Tier 1 green/Tier 2 amber/Tier 3 red) and department badges (7 values); Add/Edit via Sheet; deactivate/reactivate via kebab menu; (8) Travel tab: minimal 3-field form (lodging_per_day $250, airfare_default $1800, hours_per_day 10) with explanatory callout box showing how each value is used; (9) Service layer: settingsService.ts (getSettings + updateSettings), catalogService.ts (5 functions: get/create/update/deactivate/reactivate), teamContactsService.ts (5 functions: get/create/update/deactivate/reactivate); all settings writes use `.update().eq('id', 1)`; (10) TypeScript types fully specified: Settings interface (37 fields), HardwareCatalogItem interface, TeamContact interface with Department and support_tier types; (11) Complete file list: 5 route files + 7 component files + 3 service files + 3 type files; (12) MRP cross-reference table: all 16 key settings values mapped to source MRP sheets (COST ANALYSIS, FINANCIALS, INVOICING, Deployment Guide Appendix C). |
| 2026-03-06 | design-financials-view | final-mega-spec/ui-spec/financials-view.md | Global Financials page fully specified. Key findings: (1) Route `/financials` with search params: tab (7 values: pipeline/pl/her/receivables/per-project/reconciliation/monthly-close, default pipeline), period (7 presets: this_month/last_month/last_3/last_6/ytd/last_year/custom, default ytd), start_date/end_date (custom range), close_month (YYYY-MM for monthly close tab); (2) Route loader fetches only period-independent data (getRevenuePipeline, computeCollectedRevenue, getAgingReceivables); period-dependent data fetched via useQuery per tab to avoid re-running loader on period change; (3) PeriodSelector component: preset Select + conditional custom date inputs; resolvePeriod() helper in src/lib/periodUtils.ts converts presets to { start_date, end_date, startYear, startMonth, endYear, endMonth }; (4) Pipeline tab: 6 summary cards (pipeline value, collected, outstanding, in-progress, closed, avg contract) + 6 funnel stage cards with ChevronRight separators; stage dot colors per revenue_stage; cards dim to opacity-50 when count=0; (5) P&L tab: useQuery for computeGlobalPnL + computeMonthlyPnLTable; 8 summary cards (2 rows of 4: revenue breakdown + profit/cost); expense breakdown 4-tile row (labor/travel/shipping/hw-misc); monthly P&L table with 8 columns + totals row in TableFooter; HER column uses classifyHER() color dots; snapshot warning icon per month; (6) HER tab: useQuery for getMonthlyHERHistory + getMonthlyOpexSnapshots; period aggregate HER card (full-width, color-coded background per status); Recharts BarChart with 3 reference lines (1.0 red dashed, 1.5 yellow dashed, 2.0 green dashed), bar fill color per HER status, gray bars for months without snapshots; monthly HER table below chart; (7) Receivables tab: from route loader (no useQuery); 4 aging bucket cards with counts+totals+action guidance; table sorted days_outstanding desc; bucket badge colors (green/amber/orange/red); Action column links to /projects/$projectId/financials; (8) Per-Project tab: getAllProjectsWithPnL() useQuery (staleTime=2min); single wide table with 11 columns including gross_margin_pct (green/yellow/red thresholds at 20%/10%), client-side sortable columns; totals row; all non-cancelled projects regardless of period; (9) Reconciliation tab: Run All button calls runAllReconciliationChecks() (R1-R5 in parallel); each check card shows status icon (gray/spinner/green-check/amber-warning); expandable discrepancy tables per check; R1 has Post Adjustment action, R3 has Update BOM Cost action, R5 has Auto-fix action (syncRevenueStage); individual Run buttons per check; (10) Monthly Close tab: month navigator (prev/next with future month guard); close status banner (green=closed, amber=not closed); 5 collapsible steps: Expense Review (grouped by project, missing-receipts warning for >$100 items), Team OpEx Inputs (6 salary/allocation fields with live team_hardware_spend preview), Hardware Revenue (final invoices paid this month), Snapshot Preview (HER preview with color), Close Month button with re-close confirmation; bottom Team OpEx Config read-only panel with link to Settings; (11) New type: ProjectPnLSummary + getAllProjectsWithPnL() defined in financialReportingService.ts; new PeriodPreset type + resolvePeriod() in lib/periodUtils.ts; (12) 13 component files under src/components/financials/ + 1 lib file (periodUtils.ts); all service functions reference existing logic specs. |
| 2026-03-06 | design-inventory-view | final-mega-spec/ui-spec/inventory-view.md | Global Inventory page fully specified. Key findings: (1) Route `/inventory` with 3 search params: `category` (bom_category enum), `low_stock` (boolean toggle), `q` (text search on name/SKU); (2) Stats cards: Total Items, On-Hand Value (SUM qty_on_hand × unit_cost, items with null unit_cost excluded), Low Stock count (red border + click-to-filter when > 0); (3) Inventory table: 7 columns — Item (name+SKU+vendor+CategoryBadge), On Hand, Allocated, Available (red bold when is_low_stock), Reorder At (inline editable with pencil icon), On-Hand Value, Actions (DropdownMenu: View History, Adjust Stock, Order from Vendor); (4) CategoryBadge: 10 color-coded variants per bom_category; (5) Movement History: right-side Sheet (480px), useQuery for getInventoryMovements(hardwareCatalogId, limit=50), newest-first, 6 movement types with icons+colors (green=positive, red=negative), project link when project_id set; (6) Adjust Stock dialog: RadioGroup for movement_type (increase/decrease/return), qty input, required reason textarea; calls adjustInventory() RPC; (7) New PO dialog: vendor, dates, notes, dynamic useFieldArray line items (combobox item selector, qty, unit_cost), row totals + PO total; pre-fillable from row action; project_id=null for stock replenishment POs; (8) Reconciliation dialog: calls runReconciliation() RPC, shows discrepancy table (actual vs movement-implied qty), "Post Adjustment" action per discrepant row; (9) All mutations call router.invalidate() after success; (10) 9 component files under src/components/inventory/. |
| 2026-03-06 | design-wizard-deployment | final-mega-spec/ui-spec/wizard-deployment.md | Stage 3 Deployment Wizard fully specified. Key findings: (1) Two-column layout: 240px phase sidebar (16 phases, ○/◑/● icons, step counts, overall progress bar, deployment_status badge) + phase detail panel (right); Phase 15 Packaging displayed between Phase 11 and Phase 12 in sidebar despite numeric sort; (2) Non-linear navigation — ops can jump to any phase; only sequential enforcement is at deployment_status transition level; (3) ChecklistStep component: checkbox (optimistic toggle), step title+description, warnings array (critical=red border, warning=yellow border), notes textarea (always editable, auto-saves 300ms debounce), completed_at timestamp; (4) Token live rendering via renderStepDescription() with hasUnsetTokens flag; unset tokens show orange '(not yet set)' + TokenFieldPopover inline edit for DDNS_SUBDOMAIN/UNIFI_SITE_NAME/MAC_MINI_USERNAME/LOCATION_ID; (5) Phase-specific panels: IspSummaryPanel (Phase 0), IspConfigMethodPanel (Phase 5 — static_ip/dmz/port_forward radio with warning for port_forward+CGNAT), ReplayServiceVersionPanel (Phase 9 — V1/V2 selector, V2 disabled with tooltip), CableEstimationPanel (Phase 0 Step 3 — court×3 + door×1 + camera×1 drops formula), VlanReferencePanel (Phase 4 — VLAN 30/31/32/33 reference, tier-conditional graying); (6) KnownIssuesPanel: shown per phase from 16 troubleshooting_entries seed data; T1/T2/T3 color badges (green/yellow/red); severity left border; solution text rendered as markdown with token substitution; phase 13 has 9 entries (most); hidden when count=0; (7) 7 status advance modals: ReadyToShip (gate Phases 0–11+15 with override), MarkShipped (tracking number + carrier + ship_date), MarkInstalling (start date + installer name), StartQA (Phase 12 gate + end date), CompleteDeployment (Phases 13+14 gate + go-live date, uses complete_deployment RPC), RollbackStatus (one-step-back with reason), BulkCompletePhase; (8) Advance button always visible in page header; rollback in '...' dropdown; (9) Read-only mode when project_status='financial_close': all checkboxes disabled, notes read-only, sticky unlock banner; (10) State management: React Query with optimistic checkbox toggle (cancel→snapshot→update→rollback on error); troubleshooting_entries cached with staleTime=Infinity; (11) 16 component files in src/components/wizard/deployment/, deployment.ts + troubleshooting.ts services, tokenRendering.ts utility; (12) Tier-conditional rendering: VLAN 31 (autonomous_plus only), VLAN 33 (autonomous+), Phase 12 steps 8–10 Kisi/door/locks (autonomous+), dual ISP panel (autonomous/autonomous_plus + is_24_7), cable distance fields conditional on tier. |
| 2026-03-06 | design-wizard-procurement | final-mega-spec/ui-spec/wizard-procurement.md | Stage 2 Procurement Wizard fully specified. Key findings: (1) Route `/projects/$projectId/procurement` with parallel loader fetching project, BOM, inventory, POs, CC terminal, replay signs, settings; redirect guard sends back to intake if project_status='intake'; (2) 6 tabs (5 always-visible + CC Terminal conditional on has_front_desk): BOM Review, Inventory Check, Purchase Orders, Packing, CC Terminal, Replay Signs; tab state persisted in URL search param ?tab=; (3) BOM Review: table grouped by 10 bom_category values; inline editing for qty/unit_cost/notes; 5-column cost chain display (est_total_cost, landed_cost, customer_price all read-only GENERATED); totals row with service fee + tax + invoice total; PingPod color swap banner; Add Custom Item sheet; Remove Item with allocation guard; Regenerate BOM with existing-allocation block; (4) Inventory Check: InventoryCheckRow type with qty_needed/on_hand/allocated/available/on_order/shortfall; status badges (In Stock/On Order/Shortfall/Allocated); summary banner; "Create PO for Shortfalls" deep-link; Allocate All button triggers allocateBomItems() with per-item increment_allocation RPC; (5) Purchase Orders: PO list with expand-for-line-items; Create PO sheet with vendor autocomplete (7 common vendors), line items sub-form; PO number format PO-{YYYY}-{NNN}; status transitions Mark Ordered/Receive Items/Cancel; Receive Items modal with per-line qty_received_now inputs + tracking number; receivePOItems() updates PO item counts, calls increment_inventory RPC, writes inventory_movements record, then auto-derives PO status (received/partial/ordered); (6) Packing: checklist of all BOM items with shipped checkboxes; Mark All Shipped button; unship reversal with confirmation; shipBomItem() calls decrement_inventory_and_allocation RPC (deducts both qty_on_hand and qty_allocated if previously allocated); packing progress bar showing n_shipped/n_total; (7) CC Terminal card: single-card layout for BBPOS WisePOS E; status progression not_ordered→ordered→delivered→installed; ensureCCTerminalRecord() creates row at procurement entry (idempotent); default cost $249.00; Stripe order ID field; (8) Replay Signs card: single-card layout; qty defaults to court_count×2 with helper text; staged→shipped→delivered→installed progression; guard: outreach_date required before shipping; installed transition triggers inventory decrement movement (REPLAY-SIGN SKU, qty_delta=-qty); ensureReplaySignRecord() creates row idempotently; (9) Procurement Footer: 5 exit conditions checklist (BOM no nulls, all shipped, CC terminal ordered if applicable, replay outreach sent, no open POs); "Mark Ready for Deployment" button disabled until all pass; on advance: updates project_status to 'deployment', calls seedDeploymentChecklist() which filters templates by tier+replay_service_version+applicable_tiers, resolves 6 tokens (CUSTOMER_NAME, COURT_COUNT, DDNS_SUBDOMAIN, UNIFI_SITE_NAME, MAC_MINI_USERNAME, LOCATION_ID), inserts deployment_checklist_items; (10) 4 Supabase RPCs defined: increment_allocation, increment_inventory, decrement_inventory_and_allocation, updatePoStatus; (11) 16 component files + 6 service files enumerated with exact paths; 4 TypeScript interfaces defined in src/types/procurement.ts. |
| 2026-03-06 | design-dashboard | final-mega-spec/ui-spec/dashboard.md | Dashboard page fully specified. Key findings: (1) Route: `/projects` with TanStack Router, `validateSearch` with Zod schema for status/tier/q/page params; (2) Loader calls `getProjects(params)` which joins invoices + deployment_checklist_items for per-project computed fields: deployment_progress_pct (0–100), total_contract_value, deposit_paid, final_paid; (3) Separate `getDashboardMetrics()` computes 4 KPI values via aggregate Supabase queries: active_count, pipeline_value, overdue_deposit_count+final_count (sent 30+ days ago), completed_this_month; (4) MetricsBar: 4 cards (Active Projects/blue, Pipeline Value/green, Overdue Invoices/amber with warning styling when >0, Closed This Month/green); (5) FilterBar: debounced 300ms text search (ilike on customer_name+venue_name), Status select (7 options including 'all'), Tier select (5 options), Clear button shown only when filter active; all filter changes reset page to 1; (6) ProjectsTable: 7 columns (Venue stacked 2-line, TierBadge, Courts count, StatusCell with progress, RevenueStageCell with $, Go-Live date, Open button); entire row is clickable; (7) TierBadge: PRO=blue, AUTO=purple, A+=indigo, PBK=orange; (8) StatusCell: colored dot + label; for deployment stage shows sub-status label + mini progress bar (24px wide) + percentage; at 100% shows CheckCircle2 instead of bar; (9) RevenueStageCell: icon+label+contract value; 6 stages with distinct icons (FileText/PenLine/Send/CheckCircle2); (10) Venue cell: stacked venue_name (bold) + customer_name (if different) + city/state/country; non-US shows country code; (11) EmptyState: two variants — FolderOpen (no projects) vs SearchX (no results for filters); both show appropriate CTA button; (12) Pagination: 20 rows/page, shows X–Y of N count, page numbers with ellipsis for gaps, prev/next buttons; scrolls to top on page change; (13) DashboardSkeleton: full skeleton matching all layout sections for pending state; (14) 12 component files + service functions + format utilities fully enumerated with TypeScript interfaces. |
| 2026-03-06 | logic-power-calculations | final-mega-spec/business-logic/power-calculations.md | PoE budget, UPS runtime, rack sizing, and circuit requirements fully specified. Key findings: (1) Per-device power table: replay camera 10W (PoE af), iPad PoE adapter 25W (PoE+ at), security camera 10W, Kisi controller 8W; Apple TV NOT PoE-powered (uses court 120V outlet, not on UPS circuit); (2) Per-court PoE draw = 40W (camera 15 + iPad adapter 25); Autonomous adds 10W/sec_camera + 8W Kisi; (3) Switch PoE budget: USW-Pro-24-POE = 400W (safe at 340W/85%), USW-Pro-48-POE = 600W (safe at 510W); validated against all switch sizing configurations from bom-generation.md; (4) UPS load formula includes: switch system draw (26W/35W) + all PoE loads + Mac Mini (35W) + UDM (33W) + NVR (25W/45W if Autonomous+) + PDU overhead (3W) + UPS self-consumption (5W); TVs and Apple TVs excluded (not on UPS circuit); (5) Calibration reference: 6-court Pro = 342W → (280Wh × 0.85) / 342 × 60 = 41.7 min ≈ 43 min confirmed against known reference point; (6) Runtime table for key configs: 1-court=101min, 4-court=55min, 6-court=43min, 8-court=34min, 9-court=30min, 16-court=19min, 17-court (2 switches)=17min; (7) Yellow warning when runtime < 30 min, upgrade recommendation when < 15 min; (8) Rack U sizing formula: 2U (Mac Mini shelf) + 1U (UDM) + 1U (patch panel) + N×1U (switches) + NVR U (0/1/2) + 2U (UPS); PDU on back = 0U; recommended rack sizes: 7U, 9U, 12U with 1U slack buffer; (9) 20A dedicated circuit required; max realistic rack load ~826W, well within 1920W NEC 80% limit; circuit warning fires only above 1600W (unreachable in practice); (10) calcPowerSummary() function specified in src/services/power.ts returning PowerSummary with all 12 fields; UI surfaced as collapsible "Power & Infrastructure" panel in Stage 2 BOM review with formatted display strings and color-coded warning badges. |
| 2026-03-06 | logic-replay-service-version | final-mega-spec/business-logic/replay-service-version.md | V1 vs V2 replay service fully specified. Key findings: (1) V1 uses UDP protocol — pixelation is a known architectural issue (not a bug); V2 uses TCP — pixelation resolved; V2 estimated ~April 2026; (2) `replay_service_version` TEXT field added to projects table with CHECK IN ('v1','v2'), default 'v1'; (3) Phase 9 renders different step sets per version: V1 = 11 steps (74–84) including Jersey City VPN server + deploy.py; V2 = 5 steps (80–84) — eliminates steps 74–79 entirely, deploys from GitHub, configures via admin dashboard; (4) V1-only steps 74–79 require VPN to Jersey City deployment server, logo upload to assets folder, `./deploy.py setup <AREA_NAME>` command where AREA_NAME is URL-safe customer slug (e.g. "acepickleballclub"); (5) Version field is locked once Phase 9 step 1 is checked — changing version resets all Phase 9 checklist items with confirmation modal; (6) V2 install command stored in settings table as `replay_service_v2_install_command` (default: "Contact Patrick for V2 install command") — editable in Settings page, gates Phase 9 V2 step 1; (7) Phase 9 header shows version badge: [V1 — UDP] with orange warning banner, [V2 — TCP] with green success banner; (8) Pixelation troubleshooting tip (Tier 3, contact Patrick) shown in Phase 9 and Phase 13 only when replay_service_version='v1'; suppressed for V2 projects; (9) Port 4000 uses TCP/UDP in V1, TCP only in V2; health endpoint and instant-replay endpoint are identical for both versions; (10) Progress calculation accounts for different step counts: V1 Phase 9 = 11 steps (9.09% each), V2 Phase 9 = 5 steps (20% each); (11) All V1 and V2 step seed data fully enumerated with id, order, description, prerequisite, and token fields; (12) TypeScript service function getPhase9Steps(version) returns appropriate PHASE_9_V1_STEPS or PHASE_9_V2_STEPS constant; REPLAY_VERSION_BANNER constant provides label/protocol/message/severity per version. |
| 2026-03-06 | logic-cable-estimation | final-mega-spec/business-logic/cable-estimation.md | Cat6 cable estimation fully specified. Key findings: (1) Formula: courts×avg_dist×3 + doors×avg_dist×1 + security_cameras×avg_dist×1 = total Cat6 ft; formula verified against Hardware Installation Guide example (8 courts×200ft×3=4800 + 2 doors×300ft×1=600 + 8 cams×150ft=1200 = 6,600ft); (2) Multiplier of 3 for courts comes from 3 required Cat6 home runs per court: display + kiosk + replay camera; (3) Default distance assumptions: courts=150ft, doors=200ft, cameras=100ft — UI constants, not stored in DB; (4) Low-voltage wire for doors (Autonomous tier): 18/2 wire (maglock/strike) + 22/4 wire (push-to-exit) + 22/4 wire (door sensor) — each door×avg_distance; mag lock requires push-to-exit button, electric strike does not; (5) Max run constraint: 100m (328ft) per Cat6 home run — intermediate PoE switch required if exceeded; UI shows per-field warning if avg_distance > 328ft; (6) Cable specs: solid copper core (not CCA), UTP, UL Listed, riser or plenum jacket per local code; PoE adapters very sensitive to cable quality — certification recommended; (7) Convenience output: total_cat6_boxes = ceil(total_cat6_ft / 1000) — 1000ft is standard Cat6 box size; 15-20% overage recommended for purchase; (8) No database storage: estimate is computed client-side from project fields (num_courts, num_doors, num_security_cameras already in projects table); avg_distance inputs are ephemeral UI state; (9) UI surfaced in two places: Stage 1 intake (read-only with defaults) and Stage 3 deployment Phase 0 Step 8 (editable calculator); (10) Tier behavior: Pro/PBK = courts Cat6 only (num_doors=0, no security cameras); Autonomous/Autonomous+ = courts + doors + cameras Cat6 plus low-voltage wire; (11) calculateCableEstimate() pure function specified in src/services/cableEstimationService.ts with full TypeScript interface. |
| 2026-03-06 | logic-troubleshooting | final-mega-spec/business-logic/troubleshooting.md | 16 troubleshooting entries fully specified. Key findings: (1) All 16 entries from Appendix A of deployment guide enumerated with exact symptom/solution text, support tier (T1/T2/T3), severity (critical/warning/info), and phase associations; (2) Entries span phases 3–14 — Phases 0/1/2/15 have no entries; Phase 13 (Testing) has the most entries (9) as it is when issues first surface; (3) Data model: troubleshooting_entries table with GIN index on phases[] array for efficient per-phase queries; no per-project rows — global seed data only; (4) 5 critical entries: Mac Mini black screen (phases 8/13/14), Flic button pairing with App Lock on (phase 12), port 4000 unreachable (phases 4/5/13), .DS_Store in cache folder (phases 8/9), app shows wrong club (phases 10/11); (5) Support tier breakdown: 2× T1 (battery replacement, overheating), 13× T2 (config-specialist level), 1× T3 (pixelation = V1 UDP architectural issue, requires Patrick); (6) UI: each phase accordion shows "Known Issues (N)" section below checklist steps; collapsed by default; symptom = header, solution = expanded body; severity controls left border color (red/yellow/gray); support tier shown as colored badge (T1=green, T2=yellow, T3=red); (7) Solution text supports token rendering via same renderStepDescription() function as checklist steps; (8) Full SQL seed block provided as single INSERT with all 16 rows; TypeScript interface defined; getTroubleshootingForPhase() service function specified using .contains('phases', [phase]) Supabase query; (9) Entry 3 and Entry 11 cross-reference V1/V2 replay service distinction; V2 (TCP) fixes pixelation issue from V1 (UDP); (10) Entry 16 (iPad enrollment order) is preventive — surfaced during Phase 10 to prevent the mistake of powering iPads in wrong order. |
| 2026-03-06 | logic-sign-fulfillment | final-mega-spec/business-logic/sign-fulfillment.md | Complete replay sign fulfillment workflow specified. Key findings: (1) Qty formula: court_count × 2, stored as GENERATED ALWAYS AS on projects.replay_sign_count; snapshot copied to replay_signs.qty at row creation to allow manual override; (2) State machine: staged → shipped → delivered → installed — each transition sets a date field (shipped_date, delivered_date, installed_date); guards enforce chronological ordering; no backward transitions; (3) Outreach tracking: outreach_channel ('slack'|'email'|'other') + outreach_date required before shipping; vendor_order_id (Fast Signs order confirmation) + tracking_number (UPS/FedEx) captured along the way; (4) Inventory decrement on 'installed': inserts inventory_movements row with qty_delta = -sign.qty and movement_type='project_shipped' for REPLAY-SIGN SKU; idempotency guard checks for existing movement before inserting to prevent double-decrement; negative quantity_on_hand triggers warning but does not block; (5) Auto-row creation: ensureReplaySignRecord() called when project enters procurement stage — unlike cc_terminals (conditional on has_front_desk), replay signs are always created for every project, every tier; (6) Six service functions specified: getReplaySign, recordOutreach, recordVendorOrderId, advanceStatus (with inventory decrement), updateQty (staged-only), plus decrementReplaySignInventory (internal); (7) 8 validation rules: qty ≥ 1, outreach_date ≤ today, date chronology guards, channel required with date, no backward transitions, qty locked after staged; (8) Court count change edge case: mismatch between projects.replay_sign_count and replay_signs.qty detected and surfaced as UI warning with "Update qty" button (only while status='staged'); (9) PBK tier uses same formula and workflow — no exceptions; (10) Concrete 6-court Telepark example traced end-to-end: 12 signs ordered (qty=12), $300 total cost via BOM REPLAY-SIGN item. |
| 2026-03-06 | logic-reconciliation | final-mega-spec/business-logic/reconciliation.md | Five reconciliation checks fully specified. Key findings: (1) R1 Inventory ↔ Movement Log: qty_on_hand must equal SUM(inventory_movements.qty_delta); discrepancy query returns items where actual ≠ movement-implied; resolved via manual adjustment with reason note; (2) R2 PO Receipts ↔ Movements: every received PO line must have a matching purchase_order_received movement; PO ID embedded in movement notes as "PO {uuid} received" — notes-parsing SQL approach specified (alternative: add reference_id column to inventory_movements); (3) R3 BOM ↔ PO Costs: unit_cost on PO items must match BOM unit_cost within ±5% tolerance; discrepancies flag as variance_pct and total_cost_impact; resolution: update BOM costs if PO price was authoritative; (4) R4 Project Cost ↔ Invoice: gross_margin = invoice_total − hardware_cost − expenses; flags: no_invoices (stage but no invoices sent), loss (margin < 0%), thin_margin (margin < 15%), ok (≥15%); (5) R5 Revenue Stage ↔ Invoice Status: revenue_stage must match most-advanced invoice state; syncRevenueStage() auto-fixes by re-deriving from invoice states; (6) Monthly close workflow: run all 5 checks → resolve all discrepancies → snapshot monthly OpEx → verify HER > 1.0 → implicit close marker is monthly_opex_snapshots row existing; (7) UI: Reconciliation tab on Global Financials page; summary card with ✅/⚠️/❌ per check; each check expands to discrepancy table with action buttons that deep-link to resolution flows; (8) R1+R2 concrete failure example: PO-2026-007 received 3x UDM-Pro in purchase_order_items but receivePOItems() crashed before writing movement — R2 surfaces root cause, R1 shows symptom; both resolved by posting single adjustment movement; (9) 7 service functions in src/services/reconciliation.ts; runAllReconciliationChecks() runs all 5 in parallel via Promise.all; (10) Return types fully specified for all 5 check row types plus ReconciliationReport aggregate. |
| 2026-03-06 | logic-financial-reporting | final-mega-spec/business-logic/financial-reporting.md | Complete financial reporting workflow specified. Key findings: (1) Four distinct reports: Revenue Pipeline (funnel by revenue_stage), Global P&L (period-filtered, cash-basis), HER by month (hardware revenue / team_hardware_spend), Aging Receivables (sent invoices by days outstanding); (2) Revenue recognition is cash-basis: revenue recognized when FINAL invoice date_paid falls in period — deposit invoices excluded from recognized revenue; (3) Revenue pipeline computes total_contract_value, deposit_outstanding, final_outstanding per stage across all non-cancelled projects; pipeline summary includes total_pipeline_value, total_collected, total_outstanding, avg_contract_value; (4) Global P&L: hardware_revenue + service_revenue − COGS = gross_profit; gross_profit − total_direct_expenses = net_profit; team_hardware_spend from monthly_opex_snapshots is shown separately (not subtracted from net_profit — overhead is HER metric, not per-project P&L); (5) HER formula fully specified: hardware_revenue / team_hardware_spend; team_hardware_spend = (niko_salary × niko_direct) + (niko_salary × (1−niko_direct) × 0.50) + (chad_salary × chad_indirect) + monthly_rent + (monthly_indirect_salaries × 0.20); thresholds: <1.0=loss (red), 1.0–1.5=break_even (yellow), 1.5–2.0=healthy (green), >2.0=strong (green-dark); (6) HER stored as GENERATED ALWAYS in monthly_opex_snapshots — recomputes automatically when inputs change; period HER = aggregate (not average) of monthly HER values; (7) Monthly P&L table: one row per calendar month, all data fetched in parallel (3 queries); COGS fetched via IN clause on closed project IDs; (8) Monthly close workflow: 7 steps — navigate to month → review expenses → missing receipt check (>$100 threshold, soft block) → enter salary inputs (pre-filled from settings, per-month override allowed) → verify hardware revenue → write snapshot (upsert with onConflict='period_year,period_month') → confirmation with HER display; (9) Re-close guard: existing snapshot shows updated_at timestamp, requires distinct "Re-close Month" button; (10) Aging receivables: 4 buckets (current 0-30, past_due_30 31-60, past_due_60 61-90, past_due_90 90+); 90+ escalates to Andy (PM); cancelled projects' outstanding invoices included in aging with badge; (11) 7 period presets: this_month, last_month, last_3, last_6, ytd, last_year, custom; all return ISO date ranges; (12) 13 service functions in src/services/financialReportingService.ts; (13) Concrete March 2026 example: 2 projects closed, $40,733 hardware revenue, $37,500 service revenue, $33,500 COGS, $44,733 gross profit (57.2%), $11,650 expenses, $33,083 net profit (42.3%), team_hardware_spend $11,750, HER 3.47 (strong); (14) 6 edge cases: month with no closed projects, month with no snapshot, cancelled projects with outstanding invoices, revenue recognized vs invoiced distinction, partial month on custom period (opex snapshots not prorated), zeroed salary settings warning. |
| 2026-03-06 | logic-invoicing | final-mega-spec/business-logic/invoicing-expenses.md | Complete two-installment invoicing and expense tracking workflow specified. Key findings: (1) Invoice amount calculation chain: hardware_subtotal (sum BOM customer_price) + service_fee (tier venue fee + courts × court fee) = subtotal; subtotal × 1.1025 (10.25% tax) = total_amount; deposit = 50% of total (assumed — XLSX confirmation needed); final = remaining 50%; (2) Service fee formulas by tier: Pro = $5,000 + (courts × $2,500); Autonomous/Autonomous+ = $7,500 + (courts × $2,500); PBK = settings.pbk_venue_fee + courts × settings.pbk_court_fee (defaults $0 until configured); (3) Revenue stage state machine: proposal → signed → deposit_invoiced → deposit_paid → final_invoiced → final_paid; each transition triggered by invoice status changes; final_paid also advances project_status to 'completed'; (4) Guard rules: deposit can only be sent if revenue_stage='signed' and BOM approved; final can only be sent if go_live_date is set AND revenue_stage='deposit_paid'; (5) Invoice rows created at project creation (status='not_sent'); amounts populated when BOM is approved in Stage 2; amounts LOCKED once invoice is sent; BOM edits after sending show warning; (6) Invoice_number (from billing system e.g. QuickBooks) required when marking invoice sent; (7) Aging receivables: 4 buckets (0-30, 31-60, 61-90, 90+ days from date_sent) computed from invoices where status='sent'; 90+ days escalates to Andy (PM); (8) 12 expense categories documented with payment methods (podplay_card / ramp_reimburse); travel defaults pre-fill: airfare=$1,800, lodging=$250/night; (9) Labor expense: NOT auto-created; ops manually logs via "Log Installer Labor" action in Stage 4; computes as installer_hours × COALESCE(installer.hourly_rate, $120); (10) Project P&L computed client-side: revenue (hardware+service) − cogs − labor − other_expenses = net_profit; gross_margin and net_margin percentages; tax shown as informational only; (11) 11 validation rules: NULL BOM costs warn but don't block; PBK $0 fee warning; future dates blocked; invoice_number required on send; deposit_pct lockable per-invoice for non-50/50 splits; (12) 14 service functions specified in src/services/invoicingService.ts; (13) Edge cases: partial BOM costing (items with NULL unit_cost excluded from subtotal), PBK unconfigured fees warning, cancelled project receivables still shown in aging, backdating allowed, deposit_pct overridable per invoice before send. | 2026-03-06 | logic-deployment-tracking | final-mega-spec/business-logic/deployment-tracking.md | Complete deployment tracking workflow specified. Key findings: (1) Two-field tracking: deployment_status (7-value coarse status manually advanced by ops) + deployment_checklist_items.is_completed (121 fine-grained steps); (2) State machine: not_started → config → ready_to_ship → shipped → installing → qc → completed — each transition has guards and explicit system actions; (3) Checklist instantiated once on project entering Stage 3 (project_status='deployment') — idempotent check prevents double-creation; (4) Progress calculation: overall % = completed/total × 100; per-phase: completed_in_phase/total_in_phase; deployment_status-labeled progress strings per status (e.g. "Configuring — 43%", "Installing — 70%"); (5) Phase-to-status mapping: Phases 0–11 + 15 = config, Phase 12 = installing, Phases 13–14 = qc; Phase 15 (Packaging) completes during config and gates ready_to_ship transition; (6) config→ready_to_ship gate: Phases 0–11 + 15 all complete; shipped→installing gate: installation_start_date required; qc→completed gate: Phases 13–14 complete + go_live_date required; all gates overridable with confirmation modal; (7) Non-linear phase navigation: within-status phase order is flexible; only status-level transitions are enforced sequentially; (8) Tier step counts: Pro=116, PBK=116 (same as Pro), Autonomous=119, Autonomous+=121; (9) Token rendering is live (not stored): tokens render from project fields at read time; unset tokens show as "(not yet set)" in orange; (10) Uncheck guard: cannot uncheck items in phases already passed by deployment_status; (11) Rollback: one step at a time allowed; blocked once project_status='financial_close'; (12) 2 schema gaps identified: shipping_tracking_number TEXT (for shipped status), bom_approved BOOLEAN (gate for Stage 3 entry); (13) Bulk phase complete available for ops who completed phases without tracking; (14) 14+ warning items identified in step descriptions (Starlink, cache folder Finder warning, iPad enrollment order, Mosyle shutdown restriction, App Lock during Flic pairing, etc.); (15) Concrete 6-court Pro example: 116 items instantiated, timeline spans NJ office config (~2 weeks) + shipping + on-site installation. |
| 2026-03-06 | logic-inventory-management | final-mega-spec/business-logic/inventory-management.md | Complete inventory management workflow specified. Key findings: (1) Three key quantities: qty_on_hand (physical stock in NJ lab), qty_allocated (reserved for projects), qty_available = GENERATED ALWAYS as on_hand − allocated; (2) Six operations fully specified with TypeScript signatures: createPurchaseOrder, receivePOItems, allocateInventoryForProject, shipProjectInventory, adjustInventory, deallocateProjectInventory; (3) Three Supabase RPCs required for atomic multi-column updates: increment_inventory (upserts on_hand), allocate_inventory (increments allocated), ship_inventory (decrements both on_hand and allocated with guard against negative), deallocate_inventory (decrements allocated only on cancellation); (4) Allocation is all-or-nothing: either all BOM items have sufficient qty_available or none are allocated — shortfall list returned for ops to resolve via PO creation; (5) PO number format: PO-{YYYY}-{NNN} sequential within year; status progression: pending → ordered → partial → received → cancelled; (6) Partial receiving supported: qty_received per line item; PO auto-derives status from sum of all line receivings; (7) Low stock alert rule: qty_available ≤ reorder_threshold AND reorder_threshold > 0 — computed on every inventory query, no background job; (8) De-allocation on project cancellation: releases qty_allocated without touching qty_on_hand; movement record uses qty_delta=0 with audit note; (9) Stage 2 procurement workflow integrated as 5-step sequence: BOM Review → Create POs → Receive POs → Confirm Allocation → Pack & Ship → project.status = shipped; (10) 8 edge cases documented: drop-ship, extra stock POs, receiving over-qty guard, allocation contention between two projects, BOM qty edit after allocation, damaged on arrival, lost in transit; (11) Full reconciliation query: SUM(inventory_movements.qty_delta) per item should equal qty_on_hand — discrepancies surface in global inventory reconciliation view. |
| 2026-03-06 | logic-cost-analysis | final-mega-spec/business-logic/cost-analysis.md | Complete cost analysis workflow specified. Key findings: (1) BOM cost chain is 3-step: est_total_cost = qty × unit_cost → landed_cost = est_total_cost × 1.10 → customer_price = landed_cost / 0.90; all three stored as Postgres GENERATED ALWAYS columns recomputing automatically on edit; (2) Effective gross margin at default rates is 18.2% (not 10%) because margin applies over landed_cost which already includes 10% shipping — explained and documented; (3) Sales tax (10.25%) is applied only at invoice time, NOT in BOM cost chain; (4) Service fee formula: Pro = $5,000 + (courts × $2,500), Autonomous/Autonomous+ = $7,500 + (courts × $2,500), PBK = $0 default (requires XLSX); (5) Invoice structure: both deposit+final invoices store same hardware_subtotal+service_fee; deposit_pct controls what fraction each invoice covers (default 0.50/0.50); subtotal/tax_amount/total_amount are GENERATED ALWAYS; deposit_amount and final_amount computed at read time (not stored) to stay fresh if deposit_pct changes; (6) Revenue stage progression state machine fully specified: 6 stages from proposal → final_paid, each triggered by invoice status changes; atomic update of both invoice.status and projects.revenue_stage via updateInvoiceStatus(); (7) Labor cost: installer_hours × rate, where rate = installer.hourly_rate ?? settings.labor_rate_per_hour ($120/hr); NOT billed to customer — internal P&L only; (8) Project P&L formula: total_revenue (hardware+service) − hardware_cogs − labor_cost − expenses = net_profit; gross_margin and net_margin percentages computed; (9) HER formula: hardware_revenue / team_hardware_spend; team_hardware_spend = Niko (50% direct + 50%×50% indirect) + Chad (20%) + rent ($2,300/mo) + indirect salaries ($2,450/mo) = $11,750/mo at example salaries; stored in monthly_opex_snapshots via month-end close function; (10) Aging receivables: 4 buckets (0-30, 31-60, 61-90, 90+ days) computed from invoices where status=sent and date_paid IS NULL; (11) 9 validation rules: NULL unit_cost shows yellow flag; margin≥1.0 returns NULL customer_price; invoice amounts locked after 'sent'; BOM staleness warning if BOM changed after invoice populated; (12) Concrete 6-court Autonomous+ example: $19,770 hardware revenue + $22,500 service = $42,270 total; $4,332.68 tax; $46,602.68 total; $23,301.34 deposit; net_profit $18,195 (43.0% margin). |
| 2026-03-06 | logic-customer-onboarding | final-mega-spec/business-logic/customer-onboarding.md | Complete 6-step onboarding workflow specified. Key findings: (1) 6-step wizard: Customer/Venue Info → Service Configuration → ISP & Networking → Installation Details → Credentials & Infrastructure → Review & Submit; (2) Submission is atomic — 5 sequential DB operations: INSERT projects, generateBOM(), seedChecklist(), ensureReplaySignRecord(), ensureFrontDeskRecords() (conditional); (3) On error any of 5 operations: roll back all (delete project row), preserve form state, show error toast — no retry loop; (4) ISP validation: Starlink=BLOCKING error (CGNAT blocks port 4000 in all regions); Philippines requires static IP as BLOCKING gate; speed adequacy is WARNING only; (5) Token substitution at checklist seeding — if field not set at intake, inserts `[GET FROM AGUSTIN]` or `[SET IN STEP 5]` placeholders; tokens NOT retroactively updated when project fields change; (6) BOM regeneration on tier/court/door/camera edit: deletes is_manual=false rows, re-runs generateBOM(); is_manual=true items preserved; (7) SOW preview shows service fee + hardware revenue + tax + deposit/final split — read-only copyable block (no PDF in V1); (8) Concrete 6-court Autonomous+ example: $41,850 hardware → $51,150 hardware revenue → $73,650 contract → $81,199 with tax → $40,599 deposit; (9) State machine: intake → procurement (no guard), intake → cancelled (confirmation dialog); revenue_stage: proposal → signed (signed_date set) — later stages in Stage 4 only; (10) 11 edge cases documented: court_count=1, PBK tier, has_front_desk toggle after create, door_count=0 on autonomous, DDNS duplicate, Starlink acknowledged, location_id empty, Philippines ISP gates. |
| 2026-03-06 | model-contacts-directory | analysis/model-contacts-directory.md, final-mega-spec/data-model/schema.md | Team contacts directory fully specified. Key findings: (1) 7 team members identified from Appendix C (Key Contacts) and Appendix D (Support Tiers): Andy Korzeniacki (PM, 917-937-6896, andyk@podplay.app), Nico (Hardware/Installs Lead, contact via Chad, Tier 2), Chad (Head of Operations), Stan Wu (Config Specialist, Tier 2), Agustin (App Readiness — LOCATION_ID creation), CS Team (Booking/Credits, group), Patrick (Engineer, Tier 3 only); (2) New `team_contacts` table with fields: slug (unique text), name, role, department, phone, email, contact_via, support_tier (1/2/3 or NULL), notes, is_active; (3) 4 RLS policies (authenticated read/insert/update/delete); (4) 3 indexes: slug (lookup), department (filter), support_tier (escalation lookup, partial index WHERE NOT NULL); (5) 7 seed rows with all known data; (6) Andy Korzeniacki is the ONLY contact with known phone+email; all others have NULL contact details (operational knowledge held by Kim Lapus); (7) Nico must ALWAYS be contacted via Chad — no direct path; (8) Patrick is Tier 3 escalation only — weekly developer call for outstanding issues; (9) Agustin must be triggered at Phase 1 Step 16 — app readiness gate before hardware ships; (10) Migration order extended to 31 steps (team_contacts=30, seed=31); (11) Gaps: 5 of 7 contacts missing email/phone — requires Kim Lapus payroll/HR input. |
| 2026-03-06 | model-team-opex | analysis/model-team-opex.md | Confirmation pass — all team OpEx data already captured in schema.md during model-settings and model-financials iterations. Key findings: (1) No new SQL required — `settings` table (lines 671–693) already has `niko_annual_salary`, `niko_direct_allocation=0.50`, `chad_annual_salary`, `chad_indirect_allocation=0.20`, `annual_rent=27600.00`, `annual_indirect_salaries=147000.00`; (2) `monthly_opex_snapshots` table (lines 1724–1834) stores per-period snapshots with GENERATED ALWAYS columns for `team_hardware_spend` and `her_ratio`; (3) HER formula confirmed: hardware_revenue / [(niko_salary × 0.50) + (niko_salary × 0.50 × 0.50) + (chad_salary × 0.20) + monthly_rent + (indirect_salaries × 0.20)]; (4) Concrete example documented: Niko $8K/mo + Chad $5K/mo → team_hardware_spend = $11,750/mo; HER = 2.55 at $30K hardware revenue; (5) Niko: 50% direct (hardware/installs) + 50% indirect (50% of that to hardware pool); Chad: 20% to hardware overhead, 80% not in HER; (6) Rent $27,600/yr = $2,300/mo allocated 100% to hardware (lab is hardware ops space); (7) Indirect salaries $147K/yr = $12,250/mo with 20% → $2,450/mo to hardware; (8) 6 known gaps: Niko salary $0 (default — needs payroll), Chad salary $0, indirect pool composition unknown, rent allocation method unconfirmed, Niko indirect 0.50 multiplier unconfirmed, indirect salary 20% unconfirmed — all require XLSX FINANCIALS tab; (9) MRP field source map documented: FINANCIALS tab row-by-row mapping to all 8 schema fields. |
| 2026-03-06 | model-network-reference | analysis/model-network-reference.md, final-mega-spec/data-model/schema.md | Network reference model fully specified. Key findings: (1) Two new reference/seed tables: `network_vlans` (4 rows — Default/Management, REPLAY, SURVEILLANCE, ACCESS CONTROL) and `isp_bandwidth_requirements` (5 rows — court count ranges 1-4, 5-11, 12-19, 20-24, 25+); (2) REPLAY VLAN exact settings from Step 42: VLAN ID=32, subnet 192.168.32.0/24, gateway 192.168.32.254, DHCP 192.168.32.1–254, mDNS=true (required for Apple TV discovery), internet=true; (3) Mac Mini always at fixed IP 192.168.32.100 on REPLAY VLAN (Step 71); (4) Camera factory default IP 192.168.1.108 on initial Default subnet (transient — changes to DHCP-fixed on REPLAY VLAN after per-camera config, Step 54); (5) Default VLAN transition: initially 192.168.1.1 for camera config, changed to 192.168.30.1 after ALL cameras configured (Step 58) — wizard must warn about this ordering; (6) SURVEILLANCE (VLAN 31) and ACCESS CONTROL (VLAN 33) gateway IPs assumed .254 by REPLAY convention — not explicitly documented, needs Nico confirmation; (7) Port 4000 TCP/UDP inbound to Mac Mini is the SOLE replay service communication path — V1 uses UDP (pixelation known issue), V2 (~April 2026) uses TCP; port forward chain: static IP > DMZ > port forward; Starlink CGNAT blocks port 4000 in ALL regions; (8) ISP speed table: fiber 50→400 Mbps symmetric by court band; cable: only 1-4 courts has fixed floor (60 Mbps upload), all others "highest possible upload"; dedicated: 30→150 Mbps symmetric; (9) ISP validation TypeScript query and rules documented: Starlink is a hard BLOCKING error; cable 5+ courts is advisory only; (10) VLAN conditional tiers: Default+REPLAY=all tiers; SURVEILLANCE=autonomous_plus only; ACCESS CONTROL=autonomous+autonomous_plus; (11) Migration order extended to 29 steps (network_vlans=26+27 seed, isp_bandwidth_requirements=28+29 seed); (12) 5 gaps: SURVEILLANCE/ACCESS CONTROL gateway IPs unconfirmed; camera per-court DHCP IPs not normalized; fiber 1-4 "50-100/100" interpretation stored as 50 Mbps minimum; internal LAN 1 Gbps limit informational only. |
| 2026-03-06 | model-international-deployment | analysis/model-international-deployment.md, final-mega-spec/data-model/schema.md | International deployment model fully specified. Key findings: (1) Cosmos (Philippines) is a separate legal entity — NOT a PodPlay sub-org; needs own Mosyle instance, own ABM org, own UniFi account; (2) Three new enums: `deployment_region` (us/philippines), `video_standard` (ntsc/pal), `power_standard` (120v_60hz/220v_60hz); (3) Seven new fields added to `projects` table: deployment_region, video_standard, power_standard, isp_provider, isp_provider_backup, isp_has_static_ip, isp_has_backup_static_ip, cosmos_entity; (4) New `deployment_regions` reference table: seed data for 'us' and 'philippines' with ISP requirements, supported_isps JSONB, wizard warning messages, and open_questions JSONB; (5) Philippines ISP rules: business plan + static IP MANDATORY (residential CGNAT blocks port 4000), dual ISP required for Autonomous tier — PLDT + Converge only (not PLDT + Globe — shared backbone), supported ISPs: PLDT Beyond Fiber / Globe GFiber Biz / Converge FlexiBIZ; (6) Starlink incompatible in ALL regions (CGNAT, port 4000 blocked); (7) 12 open questions from Appendix F seeded as JSONB in Philippines row — 8 CRITICAL (PAL/NTSC, power compatibility, port 4000 fallback, deployment server access, deploy.py, admin dashboard, FreeDNS domain, Mac Mini chip), 1 HIGH, 3 MEDIUM (EmpireTech/Flic/Kisi Philippines availability); (8) 5 wizard validation rules for Philippines projects specified: static IP gate (blocking), dual ISP warning (non-blocking), power warning, video standard warning, BOM vendor flags; (9) Migration order updated: 3 new enums before projects table, deployment_regions as step 24, seed insert step 25. |
| 2026-03-06 | model-device-migration | analysis/model-device-migration.md, final-mega-spec/data-model/schema.md | ABM transfer workflow and device migration model fully specified. Key findings: (1) Two new tables: `device_migrations` (migration event metadata) and `device_migration_items` (per-device tracking); (2) Four new enums: `mdm_provider` (mosyle/jamf/other), `device_migration_status` (planning/released/enrolled/configured/completed/cancelled), `migration_device_type` (ipad/apple_tv/mac_mini), `migration_device_status` (pending/released/enrolled/configured); (3) Migration-level status is manually advanced by ops (planning→released→completed) with auto-derivation for enrolled/configured states based on all items reaching those states; (4) Critical business rule: iPad enrollment ORDER must match court-number order (Mosyle assigns devices in power-on sequence) — `enrollment_order` INTEGER field captures expected sequence; (5) Mac Mini replay service SURVIVES factory reset — only MDM profiles are removed; Samsung SSD, DDNS cron, replay service binary all persist on file system; (6) App Lock must be OFF before Flic button re-pairing post-migration; (7) VPP licenses are NOT auto-transferred to new ABM org — must coordinate with Agustin before device power-on; (8) `project_id` is nullable FK (SET NULL on project delete) — migration may precede project creation; (9) Progress calculation: weighted average of item statuses (pending=0%, released=25%, enrolled=75%, configured=100%); (10) Migration order updated to 22 steps (device_migrations=21, device_migration_items=22); (11) Source: Appendix E of deployment guide (6-step ABM transfer process) + Appendix F Q9-Q11 (Asia deployment answered questions: Cosmos PH needs own ABM + Mosyle + UniFi). |
| 2026-03-06 | model-support-tiers | analysis/model-support-tiers.md, final-mega-spec/data-model/schema.md, final-mega-spec/data-model/seed-data.md | Support tiers model fully specified. Key findings: (1) Three fixed tiers (integers 1/2/3, CHECK constraint — no separate reference table): Tier 1=on-site/remote ops, Tier 2=config specialist (Nico-level), Tier 3=engineer/developer (Patrick-level); (2) `troubleshooting_tips` table created with 5 fields: phase_number (0–15, NULL=global), issue TEXT, solution TEXT, support_tier INTEGER, severity TEXT (info/warning/critical), sort_order INTEGER; (3) 16 known issue/solution pairs from Appendix A fully seeded with INSERT statement — all 16 are phase-specific (none NULL); (4) Phase distribution: 3(×1), 5(×1), 6(×1), 7(×1), 8(×2), 9(×2), 10(×3), 12(×3), 13(×1); (5) Tier distribution: T1=10, T2=5, T3=1 — confirming guide claim that "most day-to-day issues are Tier 1"; (6) Severity distribution: critical=1 (port 4000), warning=12, info=3; (7) Single-quote escaping applied to SQL strings for Flic pairing steps containing apostrophes ('won''t pair', 'customer''s club'); (8) Escalation path state machine documented: T1 → T2 → T3 with resolution loop back to tip documentation; (9) Migration order updated to 20 steps (troubleshooting_tips = step 20, no foreign keys); (10) UI integration: tips shown contextually per phase in deployment wizard accordion, severity badge colors (red/amber/blue), tier badge (gray/yellow/red), T2+ shows escalation callout. |
| 2026-03-06 | model-replay-signs | analysis/model-replay-signs.md, final-mega-spec/data-model/schema.md | Full replay signs model documented. Key findings: (1) `replay_signs` table already existed in schema.md but was missing two indexes — added `idx_replay_signs_project` and `idx_replay_signs_status`; (2) `outreach_channel` TEXT field was unconstrained — added `CHECK (outreach_channel IN ('slack', 'email', 'other'))` to enforce valid values; (3) Qty rule: `court_count × 2`, copied from `projects.replay_sign_count` (GENERATED ALWAYS computed column) as a snapshot at row creation — allows override for edge cases (extra backup sign); (4) Full state machine: staged → shipped → delivered → installed; each transition stamps a date field; `installed` transition has side effect of creating `inventory_movements` record for REPLAY-SIGN SKU (movement_type=`project_shipped`, qty_delta=-qty); (5) Outreach tracking: outreach_channel + outreach_date capture how/when ops contacted Fast Signs vendor before order; vendor_order_id stores Fast Signs confirmation number; (6) Auto-row creation is always triggered at Stage 2 entry (no conditional — all tiers get replay signs), unlike cc_terminals which only creates if has_front_desk=true; idempotent `ensureReplaySignRecord()` function specified; (7) Cost does NOT flow through relay_signs — $25/unit cost flows via project_bom_items for REPLAY-SIGN SKU, which is in BOM; replay_signs table is fulfillment tracking only; (8) "Replay Signs Model" documentation section appended to schema.md with full state machine table, qty rule, field source map, and TypeScript auto-row creation snippet. |
| 2026-03-06 | model-cc-terminals | final-mega-spec/data-model/schema.md, analysis/model-cc-terminals.md | Full CC terminal & front desk equipment model appended to schema.md. Key findings: (1) VENDOR CORRECTION: `square_order_id` was wrong — BBPOS WisePOS E is ordered via **Stripe**, not Square; renamed to `stripe_order_id`; (2) Two parallel side-channel procurement workflows specified: `cc_terminals` (Stripe, $249, admin PIN 07139) and `replay_signs` (Fast Signs, $25/sign, 2 per court); (3) `FD-CC-TERMINAL` in BOM is for cost/invoice purposes only — physical procurement exclusively via `cc_terminals` table (NOT via standard PO); `FD-QR-SCANNER` and `FD-WEBCAM` go through standard PO; (4) Auto-row creation on Stage 2 entry: `ensureFrontDeskRecords()` for cc_terminals (when has_front_desk=true), `ensureReplaySignRecord()` for replay_signs (always); both idempotent; (5) Replay sign inventory decrement specified: movement_type=`shipped`, qty_delta=negative, triggered when sign status → `installed`; (6) Admin PIN `07139` stored in `settings.cc_terminal_pin`, displayed in Phase 15 checklist; (7) Status state machines fully documented for both `cc_terminal_status` (4 states) and `sign_status` (4 states); (8) Field source maps for both tables with MRP column references; (9) 5 known gaps: CC Form column names (XLSX blocked), qty>1 edge case, Stripe portal process, sign status vs outreach distinction, sign qty exceptions. |
| 2026-03-06 | model-checklist-templates | final-mega-spec/data-model/seed-data.md, schema.md | SQL INSERT for all 121 deployment_checklist_templates rows across 16 phases (0–15). Key findings: (1) 5 tier-specific steps (applicable_tiers not null): Phase 4 step 10 (SURVEILLANCE VLAN → autonomous_plus only), Phase 4 step 11 (ACCESS CONTROL VLAN → autonomous/autonomous_plus), Phase 12 steps 8-10 (Kisi install → autonomous/autonomous_plus); (2) All 121 current rows is_v2_only=false — Phase 9 V1 replay service steps will be supplemented with V2 rows when V2 launches ~April 2026; (3) 6 auto-fill tokens: CUSTOMER_NAME, COURT_COUNT, DDNS_SUBDOMAIN, UNIFI_SITE_NAME, MAC_MINI_USERNAME, LOCATION_ID; (4) SCHEMA GAP FOUND AND FIXED: location_id was missing from projects table — added as TEXT field (PodPlay venue ID for MDM P-List config, set by dev team Agustin); (5) Template instantiation algorithm specified: filter by applicable_tiers + is_v2_only, replace tokens from project fields, bulk insert deployment_checklist_items; (6) Fixed incorrect phase ordering in schema.md comment block (phases 6-15 were all shifted/wrong — Camera and DDNS were swapped, Mac Mini/iPad/Apple TV were wrong, etc.); (7) Flic button warning contains quoted strings requiring doubled single-quote escaping in PostgreSQL. |
| 2026-03-06 | model-settings | final-mega-spec/data-model/schema.md | Complete settings model appended to schema.md. Two tables: (1) `settings` single-row config table (id=1 enforced via CHECK constraint + single-row invariant) with 38 fields covering: all 4 tier pricing (Pro $5K/$2.5K, Autonomous $7.5K/$2.5K, Autonomous+ $7.5K/$2.5K, PBK $0 default until XLSX confirmed); cost chain rates (shipping 10%, target_margin 10%, sales_tax 10.25%, deposit_pct 50% assumed); labor ($120/hr); travel defaults (lodging $250/day, airfare $1,800, 10 hrs/day); team OpEx (rent $27.6K/yr, indirect_salaries $147K/yr); BOM sizing thresholds (switch_24/48 breakpoints, SSD 1TB/2TB/4TB breakpoints, NVR 4-bay/7-bay breakpoints); ISP validation thresholds; operational defaults (VLAN IDs 30/31/32/33, Mac Mini IP 192.168.32.100, port 4000, ddns_domain podplaydns.com, cc_terminal_pin 07139, label_sets_per_court 5, replay_sign_multiplier 2). (2) `team_opex` per-member salary allocation table with direct_pct/indirect_pct for HER calculation; seeded with Niko (50%/50%), Chad (0%/20%), Andy, Stan (salaries 0 until XLSX confirmed). Complete INSERT seed statement for settings row. RLS policies for both tables. HER formula documented. Field source map (38 rows). 6 known gaps: PBK fees, deposit %, sizing breakpoints, annual salaries, rent allocation method, ISP thresholds. | 2026-03-06 | model-installers | final-mega-spec/data-model/schema.md | Expanded thin installers table to complete spec. Key additions: (1) installer_type field on installer record itself (podplay_vetted/client_own default classification, distinct from per-project projects.installer_type); (2) hourly_rate NUMERIC(10,2) — NULL means use settings.labor_rate_per_hour ($120 default), non-NULL overrides for this installer; (3) GIN index on regions TEXT[] for `'NY' = ANY(regions)` queries; (4) index on installer_type; (5) full field source map (all fields → installer directory MRP tab); (6) labor cost formula documented: installer_hours × COALESCE(installers.hourly_rate, settings.labor_rate_per_hour); (7) installer suggestion query for matching venue_state to regions array; (8) project count query; (9) seed data structure noted (blocked on XLSX for actual names/rates); (10) inactive installer behavior clarified: is_active=false hides from dropdowns but FK SET NULL only fires on DELETE. |
| 2026-03-06 | model-deployment-checklist | final-mega-spec/data-model/seed-data.md | Created seed-data.md with complete deployment checklist template section: 16 phases (0–15) decomposed into 121 individual checkable steps. Each step specifies: title, full description, warnings array (red-box blockers), auto_fill_tokens array (6 token types: CUSTOMER_NAME/COURT_COUNT/DDNS_SUBDOMAIN/UNIFI_SITE_NAME/MAC_MINI_USERNAME/LOCATION_ID), applicable_tiers (5 steps are autonomous/autonomous_plus only), is_v2_only flag (all current steps false; Phase 9 V1 steps noted as V1-only). Key warnings documented: Starlink CGNAT incompatibility, camera one-at-a-time config, Mac Mini overheating, port 4000 criticality, .DS_Store cache folder hazard, App Lock off required for Flic pairing, iPad power-on order for Mosyle enrollment. Token substitution model: tokens resolved at checklist instantiation (Stage 3 entry). Phase 9 V1 replay service (10 steps) flagged for replacement when V2 launches ~April 2026. |
| 2026-03-06 | model-financials | final-mega-spec/data-model/schema.md | Appended full financial model spec: field source maps for invoices (INVOICING sheet) and expenses (EXPENSES sheet) tables; new monthly_opex_snapshots table for period-based HER calculation (hardware_revenue / team_hardware_spend with Niko 50/50, Chad 20%, rent $2,300/mo, indirect $12,250/mo); complete per-project P&L formula chain (hardware_revenue + service_fee - cogs - labor_cost - expenses); revenue pipeline bucketing logic (6 stages); aging receivables (0-30/31-60/61-90/90+ day buckets); 4 reconciliation checks (inventory vs POs, BOM cost vs invoice, expenses vs P&L, revenue stage consistency) with SQL reconciliation query; updated migration order to 19 steps. |
| 2026-03-06 | model-inventory | final-mega-spec/data-model/schema.md | Appended full inventory model spec: field source maps for all 4 tables (inventory, inventory_movements, purchase_orders, purchase_order_items), stock lifecycle state machine (6 movement types with qty_delta rules), allocation workflow (allocate → ship → return), PO lifecycle (pending → ordered → partial → received → cancelled), reorder thresholds by category, reconciliation SQL query, PO number convention (PO-YYYY-NNN), display grouping order. 5 known gaps identified (serial numbers, TV drop-ship workflow, trigger vs client-side updates). |
| 2026-03-06 | model-bom | final-mega-spec/data-model/schema.md | Appended BOM model: field source maps for hardware_catalog/bom_templates/project_bom_items, complete 47-item hardware catalog across 9 categories with SKUs, complete BOM template matrix for Pro/Autonomous/Autonomous+ tiers, conditional sizing rules (switch/SSD/NVR), full 8-step cost chain with concrete 6-court example, TypeScript BOM generation algorithm. |
| 2026-03-06 | model-project | final-mega-spec/data-model/schema.md | Created schema.md with all enums (8 types), all tables (projects, installers, settings, hardware_catalog, bom_templates, project_bom_items, inventory, inventory_movements, purchase_orders, purchase_order_items, deployment_checklist_templates, deployment_checklist_items, invoices, expenses, replay_signs, cc_terminals), derived calculations, migration order, field source map for projects table. |
| 2026-03-06 | source-pricing-model | analysis/source-pricing-model.md | Derived pricing: Pro $5K venue + $2.5K/court; Autonomous $7.5K venue + $2.5K/court; PBK unknown. Cost chain: qty × unit_cost → ×1.10 shipping → ÷0.90 margin. Tax 10.25% at invoice time. HER formula structure derived. Settings table defaults confirmed. |
| 2026-03-06 | source-mrp-usage-guide | analysis/source-mrp-usage-guide.md | PARTIAL — PDF not in repo. Derived from config guide, hardware BOM, training transcripts. Captured full config workflow: DDNS setup, camera config (one at a time, 192.168.1.108), VLAN setup (Replay .32, Surveillance .31, Access Control .33), ISP priority (static IP > DMZ > port forward). |
| 2026-03-06 | source-hardware-guide | analysis/source-hardware-guide.md | Extracted hardware guide: display height 8'9" AFF, kiosk height 4'8" AFF, camera baseline distances (16-20' ideal, 11' height), 5-row wall mount chart, Cat6 12' coiled at camera. Cabling not provided by PodPlay (customer supplies Cat6, speaker cable, door wire). |
| 2026-03-06 | source-deployment-guide | analysis/source-deployment-guide.md | Extracted all 15 phases + 6 appendices: exact steps, camera settings (1080p/30fps/H.264/8192Kbps), VLAN IDs, Mac Mini IP 192.168.32.100, 14 troubleshooting pairs, ISP speed table, V1 vs V2 replay service. |
