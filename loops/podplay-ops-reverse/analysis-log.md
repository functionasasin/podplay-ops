# Analysis Log — PodPlay Ops Wizard Reverse Loop

Entries in reverse chronological order (newest first).

| Timestamp | Aspect Code | Output File | Key Findings |
|-----------|-------------|-------------|--------------|
| 2026-03-06 | logic-invoicing | final-mega-spec/business-logic/invoicing-expenses.md | Complete two-installment invoicing and expense tracking workflow specified. Key findings: (1) Invoice amount calculation chain: hardware_subtotal (sum BOM customer_price) + service_fee (tier venue fee + courts × court fee) = subtotal; subtotal × 1.1025 (10.25% tax) = total_amount; deposit = 50% of total (assumed — XLSX confirmation needed); final = remaining 50%; (2) Service fee formulas by tier: Pro = $5,000 + (courts × $2,500); Autonomous/Autonomous+ = $7,500 + (courts × $2,500); PBK = settings.pbk_venue_fee + courts × settings.pbk_court_fee (defaults $0 until configured); (3) Revenue stage state machine: proposal → signed → deposit_invoiced → deposit_paid → final_invoiced → final_paid; each transition triggered by invoice status changes; final_paid also advances project_status to 'completed'; (4) Guard rules: deposit can only be sent if revenue_stage='signed' and BOM approved; final can only be sent if go_live_date is set AND revenue_stage='deposit_paid'; (5) Invoice rows created at project creation (status='not_sent'); amounts populated when BOM is approved in Stage 2; amounts LOCKED once invoice is sent; BOM edits after sending show warning; (6) Invoice_number (from billing system e.g. QuickBooks) required when marking invoice sent; (7) Aging receivables: 4 buckets (0-30, 31-60, 61-90, 90+ days from date_sent) computed from invoices where status='sent'; 90+ days escalates to Andy (PM); (8) 12 expense categories documented with payment methods (podplay_card / ramp_reimburse); travel defaults pre-fill: airfare=$1,800, lodging=$250/night; (9) Labor expense: NOT auto-created; ops manually logs via "Log Installer Labor" action in Stage 4; computes as installer_hours × COALESCE(installer.hourly_rate, $120); (10) Project P&L computed client-side: revenue (hardware+service) − cogs − labor − other_expenses = net_profit; gross_margin and net_margin percentages; tax shown as informational only; (11) 11 validation rules: NULL BOM costs warn but don't block; PBK $0 fee warning; future dates blocked; invoice_number required on send; deposit_pct lockable per-invoice for non-50/50 splits; (12) 14 service functions specified in src/services/invoicingService.ts; (13) Edge cases: partial BOM costing (items with NULL unit_cost excluded from subtotal), PBK unconfigured fees warning, cancelled project receivables still shown in aging, backdating allowed, deposit_pct overridable per invoice before send. | 2026-03-06 | logic-deployment-tracking | final-mega-spec/business-logic/deployment-tracking.md | Complete deployment tracking workflow specified. Key findings: (1) Two-field tracking: deployment_status (7-value coarse status manually advanced by ops) + deployment_checklist_items.is_completed (121 fine-grained steps); (2) State machine: not_started → config → ready_to_ship → shipped → installing → qc → completed — each transition has guards and explicit system actions; (3) Checklist instantiated once on project entering Stage 3 (project_status='deployment') — idempotent check prevents double-creation; (4) Progress calculation: overall % = completed/total × 100; per-phase: completed_in_phase/total_in_phase; deployment_status-labeled progress strings per status (e.g. "Configuring — 43%", "Installing — 70%"); (5) Phase-to-status mapping: Phases 0–11 + 15 = config, Phase 12 = installing, Phases 13–14 = qc; Phase 15 (Packaging) completes during config and gates ready_to_ship transition; (6) config→ready_to_ship gate: Phases 0–11 + 15 all complete; shipped→installing gate: installation_start_date required; qc→completed gate: Phases 13–14 complete + go_live_date required; all gates overridable with confirmation modal; (7) Non-linear phase navigation: within-status phase order is flexible; only status-level transitions are enforced sequentially; (8) Tier step counts: Pro=116, PBK=116 (same as Pro), Autonomous=119, Autonomous+=121; (9) Token rendering is live (not stored): tokens render from project fields at read time; unset tokens show as "(not yet set)" in orange; (10) Uncheck guard: cannot uncheck items in phases already passed by deployment_status; (11) Rollback: one step at a time allowed; blocked once project_status='financial_close'; (12) 2 schema gaps identified: shipping_tracking_number TEXT (for shipped status), bom_approved BOOLEAN (gate for Stage 3 entry); (13) Bulk phase complete available for ops who completed phases without tracking; (14) 14+ warning items identified in step descriptions (Starlink, cache folder Finder warning, iPad enrollment order, Mosyle shutdown restriction, App Lock during Flic pairing, etc.); (15) Concrete 6-court Pro example: 116 items instantiated, timeline spans NJ office config (~2 weeks) + shipping + on-site installation. |
| 2026-03-06 | logic-inventory-management | final-mega-spec/business-logic/inventory-management.md | Complete inventory management workflow specified. Key findings: (1) Three key quantities: qty_on_hand (physical stock in NJ lab), qty_allocated (reserved for projects), qty_available = GENERATED ALWAYS as on_hand − allocated; (2) Six operations fully specified with TypeScript signatures: createPurchaseOrder, receivePOItems, allocateInventoryForProject, shipProjectInventory, adjustInventory, deallocateProjectInventory; (3) Three Supabase RPCs required for atomic multi-column updates: increment_inventory (upserts on_hand), allocate_inventory (increments allocated), ship_inventory (decrements both on_hand and allocated with guard against negative), deallocate_inventory (decrements allocated only on cancellation); (4) Allocation is all-or-nothing: either all BOM items have sufficient qty_available or none are allocated — shortfall list returned for ops to resolve via PO creation; (5) PO number format: PO-{YYYY}-{NNN} sequential within year; status progression: pending → ordered → partial → received → cancelled; (6) Partial receiving supported: qty_received per line item; PO auto-derives status from sum of all line receivings; (7) Low stock alert rule: qty_available ≤ reorder_threshold AND reorder_threshold > 0 — computed on every inventory query, no background job; (8) De-allocation on project cancellation: releases qty_allocated without touching qty_on_hand; movement record uses qty_delta=0 with audit note; (9) Stage 2 procurement workflow integrated as 5-step sequence: BOM Review → Create POs → Receive POs → Confirm Allocation → Pack & Ship → project.status = shipped; (10) 8 edge cases documented: drop-ship, extra stock POs, receiving over-qty guard, allocation contention between two projects, BOM qty edit after allocation, damaged on arrival, lost in transit; (11) Full reconciliation query: SUM(inventory_movements.qty_delta) per item should equal qty_on_hand — discrepancies surface in global inventory reconciliation view. |
| 2026-03-06 | logic-cost-analysis | final-mega-spec/business-logic/cost-analysis.md | Complete cost analysis workflow specified. Key findings: (1) BOM cost chain is 3-step: est_total_cost = qty × unit_cost → landed_cost = est_total_cost × 1.10 → customer_price = landed_cost / 0.90; all three stored as Postgres GENERATED ALWAYS columns recomputing automatically on edit; (2) Effective gross margin at default rates is 18.2% (not 10%) because margin applies over landed_cost which already includes 10% shipping — explained and documented; (3) Sales tax (10.25%) is applied only at invoice time, NOT in BOM cost chain; (4) Service fee formula: Pro = $5,000 + (courts × $2,500), Autonomous/Autonomous+ = $7,500 + (courts × $2,500), PBK = $0 default (requires XLSX); (5) Invoice structure: both deposit+final invoices store same hardware_subtotal+service_fee; deposit_pct controls what fraction each invoice covers (default 0.50/0.50); subtotal/tax_amount/total_amount are GENERATED ALWAYS; deposit_amount and final_amount computed at read time (not stored) to stay fresh if deposit_pct changes; (6) Revenue stage progression state machine fully specified: 6 stages from proposal → final_paid, each triggered by invoice status changes; atomic update of both invoice.status and projects.revenue_stage via updateInvoiceStatus(); (7) Labor cost: installer_hours × rate, where rate = installer.hourly_rate ?? settings.labor_rate_per_hour ($120/hr); NOT billed to customer — internal P&L only; (8) Project P&L formula: total_revenue (hardware+service) − hardware_cogs − labor_cost − expenses = net_profit; gross_margin and net_margin percentages computed; (9) HER formula: hardware_revenue / team_hardware_spend; team_hardware_spend = Niko (50% direct + 50%×50% indirect) + Chad (20%) + rent ($2,300/mo) + indirect salaries ($2,450/mo) = $11,750/mo at example salaries; stored in monthly_opex_snapshots via month-end close function; (10) Aging receivables: 4 buckets (0-30, 31-60, 61-90, 90+ days) computed from invoices where status=sent and date_paid IS NULL; (11) 9 validation rules: NULL unit_cost shows yellow flag; margin≥1.0 returns NULL customer_price; invoice amounts locked after 'sent'; BOM staleness warning if BOM changed after invoice populated; (12) Concrete 6-court Autonomous+ example: $19,770 hardware revenue + $22,500 service = $42,270 total; $4,332.68 tax; $46,602.68 total; $23,301.34 deposit; net_profit $18,195 (43.0% margin). |
| 2026-03-06 | logic-customer-onboarding | final-mega-spec/business-logic/customer-onboarding.md | Complete 6-step onboarding workflow specified. Key findings: (1) 6-step wizard: Customer/Venue Info → Service Configuration → ISP & Networking → Installation Details → Credentials & Infrastructure → Review & Submit; (2) Submission is atomic — 5 sequential DB operations: INSERT projects, generateBOM(), seedChecklist(), ensureReplaySignRecord(), ensureFrontDeskRecords() (conditional); (3) On error any of 5 operations: roll back all (delete project row), preserve form state, show error toast — no retry loop; (4) ISP validation: Starlink=BLOCKING error (CGNAT blocks port 4000 in all regions); Philippines requires static IP as BLOCKING gate; speed adequacy is WARNING only; (5) Token substitution at checklist seeding — if field not set at intake, inserts `[GET FROM AGUSTIN]` or `[SET IN STEP 5]` placeholders; tokens NOT retroactively updated when project fields change; (6) BOM regeneration on tier/court/door/camera edit: deletes is_manual=false rows, re-runs generateBOM(); is_manual=true items preserved; (7) SOW preview shows service fee + hardware revenue + tax + deposit/final split — read-only copyable block (no PDF in V1); (8) Concrete 6-court Autonomous+ example: $41,850 hardware → $51,150 hardware revenue → $73,650 contract → $81,199 with tax → $40,599 deposit; (9) State machine: intake → procurement (no guard), intake → cancelled (confirmation dialog); revenue_stage: proposal → signed (signed_date set) — later stages in Stage 4 only; (10) 11 edge cases documented: court_count=1, PBK tier, has_front_desk toggle after create, door_count=0 on autonomous, DDNS duplicate, Starlink acknowledged, location_id empty, Philippines ISP gates. |
| 2026-03-06 | model-contacts-directory | analysis/model-contacts-directory.md, final-mega-spec/data-model/schema.md | Team contacts directory fully specified. Key findings: (1) 7 team members identified from Appendix C (Key Contacts) and Appendix D (Support Tiers): Andy Korzeniacki (PM, 917-937-6896, andyk@podplay.app), Nico (Hardware/Installs Lead, contact via Chad, Tier 2), Chad (Head of Operations), Stan Wu (Config Specialist, Tier 2), Agustin (App Readiness — LOCATION_ID creation), CS Team (Booking/Credits, group), Patrick (Engineer, Tier 3 only); (2) New `team_contacts` table with fields: slug (unique text), name, role, department, phone, email, contact_via, support_tier (1/2/3 or NULL), notes, is_active; (3) 4 RLS policies (authenticated read/insert/update/delete); (4) 3 indexes: slug (lookup), department (filter), support_tier (escalation lookup, partial index WHERE NOT NULL); (5) 7 seed rows with all known data; (6) Andy Korzeniacki is the ONLY contact with known phone+email; all others have NULL contact details (operational knowledge held by Kim Lapus); (7) Nico must ALWAYS be contacted via Chad — no direct path; (8) Patrick is Tier 3 escalation only — weekly developer call for outstanding issues; (9) Agustin must be triggered at Phase 1 Step 16 — app readiness gate before hardware ships; (10) Migration order extended to 31 steps (team_contacts=30, seed=31); (11) Gaps: 5 of 7 contacts missing email/phone — requires Kim Lapus payroll/HR input. |
| 2026-03-06 | model-team-opex | analysis/model-team-opex.md | Confirmation pass — all team OpEx data already captured in schema.md during model-settings and model-financials iterations. Key findings: (1) No new SQL required — `settings` table (lines 671–693) already has `niko_annual_salary`, `niko_direct_allocation=0.50`, `chad_annual_salary`, `chad_indirect_allocation=0.20`, `annual_rent=27600.00`, `annual_indirect_salaries=147000.00`; (2) `monthly_opex_snapshots` table (lines 1724–1834) stores per-period snapshots with GENERATED ALWAYS columns for `team_hardware_spend` and `her_ratio`; (3) HER formula confirmed: hardware_revenue / [(niko_salary × 0.50) + (niko_salary × 0.50 × 0.50) + (chad_salary × 0.20) + monthly_rent + (indirect_salaries × 0.20)]; (4) Concrete example documented: Niko $8K/mo + Chad $5K/mo → team_hardware_spend = $11,750/mo; HER = 2.55 at $30K hardware revenue; (5) Niko: 50% direct (hardware/installs) + 50% indirect (50% of that to hardware pool); Chad: 20% to hardware overhead, 80% not in HER; (6) Rent $27,600/yr = $2,300/mo allocated 100% to hardware (lab is hardware ops space); (7) Indirect salaries $147K/yr = $12,250/mo with 20% → $2,450/mo to hardware; (8) 6 known gaps: Niko salary $0 (default — needs payroll), Chad salary $0, indirect pool composition unknown, rent allocation method unconfirmed, Niko indirect 0.50 multiplier unconfirmed, indirect salary 20% unconfirmed — all require XLSX FINANCIALS tab; (9) MRP field source map documented: FINANCIALS tab row-by-row mapping to all 8 schema fields. |
| 2026-03-06 | model-network-reference | analysis/model-network-reference.md, final-mega-spec/data-model/schema.md | Network reference model fully specified. Key findings: (1) Two new reference/seed tables: `network_vlans` (4 rows — Default/Management, REPLAY, SURVEILLANCE, ACCESS CONTROL) and `isp_bandwidth_requirements` (5 rows — court count ranges 1-4, 5-11, 12-19, 20-24, 25+); (2) REPLAY VLAN exact settings from Step 42: VLAN ID=32, subnet 192.168.32.0/24, gateway 192.168.32.254, DHCP 192.168.32.1–254, mDNS=true (required for Apple TV discovery), internet=true; (3) Mac Mini always at fixed IP 192.168.32.100 on REPLAY VLAN (Step 71); (4) Camera factory default IP 192.168.1.108 on initial Default subnet (transient — changes to DHCP-fixed on REPLAY VLAN after per-camera config, Step 54); (5) Default VLAN transition: initially 192.168.1.1 for camera config, changed to 192.168.30.1 after ALL cameras configured (Step 58) — wizard must warn about this ordering; (6) SURVEILLANCE (VLAN 31) and ACCESS CONTROL (VLAN 33) gateway IPs assumed .254 by REPLAY convention — not explicitly documented, needs Nico confirmation; (7) Port 4000 TCP/UDP inbound to Mac Mini is the SOLE replay service communication path — V1 uses UDP (pixelation known issue), V2 (~April 2026) uses TCP; port forward chain: static IP > DMZ > port forward; Starlink CGNAT blocks port 4000 in ALL regions; (8) ISP speed table: fiber 50→400 Mbps symmetric by court band; cable: only 1-4 courts has fixed floor (60 Mbps upload), all others "highest possible upload"; dedicated: 30→150 Mbps symmetric; (9) ISP validation TypeScript query and rules documented: Starlink is a hard BLOCKING error; cable 5+ courts is advisory only; (10) VLAN conditional tiers: Default+REPLAY=all tiers; SURVEILLANCE=autonomous_plus only; ACCESS CONTROL=autonomous+autonomous_plus; (11) Migration order extended to 29 steps (network_vlans=26+27 seed, isp_bandwidth_requirements=28+29 seed); (12) 5 gaps: SURVEILLANCE/ACCESS CONTROL gateway IPs unconfirmed; camera per-court DHCP IPs not normalized; fiber 1-4 "50-100/100" interpretation stored as 50 Mbps minimum; internal LAN 1 Gbps limit informational only. |
| 2026-03-06 | model-international-deployment | analysis/model-international-deployment.md, final-mega-spec/data-model/schema.md | International deployment model fully specified. Key findings: (1) Cosmos (Philippines) is a separate legal entity — NOT a PodPlay sub-org; needs own Mosyle instance, own ABM org, own UniFi account; (2) Three new enums: `deployment_region` (us/philippines), `video_standard` (ntsc/pal), `power_standard` (120v_60hz/220v_60hz); (3) Seven new fields added to `projects` table: deployment_region, video_standard, power_standard, isp_provider, isp_provider_backup, isp_has_static_ip, isp_has_backup_static_ip, cosmos_entity; (4) New `deployment_regions` reference table: seed data for 'us' and 'philippines' with ISP requirements, supported_isps JSONB, wizard warning messages, and open_questions JSONB; (5) Philippines ISP rules: business plan + static IP MANDATORY (residential CGNAT blocks port 4000), dual ISP required for Autonomous tier — PLDT + Converge only (not PLDT + Globe — shared backbone), supported ISPs: PLDT Beyond Fiber / Globe GFiber Biz / Converge FlexiBIZ; (6) Starlink incompatible in ALL regions (CGNAT, port 4000 blocked); (7) 12 open questions from Appendix F seeded as JSONB in Philippines row — 8 CRITICAL (PAL/NTSC, power compatibility, port 4000 fallback, deployment server access, deploy.py, admin dashboard, FreeDNS domain, Mac Mini chip), 1 HIGH, 3 MEDIUM (EmpireTech/Flic/Kisi Philippines availability); (8) 5 wizard validation rules for Philippines projects specified: static IP gate (blocking), dual ISP warning (non-blocking), power warning, video standard warning, BOM vendor flags; (9) Migration order updated: 3 new enums before projects table, deployment_regions as step 24, seed insert step 25. |
| 2026-03-06 | model-device-migration | analysis/model-device-migration.md, final-mega-spec/data-model/schema.md | ABM transfer workflow and device migration model fully specified. Key findings: (1) Two new tables: `device_migrations` (migration event metadata) and `device_migration_items` (per-device tracking); (2) Four new enums: `mdm_provider` (mosyle/jamf/other), `device_migration_status` (planning/released/enrolled/configured/completed/cancelled), `migration_device_type` (ipad/apple_tv/mac_mini), `migration_device_status` (pending/released/enrolled/configured); (3) Migration-level status is manually advanced by ops (planning→released→completed) with auto-derivation for enrolled/configured states based on all items reaching those states; (4) Critical business rule: iPad enrollment ORDER must match court-number order (Mosyle assigns devices in power-on sequence) — `enrollment_order` INTEGER field captures expected sequence; (5) Mac Mini replay service SURVIVES factory reset — only MDM profiles are removed; Samsung SSD, DDNS cron, replay service binary all persist on file system; (6) App Lock must be OFF before Flic button re-pairing post-migration; (7) VPP licenses are NOT auto-transferred to new ABM org — must coordinate with Agustin before device power-on; (8) `project_id` is nullable FK (SET NULL on project delete) — migration may precede project creation; (9) Progress calculation: weighted average of item statuses (pending=0%, released=25%, enrolled=75%, configured=100%); (10) Migration order updated to 22 steps (device_migrations=21, device_migration_items=22); (11) Source: Appendix E of deployment guide (6-step ABM transfer process) + Appendix F Q9-Q11 (Asia deployment answered questions: Cosmos PH needs own ABM + Mosyle + UniFi). |
| 2026-03-06 | model-support-tiers | analysis/model-support-tiers.md, final-mega-spec/data-model/schema.md, final-mega-spec/data-model/seed-data.md | Support tiers model fully specified. Key findings: (1) Three fixed tiers (integers 1/2/3, CHECK constraint — no separate reference table): Tier 1=on-site/remote ops, Tier 2=config specialist (Nico-level), Tier 3=engineer/developer (Patrick-level); (2) `troubleshooting_tips` table created with 5 fields: phase_number (0–15, NULL=global), issue TEXT, solution TEXT, support_tier INTEGER, severity TEXT (info/warning/critical), sort_order INTEGER; (3) 16 known issue/solution pairs from Appendix A fully seeded with INSERT statement — all 16 are phase-specific (none NULL); (4) Phase distribution: 3(×1), 5(×1), 6(×1), 7(×1), 8(×2), 9(×2), 10(×3), 12(×3), 13(×1); (5) Tier distribution: T1=10, T2=5, T3=1 — confirming guide claim that "most day-to-day issues are Tier 1"; (6) Severity distribution: critical=1 (port 4000), warning=12, info=3; (7) Single-quote escaping applied to SQL strings for Flic pairing steps containing apostrophes ('won''t pair', 'customer''s club'); (8) Escalation path state machine documented: T1 → T2 → T3 with resolution loop back to tip documentation; (9) Migration order updated to 20 steps (troubleshooting_tips = step 20, no foreign keys); (10) UI integration: tips shown contextually per phase in deployment wizard accordion, severity badge colors (red/amber/blue), tier badge (gray/yellow/red), T2+ shows escalation callout. |
| 2026-03-06 | model-replay-signs | analysis/model-replay-signs.md, final-mega-spec/data-model/schema.md | Full replay signs model documented. Key findings: (1) `replay_signs` table already existed in schema.md but was missing two indexes — added `idx_replay_signs_project` and `idx_replay_signs_status`; (2) `outreach_channel` TEXT field was unconstrained — added `CHECK (outreach_channel IN ('slack', 'email', 'other'))` to enforce valid values; (3) Qty rule: `court_count × 2`, copied from `projects.replay_sign_count` (GENERATED ALWAYS computed column) as a snapshot at row creation — allows override for edge cases (extra backup sign); (4) Full state machine: staged → shipped → delivered → installed; each transition stamps a date field; `installed` transition has side effect of creating `inventory_movements` record for REPLAY-SIGN SKU (movement_type=`project_shipped`, qty_delta=-qty); (5) Outreach tracking: outreach_channel + outreach_date capture how/when ops contacted Fast Signs vendor before order; vendor_order_id stores Fast Signs confirmation number; (6) Auto-row creation is always triggered at Stage 2 entry (no conditional — all tiers get replay signs), unlike cc_terminals which only creates if has_front_desk=true; idempotent `ensureReplaySignRecord()` function specified; (7) Cost does NOT flow through relay_signs — $25/unit cost flows via project_bom_items for REPLAY-SIGN SKU, which is in BOM; replay_signs table is fulfillment tracking only; (8) "Replay Signs Model" documentation section appended to schema.md with full state machine table, qty rule, field source map, and TypeScript auto-row creation snippet. |
| 2026-03-06 | model-cc-terminals | final-mega-spec/data-model/schema.md, analysis/model-cc-terminals.md | Full CC terminal & front desk equipment model appended to schema.md. Key findings: (1) VENDOR CORRECTION: `square_order_id` was wrong — BBPOS WisePOS E is ordered via **Stripe**, not Square; renamed to `stripe_order_id`; (2) Two parallel side-channel procurement workflows specified: `cc_terminals` (Stripe, $249, admin PIN 07139) and `replay_signs` (Fast Signs, $25/sign, 2 per court); (3) `FD-CC-TERMINAL` in BOM is for cost/invoice purposes only — physical procurement exclusively via `cc_terminals` table (NOT via standard PO); `FD-QR-SCANNER` and `FD-WEBCAM` go through standard PO; (4) Auto-row creation on Stage 2 entry: `ensureFrontDeskRecords()` for cc_terminals (when has_front_desk=true), `ensureReplaySignRecord()` for replay_signs (always); both idempotent; (5) Replay sign inventory decrement specified: movement_type=`shipped`, qty_delta=negative, triggered when sign status → `installed`; (6) Admin PIN `07139` stored in `settings.cc_terminal_pin`, displayed in Phase 15 checklist; (7) Status state machines fully documented for both `cc_terminal_status` (4 states) and `sign_status` (4 states); (8) Field source maps for both tables with MRP column references; (9) 5 known gaps: CC Form column names (XLSX blocked), qty>1 edge case, Stripe portal process, sign status vs outreach distinction, sign qty exceptions. |
| 2026-03-06 | model-checklist-templates | final-mega-spec/data-model/seed-data.md, schema.md | SQL INSERT for all 121 deployment_checklist_templates rows across 16 phases (0–15). Key findings: (1) 5 tier-specific steps (applicable_tiers not null): Phase 4 step 10 (SURVEILLANCE VLAN → autonomous_plus only), Phase 4 step 11 (ACCESS CONTROL VLAN → autonomous/autonomous_plus), Phase 12 steps 8-10 (Kisi install → autonomous/autonomous_plus); (2) All 121 current rows is_v2_only=false — Phase 9 V1 replay service steps will be supplemented with V2 rows when V2 launches ~April 2026; (3) 6 auto-fill tokens: CUSTOMER_NAME, COURT_COUNT, DDNS_SUBDOMAIN, UNIFI_SITE_NAME, MAC_MINI_USERNAME, LOCATION_ID; (4) SCHEMA GAP FOUND AND FIXED: location_id was missing from projects table — added as TEXT field (PodPlay venue ID for MDM P-List config, set by dev team Agustin); (5) Template instantiation algorithm specified: filter by applicable_tiers + is_v2_only, replace tokens from project fields, bulk insert deployment_checklist_items; (6) Fixed incorrect phase ordering in schema.md comment block (phases 6-15 were all shifted/wrong — Camera and DDNS were swapped, Mac Mini/iPad/Apple TV were wrong, etc.); (7) Flic button warning contains quoted strings requiring doubled single-quote escaping in PostgreSQL. |
| 2026-03-06 | model-settings | final-mega-spec/data-model/schema.md | Complete settings model appended to schema.md. Two tables: (1) `settings` single-row config table (id=1 enforced via CHECK constraint + single-row invariant) with 38 fields covering: all 4 tier pricing (Pro $5K/$2.5K, Autonomous $7.5K/$2.5K, Autonomous+ $7.5K/$2.5K, PBK $0 default until XLSX confirmed); cost chain rates (shipping 10%, target_margin 10%, sales_tax 10.25%, deposit_pct 50% assumed); labor ($120/hr); travel defaults (lodging $250/day, airfare $1,800, 10 hrs/day); team OpEx (rent $27.6K/yr, indirect_salaries $147K/yr); BOM sizing thresholds (switch_24/48 breakpoints, SSD 1TB/2TB/4TB breakpoints, NVR 4-bay/7-bay breakpoints); ISP validation thresholds; operational defaults (VLAN IDs 30/31/32/33, Mac Mini IP 192.168.32.100, port 4000, ddns_domain podplaydns.com, cc_terminal_pin 07139, label_sets_per_court 5, replay_sign_multiplier 2). (2) `team_opex` per-member salary allocation table with direct_pct/indirect_pct for HER calculation; seeded with Niko (50%/50%), Chad (0%/20%), Andy, Stan (salaries 0 until XLSX confirmed). Complete INSERT seed statement for settings row. RLS policies for both tables. HER formula documented. Field source map (38 rows). 6 known gaps: PBK fees, deposit %, sizing breakpoints, annual salaries, rent allocation method, ISP thresholds. | 2026-03-06 | model-installers | final-mega-spec/data-model/schema.md | Expanded thin installers table to complete spec. Key additions: (1) installer_type field on installer record itself (podplay_vetted/client_own default classification, distinct from per-project projects.installer_type); (2) hourly_rate NUMERIC(10,2) — NULL means use settings.labor_rate_per_hour ($120 default), non-NULL overrides for this installer; (3) GIN index on regions TEXT[] for `'NY' = ANY(regions)` queries; (4) index on installer_type; (5) full field source map (all fields → installer directory MRP tab); (6) labor cost formula documented: installer_hours × COALESCE(installers.hourly_rate, settings.labor_rate_per_hour); (7) installer suggestion query for matching venue_state to regions array; (8) project count query; (9) seed data structure noted (blocked on XLSX for actual names/rates); (10) inactive installer behavior clarified: is_active=false hides from dropdowns but FK SET NULL only fires on DELETE. |
| 2026-03-06 | model-deployment-checklist | final-mega-spec/data-model/seed-data.md | Created seed-data.md with complete deployment checklist template section: 16 phases (0–15) decomposed into 121 individual checkable steps. Each step specifies: title, full description, warnings array (red-box blockers), auto_fill_tokens array (6 token types: CUSTOMER_NAME/COURT_COUNT/DDNS_SUBDOMAIN/UNIFI_SITE_NAME/MAC_MINI_USERNAME/LOCATION_ID), applicable_tiers (5 steps are autonomous/autonomous_plus only), is_v2_only flag (all current steps false; Phase 9 V1 steps noted as V1-only). Key warnings documented: Starlink CGNAT incompatibility, camera one-at-a-time config, Mac Mini overheating, port 4000 criticality, .DS_Store cache folder hazard, App Lock off required for Flic pairing, iPad power-on order for Mosyle enrollment. Token substitution model: tokens resolved at checklist instantiation (Stage 3 entry). Phase 9 V1 replay service (10 steps) flagged for replacement when V2 launches ~April 2026. |
| 2026-03-06 | model-financials | final-mega-spec/data-model/schema.md | Appended full financial model spec: field source maps for invoices (INVOICING sheet) and expenses (EXPENSES sheet) tables; new monthly_opex_snapshots table for period-based HER calculation (hardware_revenue / team_hardware_spend with Niko 50/50, Chad 20%, rent $2,300/mo, indirect $12,250/mo); complete per-project P&L formula chain (hardware_revenue + service_fee - cogs - labor_cost - expenses); revenue pipeline bucketing logic (6 stages); aging receivables (0-30/31-60/61-90/90+ day buckets); 4 reconciliation checks (inventory vs POs, BOM cost vs invoice, expenses vs P&L, revenue stage consistency) with SQL reconciliation query; updated migration order to 19 steps. |
| 2026-03-06 | model-inventory | final-mega-spec/data-model/schema.md | Appended full inventory model spec: field source maps for all 4 tables (inventory, inventory_movements, purchase_orders, purchase_order_items), stock lifecycle state machine (6 movement types with qty_delta rules), allocation workflow (allocate → ship → return), PO lifecycle (pending → ordered → partial → received → cancelled), reorder thresholds by category, reconciliation SQL query, PO number convention (PO-YYYY-NNN), display grouping order. 5 known gaps identified (serial numbers, TV drop-ship workflow, trigger vs client-side updates). |
| 2026-03-06 | model-bom | final-mega-spec/data-model/schema.md | Appended BOM model: field source maps for hardware_catalog/bom_templates/project_bom_items, complete 47-item hardware catalog across 9 categories with SKUs, complete BOM template matrix for Pro/Autonomous/Autonomous+ tiers, conditional sizing rules (switch/SSD/NVR), full 8-step cost chain with concrete 6-court example, TypeScript BOM generation algorithm. |
| 2026-03-06 | model-project | final-mega-spec/data-model/schema.md | Created schema.md with all enums (8 types), all tables (projects, installers, settings, hardware_catalog, bom_templates, project_bom_items, inventory, inventory_movements, purchase_orders, purchase_order_items, deployment_checklist_templates, deployment_checklist_items, invoices, expenses, replay_signs, cc_terminals), derived calculations, migration order, field source map for projects table. |
| 2026-03-06 | source-pricing-model | analysis/source-pricing-model.md | Derived pricing: Pro $5K venue + $2.5K/court; Autonomous $7.5K venue + $2.5K/court; PBK unknown. Cost chain: qty × unit_cost → ×1.10 shipping → ÷0.90 margin. Tax 10.25% at invoice time. HER formula structure derived. Settings table defaults confirmed. |
| 2026-03-06 | source-mrp-usage-guide | analysis/source-mrp-usage-guide.md | PARTIAL — PDF not in repo. Derived from config guide, hardware BOM, training transcripts. Captured full config workflow: DDNS setup, camera config (one at a time, 192.168.1.108), VLAN setup (Replay .32, Surveillance .31, Access Control .33), ISP priority (static IP > DMZ > port forward). |
| 2026-03-06 | source-hardware-guide | analysis/source-hardware-guide.md | Extracted hardware guide: display height 8'9" AFF, kiosk height 4'8" AFF, camera baseline distances (16-20' ideal, 11' height), 5-row wall mount chart, Cat6 12' coiled at camera. Cabling not provided by PodPlay (customer supplies Cat6, speaker cable, door wire). |
| 2026-03-06 | source-deployment-guide | analysis/source-deployment-guide.md | Extracted all 15 phases + 6 appendices: exact steps, camera settings (1080p/30fps/H.264/8192Kbps), VLAN IDs, Mac Mini IP 192.168.32.100, 14 troubleshooting pairs, ISP speed table, V1 vs V2 replay service. |
