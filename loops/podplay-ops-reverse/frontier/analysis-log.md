# Analysis Log — PodPlay Ops Wizard Reverse Loop

| # | Timestamp | Aspect | Duration | Key Findings |
|---|-----------|--------|----------|--------------|
| 19 | 2026-03-06 | design-wizard-intake | ~20 min | Complete Stage 1 intake wizard UI spec written to final-mega-spec/ui-spec/wizard-intake.md. 10 sections: (1) Route config — loader fetches project+installers+settings+bomItems in parallel, pendingComponent+errorComponent defined; (2) Service layer — full Project type, IntakeFormValues type, submitIntakeForm() (update row → generateBom → seedChecklist → ensureReplaySignRecord → ensureFrontDeskRecords), advanceProjectStatus(), findDuplicateProject(), checkDdnsSubdomainAvailable(); (3) Zod schemas — step1Schema through step5Schema + combined intakeFormSchema, superRefine for cross-field validation (door/camera count minimums, date ordering); (4) Component structure — IntakeWizard (mode detection), IntakeSummaryView, IntakeEditForm, 6 step components, 5 sub-components, 3 hooks; (5) IntakeEditForm — stepFieldMap per-step validation, projectToFormValues mapper, goNext with ISP cross-validation gate; (6) StepIndicator — vertical left panel, 6 steps labeled, completed/current/pending states; (7) Steps 1–6 fully specified — all fields (type, placeholder, width, required), conditional logic, auto-sync (venue_name follows customer_name), duplicate project warning (amber non-blocking), Starlink hard block (checkbox acknowledgement required), Philippines hard block (static IP required), ISP speed warning (amber non-blocking), installer combobox (filtered by venue_state), date validation, DDNS uniqueness check (debounced 500ms), UniFi auto-prefix, URL preview, Step 6 validation checklist (12 checks) with jump-to-step links; (8) CostPreview — full TypeScript computation (hardware subtotal → shipping → landed → hardware_revenue → service_fee → tax → invoice_total → deposit/final); (9) IntakeSummaryView — view mode with revenue stage badge, SOW text generator, clipboard copy, Advance to Procurement button, Cancel Project with AlertDialog; (10) Edit mode — BOM regeneration warning when bomItems.length > 0. Complete file list: 1 route file, 3 root components, 6 step files, 5 sub-components, 3 hooks, 2 service files, 1 schema file. |
| 18 | 2026-03-06 | design-routes | ~10 min | Complete route map written to final-mega-spec/ui-spec/routes.md. 17 routes total across 4 sections: (1) Auth routes — /login (email+password + magic link, redirect-after-login), /auth/callback (PKCE code exchange); (2) Project routes — /projects (dashboard), /projects/new (creates blank row + redirects to intake), /projects/$projectId (redirects to active stage), /projects/$projectId/{intake,procurement,deployment,financials}; (3) Global views — /inventory, /financials, /settings + 4 sub-tabs (pricing/catalog/team/travel); (4) __root.tsx (QueryClientProvider, Toaster), _auth.tsx (session guard, redirects to /login with ?redirect=). Layout hierarchy: AppLayout (global sidebar with 4 nav items + user sign-out), ProjectShell (breadcrumb + 4 stage tabs with disabled logic + non-linear navigation). Stage redirect logic: project.status maps to stage route. Stage tab disabled only if no data yet (no BOM rows, no checklist items, no invoices). Navigation guards are wizard-level soft gates (not route-level blockers). Search params: /projects (status/tier/q/page), /inventory (category/low_stock/q), /financials (year/month) — all Zod-validated via validateSearch. Loader pattern: Promise.all for parallel data fetching, errorComponent + pendingComponent per route. File creation list: 19 route files + src/main.tsx + AppLayout + LoginPage + AuthCallback. Package deps: @tanstack/react-router v1, @tanstack/react-query v5, @supabase/supabase-js v2, router-vite-plugin. |
| 17 | 2026-03-06 | logic-isp-validation | ~10 min | ISP validation spec written to final-mega-spec/business-logic/isp-validation.md. 11 sections: (1) Starlink hard error (CGNAT blocks port 4000, only hard block), (2) IspType enum (fiber/cable/dedicated/fiveg/starlink/other), (3) Speed requirement table by court count (5 tiers: 1-4/5-11/12-19/20-24/25+) with fiber/cable/dedicated columns — exact values from deployment guide, (4) validateIspSpeed() pure function, (5) ISP config method priority (static_ip > dmz > port_forward), (6) Philippines hard requirement (business plan + static IP; PLDT/Globe/Converge only; residential CGNAT blocks all inbound), (7) Dual ISP for autonomous 24/7 venues — different backbone providers required, UDM WAN2 failover, (8) DB fields added to projects table (isp_type, isp_config_method, isp_upload_mbps, isp_download_mbps, primary_isp_provider, backup_isp_provider, is_24_7, country), (9) Two validation execution points (Stage 1 intake Step 4, Stage 3 Phase 5), (10) 7 US ISP suggestions for autocomplete, (11) Service layer: src/services/ispValidation.ts with 5 pure functions + runAllIspValidations() returning { hasHardError, errors[], warnings[] }. |
| 16 | 2026-03-06 | logic-expense-tracking | ~12 min | Appended Sections 12–16 to invoicing-expenses.md. Global expense queries: getExpensesInRange() with date/project/category filters, cross-project rollup. Monthly totals: computeMonthlyExpenseSummaries() groups by year-month, produces by_category + by_payment_method + project_count per month. Per-project attribution: all expenses have project_id NOT NULL; travel group (8 categories), labor (professional_services), shipping (outbound_shipping), misc (misc_hardware+other). Monthly close workflow: 5-step manual process → closeMonth() upserts monthly_opex_snapshots row with hardware_revenue (final paid invoices), total_project_expenses, salary defaults from settings, GENERATED ALWAYS columns compute team_hardware_spend + her_ratio. Missing receipts warning: soft block for expenses >$100 with no receipt_url. Zod schema: expenseSchema in src/lib/schemas/expense.ts (12 categories enum, 2 payment methods, amount 0–100000, date no-future guard, url validation). Service layer: 8 new functions added to src/services/invoicingService.ts including getExpensesInRange, closeMonth, getMonthlyOpexSnapshot(s), computeMonthlyExpenseSummaries, computeProjectExpenseRollup, getMonthRange. |
| 15 | 2026-03-06 | logic-progress-calculation | ~15 min | Two-level progress system: (1) Deployment checklist progress — calculateDeploymentProgress (completed/total×100), calculatePhaseProgress (per-phase X/Y + pct), getPhaseStatus (empty/partial/complete), calculateAllPhasesSummary (16 phases summary array), getDeploymentStatusLabel (status-specific label: "Configuring — 39%", "Installing — 12%", "QA — 67%"). (2) Overall project progress — calculateOverallProjectProgress (intake=12%, procurement=37%, deployment=50+checklist×0.25, financial_close=75+revenue_stage×0.25, completed=100%). Phase gates: checkPhaseGate() queries checklist items for specified phases, returns blockers array; soft override appends note to internal_notes. Service layer: getProjectChecklistProgress (full detail for Stage 3 wizard), getProjectChecklistCounts (lightweight for single project), getBulkChecklistCounts (N+1-safe single query for dashboard list using .in()). Edge cases: no items → skeleton state + re-instantiate; rollback preserves completed steps; tier-specific counts (pro=116, autonomous=119, autonomous_plus=121); cancelled=0%. Pure functions in src/lib/progress.ts; Supabase queries in src/services/deployment.ts. |
| 14 | 2026-03-06 | logic-bom-generation | ~20 min | Complete BOM generation algorithm specified in final-mega-spec/business-logic/bom-generation.md. 5-step algorithm: (1) load template rows for tier, (2) apply qty formula (qty_per_venue + qty_per_court×courts + qty_per_door×doors + qty_per_camera×cameras), (3) apply 3 sizing substitutions (SSD: 1-4cts→1TB / 5-8cts→2TB / 9+cts→4TB; Switch: 1-8cts→24-port / 9-16cts→48-port×1 / 17+cts→48-port×2; NVR: ≤4cams→UNVR / 5+cams→UNVR-Pro), (4) add conditional items (front_desk→3 DESK-* items; pingpod_wifi→1 PP-WIFI-AP), (5) compute cost chain (shipping_rate + margin from settings). Full TypeScript service functions: generateBom(), regenerateBom(), selectSsdSku(), selectSwitchConfig(), selectSwitchConfigAutonomous() accounting for Kisi+camera port budget, selectNvrSku(), computeBomTotals(). Concrete 6-court Autonomous+ example: 32 BOM items, ~$16,175 est_cost / $17,793 landed / $19,770 customer_price. Edge cases documented: 0-qty rows excluded, unit_cost=NULL flagged in UI, BOM-already-exists guard via UNIQUE constraint, regeneration requires delete-first, color variant swap is UI hint only. File location: src/services/bom.ts, src/types/bom.ts. |
| 13 | 2026-03-06 | model-tier-variants | ~10 min | PBK tier: identical hardware to Pro (24 BOM rows), custom pricing via settings.pbk_venue_fee + pbk_court_fee (both default 0.00, unknown — requires XLSX). Fee calculation formula documented. Admin warning shown when PBK fees = 0. PBK follows Pro checklist (no Kisi, no NVR steps). Schema.md comment fixed: Kisi steps do NOT apply to PBK. PingPod-specific: confirmed UniFi U6-Plus WiFi AP (has_pingpod_wifi flag); audio/speakers UNCONFIRMED — not in hardware BOM or installation guide, frontier description was speculative; Flic Bluetooth buttons are standard across all tiers (not PingPod-specific). Black vs white hardware variant (cameras, junction boxes) is a UI hint triggered by has_pingpod_wifi, not an automated swap. Front desk: 3 items (DESK-CC-TERMINAL via Stripe, DESK-QR-SCANNER + FD-WEBCAM via standard PO). ensureFrontDeskRecords() creates BOM items + cc_terminals row on Stage 2 entry. Computer NOT supplied by PodPlay. CC terminal PIN 07139 surfaced in Phase 15 checklist. No new tables/enums/columns required. |
| 12 | 2026-03-06 | model-bom-templates | ~15 min | Complete hardware catalog (47 items, 8 categories) and BOM template seed data written to seed-data.md. Hardware catalog covers: network_rack (12 items: UDM variants, switch variants, patch cables, PDU, patch panel), infrastructure (3: UPS, rack, shelf), replay_system (11: Mac Mini, SSD×3, cameras×4, Flic, sign, hw-kit), displays (8: TV, mount, Apple TV, HDMI, ATV mount, iPad, PoE, kiosk case), access_control (2: Kisi controller + reader), surveillance (7: UNVR, UNVR-Pro, HDD, G5 cameras×2, junction boxes×2), front_desk (3: CC terminal, QR scanner, webcam), pingpod_specific (1: U6-Plus WiFi AP). BOM templates for all 4 tiers (pro=24 rows, autonomous=27, autonomous_plus=29, pbk=24). Key qty formulas: REPLAY-FLIC 2/court, NET-PATCH-1FT 3/court, INFRA-UPS 1/venue, SURV-HDD 4/venue (UNVR), AC-KISI-READER 1/door, SURV-CAMERA-WHITE 1/camera. Front desk (FD-*) and PingPod (PP-WIFI-AP) items handled programmatically via has_front_desk and has_pingpod_wifi flags. Example 4-court Pro BOM: est_total_cost=$8,604, landed=$9,464, customer_price=$10,516, invoice_total=$28,131 (with service fees + tax). Unit costs from 2026 market pricing (XLSX not available for exact values). |
| 6 | 2026-03-06 | model-bom | ~10 min | Complete BOM model appended to schema.md. 47 hardware items catalogued across 9 categories (network_rack, replay_system, displays, access_control, surveillance, front_desk, infrastructure, pingpod_specific, signage) with SKUs (NET-*, REPLAY-*, DISP-*, AC-*, SURV-*, DESK-*, INFRA-*, PING-*, SIGN-*). Full BOM template matrix: Pro (28 line items + front_desk/PingPod conditional), Autonomous (Pro + AC + surveillance cams), Autonomous+ (Autonomous + NVR + HDDs). Special sizing: switch by court count (≤8→1×SW24, ≤16→2×SW24, 17+→SW48), SSD by courts (1-4→1TB, 5-12→2TB, 13+→4TB), NVR by cameras (≤4→UNVR, >4→UNVR-Pro). Full 8-step cost chain: unit_cost→est_total→landed(×1.10)→customer_price(÷0.90)→service_fee→invoice_total(×1.1025)→deposit(50%)→P&L. BOM generation algorithm in TypeScript. Key gaps: all unit_costs unknown (XLSX required), iPad/PoE/case models TBD, sizing breakpoints estimated. |
| 5 | 2026-03-06 | model-project | ~12 min | Complete project entity schema written to final-mega-spec/data-model/schema.md. 17 tables defined: projects, installers, settings, hardware_catalog, bom_templates, project_bom_items, inventory, inventory_movements, purchase_orders, purchase_order_items, deployment_checklist_templates, deployment_checklist_items, invoices, expenses, replay_signs, cc_terminals. 11 enums defined. All RLS policies (authenticated-all for Phase 1). All indexes. Field-to-MRP-sheet source map for projects table. Computed columns (replay_sign_count, est_total_cost, landed_cost, customer_price, qty_available, invoice totals). Client-side P&L, HER, and service fee calculation pseudocode. Migration order documented. |
| 4 | 2026-03-06 | source-pricing-model | ~8 min | DERIVED (XLSX/PDF not in repo). Confirmed: Pro $5K+$2.5K/court, Autonomous $7.5K+$2.5K/court, PBK unknown. Full cost chain: unit_cost → est_total_cost → landed_cost (×1.10) → customer_price (÷0.90) → tax at invoice (10.25%). All default settings confirmed. 12 expense categories, 2 payment methods. Two-installment billing (50/50 assumed). HER = hardware revenue / team spend. Team OpEx: Niko 50/50, Chad 20% indirect, rent $27.6K/yr, indirect $147K/yr. All hardware unit costs unknown — requires XLSX. Switch/SSD/NVR sizing breakpoints unknown. PBK pricing unknown. |
| 3 | 2026-03-06 | source-mrp-usage-guide | ~10 min | PARTIAL (PDF not in repo). Derived from 6 alternative sources. Fully extracted: config workflow (networking, cameras, MDM, DDNS, Mac Mini, iPad, Apple TV, testing). Hardware catalog: 30+ items across 6 categories with vendors/models. Pricing: Pro $5K+$2.5K/court, Autonomous $7.5K+$2.5K/court. Default settings: 10.25% tax, 10% shipping, 10% margin, $120/hr labor. Team: Andy (PM), Nico (config), Chad (ops), Stan (config), Agustin (app). V1 vs V2 replay service. MDM/ABM workflow. Still missing: 24 sheet names, Apps Script, unit costs, P&L formulas, inventory formulas, PBK pricing. |
| 2 | 2026-03-06 | source-hardware-guide | ~10 min | Hardware guide fully extracted: 3 tiers (Pro/Autonomous/Autonomous+). Per-court: TV 8'9" AFF, iPad 4'8" AFF, EmpireTech IPC-T54IR-ZE camera ideal 16–20' from baseline at 11' AFF. 3x Cat6 drops per court + 1x 120V. Cat6 formula: courts×dist×3 + doors×dist + cams×dist. Rack 7–12U, top-to-bottom order documented. Front desk: BBPOS WisePOS E PIN 07139, QR scanner, webcam. Bluetooth buttons: 2/court, single/double/long press actions. ISP speed table 1–30 courts all 4 circuit types. Starlink NOT compatible. Port 4000 TCP+UDP forwarding OR static IP. |
| 1 | 2026-03-06 | source-deployment-guide | ~15 min | 25-page PDF fully extracted: 16 phases (0–15), 129 steps, 6 appendices. 14 troubleshooting pairs. Exact camera encoding values (H.264 main, H.265 sub). VLAN IDs (32/31/33) and all IP addresses. V1 vs V2 replay service distinction. App Lock window 2–3 AM. ISP speed table 1–30 courts. Philippines requirements. Cat6 formula. All key contacts. source-mrp-sheets BLOCKED (XLSX not in repo). |
