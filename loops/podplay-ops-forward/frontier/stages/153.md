# Stage 153 — Responsive Sidebar

## Spec Files
- `ui-spec/responsive.md`

## Instructions
1. Update `AppLayout` so the sidebar is hidden below the `md` breakpoint (`hidden md:block`).
2. Add a hamburger menu button visible only below `md` (`md:hidden`).
3. Wrap sidebar content in a shadcn `Sheet` component that slides in from the left on mobile.
4. Close the Sheet automatically when a nav link is clicked.
5. Add an overlay backdrop when the Sheet is open.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 153 - responsive sidebar with hamburger + Sheet drawer`
