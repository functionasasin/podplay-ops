# Stage 049 — Step 2: Venue Config Form

## Spec Files
- `ui-spec/wizard-intake.md`
- `ui-spec/validation-messages.md`

## Instructions
1. Create `src/components/wizard/intake/VenueConfigStep.tsx`.
2. Fields: `venue_address` (required text input), `court_count` (required number input, min 1), `door_count` (number input), `camera_count` (number input), `has_front_desk` (boolean toggle/checkbox), `has_pingpod_wifi` (boolean toggle/checkbox).
3. Define a Zod schema with exact validation messages from the spec for each field — especially `court_count` minimum constraint.
4. Use react-hook-form with the Zod resolver. Display inline errors.
5. On valid submission, store data in the parent wizard form state and advance to the next step.

## Verify
- `npm run build` — build passes

## Commit
`podplay(forward): stage 049 - venue config form (intake step 2)`
