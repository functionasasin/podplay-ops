# Stage 039 — Dashboard Filters + Search

## Spec Files
- `ui-spec/dashboard.md`

## Instructions
1. Add filter controls above the project list in the dashboard route:
   - Status dropdown: shadcn `Select` with all `project_status` values (plus "All" option). Labels from enum-labels.
   - Tier dropdown: shadcn `Select` with all `project_tier` values (plus "All" option). Labels from enum-labels.
   - Text search: shadcn `Input` with placeholder "Search projects...".
2. Filter the project list client-side:
   - Status filter: show only projects matching selected status (or all).
   - Tier filter: show only projects matching selected tier (or all).
   - Text search: case-insensitive match on project name and customer name.
3. Filters combine with AND logic (all active filters must match).
4. Show a count of filtered results (e.g., "Showing 3 of 12 projects").

## Verify
- `npm run build` — build passes with zero errors

## Commit
`podplay(forward): stage 039 - dashboard filters and search`
