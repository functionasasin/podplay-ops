# Stage 122 — Tests: Inventory Page

## Spec Files
- `ui-spec/inventory-view.md`

## Instructions
1. Write `src/__tests__/inventory/page.test.ts`.
2. Assert page renders inside AppLayout.
3. Assert table shows inventory rows from mock data.
4. Assert loading state displays while data is fetching.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/inventory/page.test.ts` — tests pass

## Commit
`podplay(forward): stage 122 - tests for inventory page rendering`
