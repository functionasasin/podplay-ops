# Stage 050 — Tests: Venue Config Validation

## Spec Files
- `ui-spec/validation-messages.md`

## Instructions
1. Write `src/__tests__/wizard/intake/venue-config.test.ts`.
2. Test that `court_count` with value 0 triggers a minimum-value validation error with the exact spec message.
3. Test that `court_count` with a valid value (e.g., 4) passes validation.
4. Test that `venue_address` is required — empty value shows error.
5. Test that numeric inputs (`door_count`, `camera_count`) accept valid numbers and reject non-numeric input.
6. Test that boolean toggles (`has_front_desk`, `has_pingpod_wifi`) default to false and can be toggled on.

## Verify
- `npx vitest run src/__tests__/wizard/intake/venue-config.test.ts` — all tests pass

## Commit
`podplay(forward): stage 050 - tests for venue config validation`
