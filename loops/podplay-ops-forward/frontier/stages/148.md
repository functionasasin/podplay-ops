# Stage 148 — Tests: Validation Messages

## Spec Files
- `ui-spec/validation-messages.md`

## Instructions
1. Write `src/__tests__/ui/validation-messages.test.ts`.
2. Assert all 140+ keys are defined and non-empty in the `VALIDATION` export.
3. Spot-check 20 messages against their spec values (exact string match).
4. Grep `src/` (excluding `validation-messages.ts` and test files) for common hardcoded error patterns (e.g., "is required", "must be", "invalid") and assert zero matches remain.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/ui/validation-messages.test.ts` — all tests pass

## Commit
`podplay(forward): stage 148 - tests for 140+ validation message constants`
