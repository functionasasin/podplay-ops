# Stage 006 — Test Infrastructure (Vitest)

## Spec Files
- `testing/test-plan.md`

## Instructions
1. `cd apps/podplay`
2. Install test deps: `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom`
3. Create `vitest.config.ts` with jsdom environment, globals enabled, and setup file
4. Create `src/test/setup.ts` that imports `@testing-library/jest-dom`
5. Write `src/__tests__/app.test.tsx` — a smoke test that renders the App component and asserts it mounts (e.g., `expect(document.getElementById('root') || container).toBeTruthy()`)

## Verify
- `cd apps/podplay && npx vitest run` — 1 test passes

## Commit
`podplay(forward): stage 006 - Vitest + Testing Library infrastructure`
