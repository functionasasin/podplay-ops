# Stage 053 — Step 4: ISP Info Form

## Spec Files
- `ui-spec/wizard-intake.md`
- `business-logic/isp-validation.md`
- `ui-spec/validation-messages.md`

## Instructions
1. Create `src/components/wizard/intake/IspInfoStep.tsx`.
2. Fields: `isp_provider` (required text input), `has_static_ip` (boolean toggle), `upload_speed_mbps` (number input), `download_speed_mbps` (number input).
3. Show a Starlink warning banner if `isp_provider` value contains "starlink" (case-insensitive).
4. Validate upload/download speeds against court_count thresholds from `business-logic/isp-validation.md` — read `court_count` from the wizard form state (set in step 2).
5. Define a Zod schema with exact validation messages from the spec. Use `.refine()` or `.superRefine()` for the cross-field speed validation.
6. On valid submission, store data and advance.

## Verify
- `npm run build` — build passes

## Commit
`podplay(forward): stage 053 - ISP info form (intake step 4)`
