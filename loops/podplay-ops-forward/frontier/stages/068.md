# Stage 068 — Tests: Cost Chain

## Spec Files
- `business-logic/cost-analysis.md`

## Instructions
1. Write `src/__tests__/bom/cost-chain.test.ts`.
2. Test with known values: unitCost=$100, quantity=5, taxRate=8.25% (0.0825), shippingRate=5% (0.05), marginTarget=30% (0.30).
3. Assert each step of the chain:
   - `total` = 500.00
   - `tax` = 41.25
   - `shipping` = 25.00
   - `landedCost` = 566.25
   - `margin` = 169.88 (566.25 * 0.30, rounded to 2 decimals)
   - `customerPrice` = 736.13
4. Test with edge cases: quantity=1, zero tax rate, zero shipping rate, zero margin.
5. Test that all output values are rounded to exactly 2 decimal places.

## Verify
- `npx vitest run src/__tests__/bom/cost-chain.test.ts` — all tests pass

## Commit
`podplay(forward): stage 068 - tests for cost chain`
