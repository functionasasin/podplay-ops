# Stage 157 — Desktop Verify: /auth/callback

## Spec Files
- `ui-spec/empty-states.md`

## Instructions
1. Open Playwright browser at viewport 1280x800.
2. Navigate to `http://localhost:5175/auth/callback` without a valid auth code.
3. Verify: page handles gracefully with either a redirect to /login or a clear error message. No crash, no blank screen, no unhandled exception in console.
4. If any check fails, fix the issue in the source code and re-verify.

## Verify
- Page does not crash — redirects to /login or displays error message

## Commit
`podplay(forward): stage 157 - desktop verify /auth/callback graceful handling`
