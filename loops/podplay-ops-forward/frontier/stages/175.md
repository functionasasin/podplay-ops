# Stage 175 — Mobile Verify: /projects/$id/deployment

## Spec Files
- `ui-spec/responsive.md`
- `ui-spec/wizard-deployment.md`

## Instructions
1. Open browser at viewport 375x812.
2. Navigate to `/projects/$id/deployment` for an existing project.
3. Screenshot to `screenshots/mobile-deployment.png`.
4. Verify: phase list is vertically scrollable.
5. Verify: checklist items are readable with checkboxes >= 44px touch target.
6. Verify: no horizontal overflow on the page.

## Verify
- `ls screenshots/mobile-deployment.png` — file exists
- Visual: phase list scrollable, checklist readable, checkboxes >= 44px

## Commit
`podplay(forward): stage 175 - mobile verify deployment view`
