# Stage 172 — Mobile Verify: /login + /projects

## Spec Files
- `ui-spec/responsive.md`
- `ui-spec/dashboard.md`
- `ui-spec/routes.md`

## Instructions
1. Open browser at viewport 375x812.
2. Navigate to `/login`. Screenshot to `screenshots/mobile-login.png`.
3. Verify: login form is centered, inputs are full-width, no horizontal overflow.
4. Log in, navigate to `/projects`. Screenshot to `screenshots/mobile-dashboard.png`.
5. Verify: sidebar is hidden, hamburger menu icon is visible, project table/cards render without horizontal overflow.

## Verify
- `ls screenshots/mobile-login.png screenshots/mobile-dashboard.png` — both files exist
- Visual: form centered, inputs full-width, no horizontal overflow, sidebar hidden, hamburger visible on dashboard

## Commit
`podplay(forward): stage 172 - mobile verify login and dashboard`
