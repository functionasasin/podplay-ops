# Stage 141 — Travel Settings (/settings/travel)

## Spec Files
- `ui-spec/settings-view.md`

## Instructions
1. Create `src/routes/settings/travel.tsx`.
2. Fields: mileage rate, per diem, hotel budget.
3. Load defaults from settings table in Supabase.
4. Save updates on form submit.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 141 - travel settings with rate defaults`
