# Stage 138 — Tests: Catalog Settings

## Spec Files
- `ui-spec/settings-view.md`

## Instructions
1. Write `src/__tests__/settings/catalog.test.ts`.
2. Assert add catalog item creates row with correct fields.
3. Assert edit updates item values.
4. Assert archive removes item from active list.
5. Assert category filter narrows displayed items.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/settings/catalog.test.ts` — tests pass

## Commit
`podplay(forward): stage 138 - tests for catalog settings CRUD`
