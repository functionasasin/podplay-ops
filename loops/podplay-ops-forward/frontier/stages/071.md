# Stage 71 — BOM Review Table

## Spec Files
- `ui-spec/wizard-procurement.md`

## Instructions
1. Create `src/components/wizard/procurement/BomReviewTable.tsx`.
2. Load project_bom_items from Supabase filtered by projectId.
3. Render a table with columns: item name, SKU, vendor, qty (editable input), unit cost (editable input), total (computed).
4. Implement inline editing for qty and cost override fields — changes update local state immediately.
5. Implement SKU swap via a dropdown populated from hardware_catalog table.
6. When SKU is swapped, update the item name, vendor, and unit cost to match the new catalog entry.
7. Wire the component into the BOM Review tab of the procurement route.

## Verify
- `npm run build` — build passes with no errors

## Commit
`podplay(forward): stage 71 - BOM review table with inline editing and SKU swap`
