# Stage 123 — Stock Levels Table

## Spec Files
- `ui-spec/inventory-view.md`

## Instructions
1. Create `src/components/inventory/StockLevelsTable.tsx`.
2. Columns: name, SKU, category, qty on hand, allocated, available, reorder point.
3. Group rows by category.
4. Red highlight on rows where available qty <= reorder point (low-stock).
5. Available = on_hand - allocated.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 123 - stock levels table grouped by category`
