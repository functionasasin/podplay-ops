# Stage 009 — Tests: Enum Values

## Spec Files
- `data-model/schema.md`

## Instructions
1. Write `apps/podplay/src/__tests__/enums.test.ts`
2. Import all union types from `src/lib/types.ts`
3. For each of the 28 enums, write a test that asserts the exact values match the spec:
   - Create an array of expected values per enum
   - Assert the TypeScript type accepts each value (compile-time check)
   - Optionally query `pg_enum` via Supabase to verify SQL enum values match
4. Group tests by domain: tiers, statuses, financial, deployment, etc.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/enums.test.ts` — all tests pass

## Commit
`podplay(forward): stage 009 - enum value tests for all 28 types`
