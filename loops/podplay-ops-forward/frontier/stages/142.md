# Stage 142 — Tests: Travel Settings

## Spec Files
- `ui-spec/settings-view.md`

## Instructions
1. Write `src/__tests__/settings/travel.test.ts`.
2. Assert default values load from settings.
3. Assert editing values saves correctly.
4. Assert travel values are used as defaults in expense form.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/settings/travel.test.ts` — tests pass

## Commit
`podplay(forward): stage 142 - tests for travel settings`
