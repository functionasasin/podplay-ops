# Stage 170 — Desktop Verify: Full Wizard Flow

## Spec Files
- `ui-spec/empty-states.md`
- `ui-spec/loading-states.md`
- `ui-spec/validation-messages.md`
- `ui-spec/confirmation-dialogs.md`

## Instructions
1. Open Playwright browser at viewport 1280x800.
2. Create a new project via `/projects/new` — fill in all required fields and submit.
3. Navigate to intake (`/projects/$id/intake`) — complete all 7 steps with valid data. Screenshot each step to `screenshots/desktop-wizard-step-{1..7}.png`.
4. Navigate to procurement (`/projects/$id/procurement`) — verify BOM table is populated from intake data. Screenshot to `screenshots/desktop-wizard-procurement.png`.
5. Navigate to deployment (`/projects/$id/deployment`) — verify deployment items are present. Screenshot to `screenshots/desktop-wizard-deployment.png`.
6. Navigate to financials (`/projects/$id/financials`) — create a deposit invoice. Screenshot to `screenshots/desktop-wizard-financials.png`.
7. Navigate to `/financials` — verify P&L reflects the new project data. Screenshot to `screenshots/desktop-wizard-pnl.png`.
8. Verify: end-to-end flow works, data persists across pages, project status is correct at each stage, no console errors.
9. If any check fails, fix the issue in the source code and re-verify.

## Verify
- All screenshots exist in `screenshots/` — full flow from project creation through P&L verification works end-to-end

## Commit
`podplay(forward): stage 170 - desktop verify full wizard flow end-to-end`
