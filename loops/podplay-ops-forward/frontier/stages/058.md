# Stage 058 — Tests: Financial Setup Validation

## Spec Files
- `ui-spec/validation-messages.md`

## Instructions
1. Write `src/__tests__/wizard/intake/financial-setup.test.ts`.
2. Test that a past date for `target_go_live_date` triggers the exact error message from the spec.
3. Test that today's date is also rejected (must be strictly in the future).
4. Test that a future date passes validation.
5. Test that `deposit_amount` of 0 triggers the exact error message from the spec.
6. Test that a negative `deposit_amount` triggers an error.
7. Test that a positive `deposit_amount` (e.g., 500.00) passes validation.

## Verify
- `npx vitest run src/__tests__/wizard/intake/financial-setup.test.ts` — all tests pass

## Commit
`podplay(forward): stage 058 - tests for financial setup validation`
