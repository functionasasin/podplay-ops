# Stage 084 — Tests: CC Terminal

## Spec Files
- `ui-spec/wizard-procurement.md`

## Instructions
1. Write `src/__tests__/wizard/procurement/cc-terminal.test.ts`.
2. Assert: form submits, cc_terminal row created with serial + status='ordered', status transitions work.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/wizard/procurement/cc-terminal.test.ts` — all tests pass

## Commit
`podplay(forward): stage 084 - tests for CC terminal`
