# Stage 064 — Tests: BOM Generation Per Tier

## Spec Files
- `business-logic/bom-generation.md`

## Instructions
1. Write `src/__tests__/bom/generation.test.ts`.
2. Test all 4 tiers (Pro, Autonomous, Autonomous+, PBK) with a standard config: 4 courts, 2 doors, 8 cameras, has_front_desk=true, has_pingpod_wifi=true.
3. For each tier, assert the exact item list returned matches the spec — correct item names, SKUs, and quantities.
4. Test that conditional items (front desk, wifi) are included when their flags are true and excluded when false.
5. Test that quantity scaling works: e.g., camera-related items scale with camera_count, door items with door_count.

## Verify
- `npx vitest run src/__tests__/bom/generation.test.ts` — all tests pass

## Commit
`podplay(forward): stage 064 - tests for BOM generation per tier`
