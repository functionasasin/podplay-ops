# Stage 063 — BOM Generation Function

## Spec Files
- `business-logic/bom-generation.md`

## Instructions
1. Create `src/lib/bom-generation.ts`.
2. Export function `generateBOM(tier: ServiceTier, courtCount: number, doorCount: number, cameraCount: number, hasFrontDesk: boolean, hasPingpodWifi: boolean): ProjectBomItem[]`.
3. Define the BOM templates for each tier (Pro, Autonomous, Autonomous+, PBK) as static data structures, with every item and quantity rule from the spec.
4. Apply quantity rules per item: some are fixed per tier, some scale with court_count, door_count, or camera_count, some are conditional on has_front_desk or has_pingpod_wifi.
5. Return the full list of `ProjectBomItem` objects with item name, SKU, quantity, and unit cost.
6. Implement every rule from `business-logic/bom-generation.md` line by line — do not skip or approximate any rule.

## Verify
- `npm run build` — build passes

## Commit
`podplay(forward): stage 063 - BOM generation function`
