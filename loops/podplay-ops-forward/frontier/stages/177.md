# Stage 177 — Mobile Verify: /inventory + /financials

## Spec Files
- `ui-spec/responsive.md`
- `ui-spec/inventory-view.md`
- `ui-spec/financials-view.md`

## Instructions
1. Open browser at viewport 375x812.
2. Navigate to `/inventory`. Screenshot to `screenshots/mobile-inventory.png`.
3. Verify: tables have `overflow-x: auto`, no page-level horizontal overflow, cards stack vertically.
4. Navigate to `/financials` (global). Screenshot to `screenshots/mobile-global-financials.png`.
5. Verify: tables have `overflow-x: auto`, no page-level horizontal overflow, cards stack vertically.

## Verify
- `ls screenshots/mobile-inventory.png screenshots/mobile-global-financials.png` — both files exist
- Visual: tables overflow-x-auto, no page overflow, cards stack

## Commit
`podplay(forward): stage 177 - mobile verify inventory and global financials`
