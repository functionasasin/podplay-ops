# Stage 007 — Lint + Formatting

## Spec Files
- (none — tooling convention)

## Instructions
1. `cd apps/podplay`
2. Install lint/format deps: `npm install -D eslint prettier eslint-config-prettier @typescript-eslint/eslint-plugin @typescript-eslint/parser`
3. Create `.eslintrc.json` with:
   - Parser: `@typescript-eslint/parser`
   - Plugins: `@typescript-eslint`
   - Extends: `eslint:recommended`, `plugin:@typescript-eslint/recommended`, `prettier`
   - Rules: no unused vars as warning, explicit function return types off
4. Create `.prettierrc` with: `{ "semi": true, "singleQuote": true, "trailingComma": "all", "printWidth": 100 }`
5. Fix any existing lint errors in `src/`

## Verify
- `cd apps/podplay && npx eslint src/ && npx prettier --check src/` — both pass with 0 errors

## Commit
`podplay(forward): stage 007 - ESLint + Prettier configuration`
