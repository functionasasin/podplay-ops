# Stage 134 — Tests: Settings Nav

## Spec Files
- `ui-spec/settings-view.md`

## Instructions
1. Write `src/__tests__/settings/nav.test.ts`.
2. Assert all 4 tabs render: Pricing, Catalog, Team, Travel.
3. Assert each tab links to the correct route.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/settings/nav.test.ts` — tests pass

## Commit
`podplay(forward): stage 134 - tests for settings tab navigation`
