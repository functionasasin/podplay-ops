# Stage 054 — Tests: ISP Validation

## Spec Files
- `business-logic/isp-validation.md`
- `ui-spec/validation-messages.md`

## Instructions
1. Write `src/__tests__/wizard/intake/isp-info.test.ts`.
2. Test that entering "Starlink" or "starlink" as ISP provider displays the Starlink warning banner.
3. Test that entering a non-Starlink provider does not show the warning.
4. Test upload speed thresholds per court count from `business-logic/isp-validation.md` — e.g., 4 courts need X Mbps upload; providing less triggers the exact validation message from the spec.
5. Test download speed thresholds similarly.
6. Test that `isp_provider` is required — empty value shows error.
7. Assert all validation messages match the spec exactly.

## Verify
- `npx vitest run src/__tests__/wizard/intake/isp-info.test.ts` — all tests pass

## Commit
`podplay(forward): stage 054 - tests for ISP validation`
