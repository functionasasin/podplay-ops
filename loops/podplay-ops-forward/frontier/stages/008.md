# Stage 008 — Enums Migration

## Spec Files
- `data-model/schema.md` (enum definitions section)

## Instructions
1. Create `apps/podplay/supabase/migrations/00001_enums.sql`
2. Define all 28 enums from the spec as PostgreSQL enum types:
   - `service_tier`, `project_status`, `deployment_phase`, `deployment_status`
   - `revenue_stage`, `expense_category`, `invoice_status`, `payment_method`
   - And the remaining 20 enums listed in the spec
3. Create `apps/podplay/src/lib/types.ts` with matching TypeScript union types for each enum
4. Ensure each TS union type has the exact same values as the SQL enum

## Verify
- `cd apps/podplay && npx supabase db reset` — exits 0, all enums created

## Commit
`podplay(forward): stage 008 - all 28 enum types (SQL + TypeScript)`
