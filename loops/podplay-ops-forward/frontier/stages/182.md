# Stage 182 — Placeholder/Stub Hunt

## Spec Files
- `ui-spec/empty-states.md`
- `ui-spec/routes.md`

## Instructions
1. Grep `src/` for: `TODO`, `FIXME`, `placeholder`, `coming soon`, `lorem`, `stub` (case-insensitive).
2. Grep for components returning bare `<div>` or `<p>` with no meaningful children (stub components).
3. For each match: replace with real content derived from the spec. Use the appropriate spec file for the route or feature the stub belongs to.
4. Run the build to confirm no regressions.

## Verify
- `cd apps/podplay && grep -ri 'TODO\|FIXME\|placeholder\|coming soon\|lorem\|stub' src/ | wc -l` — outputs 0
- `cd apps/podplay && npm run build` — exits 0

## Commit
`podplay(forward): stage 182 - replace all placeholders and stubs with real content`
