# Stage 005 — React Hook Form + Zod Setup

## Spec Files
- `ui-spec/validation-messages.md`

## Instructions
1. `cd apps/podplay`
2. Install form libraries: `npm install react-hook-form @hookform/resolvers zod`
3. Create `src/lib/form-helpers.ts` with:
   - A typed `useZodForm<T>` wrapper that combines `useForm` with `zodResolver`
   - Helper to extract field error messages
4. Create an example Zod schema in `src/lib/schemas/example.ts` to verify the integration compiles

## Verify
- `cd apps/podplay && npm run build` — exits 0, no type errors

## Commit
`podplay(forward): stage 005 - React Hook Form + Zod integration`
