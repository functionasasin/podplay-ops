# Stage 72 — Tests: BOM Edit

## Spec Files
- `ui-spec/wizard-procurement.md`

## Instructions
1. Create `src/__tests__/wizard/procurement/bom-review.test.ts`.
2. Mock BOM data with at least 3 items including item name, SKU, vendor, qty, and unit cost.
3. Assert the table renders all mocked items.
4. Assert changing qty updates the total for that row (total = qty x unit cost).
5. Assert cost override recalculates the row total.
6. Assert SKU swap via dropdown changes the item name, vendor, and unit cost to match the new catalog entry.

## Verify
- `npx vitest run src/__tests__/wizard/procurement/bom-review.test.ts` — all tests pass

## Commit
`podplay(forward): stage 72 - tests for BOM review inline editing`
