# Stage 048 — Tests: Customer Info Validation

## Spec Files
- `ui-spec/validation-messages.md`

## Instructions
1. Write `src/__tests__/wizard/intake/customer-info.test.ts`.
2. Test that submitting with empty `customer_name` shows the exact required-field error message from the spec.
3. Test that submitting with empty `contact_email` shows the exact required-field error message from the spec.
4. Test that submitting with an invalid email format (e.g., "notanemail") shows the exact email-format error message from the spec.
5. Test that `contact_phone` is optional — submitting without it does not produce an error.
6. Test that a valid submission (all required fields filled, valid email) stores data in form state and advances.

## Verify
- `npx vitest run src/__tests__/wizard/intake/customer-info.test.ts` — all tests pass

## Commit
`podplay(forward): stage 048 - tests for customer info validation`
