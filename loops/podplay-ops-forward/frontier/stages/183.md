# Stage 183 — Build + Full Test Suite

## Spec Files
- `testing/test-plan.md`

## Instructions
1. Run `cd apps/podplay && npm run build`. Fix any errors until it exits 0.
2. Run `cd apps/podplay && npx vitest run`. Fix any test failures until it exits 0.
3. If fixes are needed, ensure they do not introduce regressions — re-run both commands after each fix.

## Verify
- `cd apps/podplay && npm run build` — exits 0
- `cd apps/podplay && npx vitest run` — exits 0

## Commit
`podplay(forward): stage 183 - build and full test suite green`
