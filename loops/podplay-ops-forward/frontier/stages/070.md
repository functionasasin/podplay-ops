# Stage 70 — Tests: Procurement Tabs Render

## Spec Files
- `ui-spec/wizard-procurement.md`

## Instructions
1. Create `src/__tests__/wizard/procurement/tabs.test.ts`.
2. Render the procurement route with mock project data.
3. Assert all 6 tab labels render: BOM Review, Inventory Check, Purchase Orders, Packing List, CC Terminals, Replay Signs.
4. Assert clicking each tab shows the correct content area.
5. Assert the first tab is selected by default.

## Verify
- `npx vitest run src/__tests__/wizard/procurement/tabs.test.ts` — all tests pass

## Commit
`podplay(forward): stage 70 - tests for procurement tab rendering`
