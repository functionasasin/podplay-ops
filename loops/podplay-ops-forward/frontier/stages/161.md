# Stage 161 — Desktop Verify: /projects/$id/procurement

## Spec Files
- `ui-spec/empty-states.md`
- `ui-spec/loading-states.md`

## Instructions
1. Open Playwright browser at viewport 1280x800.
2. Navigate to a project's procurement page (`/projects/$id/procurement`).
3. Take a screenshot and save to `screenshots/desktop-procurement.png`.
4. Verify: 6 tabs are visible, BOM table shows items (or empty state if no items yet), no console errors.
5. If any check fails, fix the issue in the source code and re-verify.

## Verify
- `screenshots/desktop-procurement.png` exists — shows 6 procurement tabs with BOM table

## Commit
`podplay(forward): stage 161 - desktop verify /projects/$id/procurement`
