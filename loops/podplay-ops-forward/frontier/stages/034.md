# Stage 034 — Tests: Toast Messages

## Spec Files
- `ui-spec/toast-messages.md`

## Instructions
1. Create `src/__tests__/toast-messages.test.ts`.
2. Import all 65 toast operation constants from `src/lib/toast-messages.ts`.
3. Assert all 65 operations have a defined `message` (non-empty string).
4. Assert all 65 operations have a defined `duration` (positive number).
5. Assert all 65 operations have a defined `type` (one of 'success', 'error', 'info', 'warning').
6. Spot-check 10 specific messages against the exact text in the spec:
   - e.g., project creation success message matches spec wording
   - e.g., delete confirmation message matches spec wording
7. Test the `showToast` wrapper calls the toast library with correct params for a known operation.

## Verify
- `npx vitest run src/__tests__/toast-messages.test.ts` — all tests pass

## Commit
`podplay(forward): stage 034 - toast message tests with 10 spot-checks`
