# Stage 164 — Desktop Verify: /inventory

## Spec Files
- `ui-spec/empty-states.md`
- `ui-spec/loading-states.md`

## Instructions
1. Open Playwright browser at viewport 1280x800.
2. Navigate to `http://localhost:5175/inventory`.
3. Take a screenshot and save to `screenshots/desktop-inventory.png`.
4. Verify: stock table is shown (or empty state if no items), category grouping is present, no console errors.
5. If any check fails, fix the issue in the source code and re-verify.

## Verify
- `screenshots/desktop-inventory.png` exists — shows inventory table or empty state with category grouping

## Commit
`podplay(forward): stage 164 - desktop verify /inventory`
