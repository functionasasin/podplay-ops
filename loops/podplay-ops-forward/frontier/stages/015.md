# Stage 015 — Tests: Inventory Tables

## Spec Files
- `data-model/schema.md`

## Instructions
1. Write `apps/podplay/src/__tests__/db/inventory-tables.test.ts`
2. Test cases:
   - Insert an inventory row for a catalog item, verify quantity_on_hand and reorder_point
   - Insert an inventory_movement of type 'received', verify it records correctly
   - Insert movements of each type (received, allocated, shipped, adjusted, returned)
   - Verify FK constraint: inventory.item_id must reference a valid hardware_catalog row
   - Verify unique constraint: only one inventory row per item_id
   - Verify reference_type and reference_id are nullable (insert movement with and without reference)
3. Clean up test data after each test

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/db/` — all tests pass

## Commit
`podplay(forward): stage 015 - inventory tables integration tests`
