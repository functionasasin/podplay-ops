# Stage 020 — BOM Templates + Deployment Checklists + Defaults Seed

## Spec Files
- `data-model/seed-data.md` (BOM templates, deployment checklists, default settings sections)

## Instructions
1. Create `apps/podplay/supabase/migrations/00007_seed_templates.sql`
2. Insert BOM templates for each of the 4 tiers (Pro, Autonomous, Autonomous+, PBK):
   - Each tier has specific catalog items with default quantities
   - Include quantity_rule jsonb for items that scale (e.g., speakers scale by zone_count)
   - Mark required vs optional items per tier
3. Insert deployment checklist templates for all 15 phases:
   - Each phase has individual steps with ordering
   - Include step descriptions and any default assignee roles
4. Insert default settings:
   - Tax rate (e.g., 0.0825)
   - Shipping rates by method
   - Margin targets by tier
   - Any other default values from the spec
5. Use idempotent inserts (ON CONFLICT DO NOTHING)

## Verify
- `cd apps/podplay && npx supabase db reset` — exits 0, all seed data present

## Commit
`podplay(forward): stage 020 - seed BOM templates, deployment checklists, default settings`
