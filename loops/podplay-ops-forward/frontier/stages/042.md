# Stage 042 — New Project Route (/projects/new)

## Spec Files
- `ui-spec/dashboard.md`
- `business-logic/customer-onboarding.md`

## Instructions
1. Create `src/routes/projects/new.tsx`.
2. Build a form with React Hook Form + Zod schema:
   - `project_name` (required, min 2 chars)
   - `customer_name` (required, min 2 chars)
   - `venue_name` (optional)
3. Use shadcn form components (Input, Label, Button).
4. On submit, insert into `projects` table via Supabase with `status='intake'`.
5. On success, redirect to `/projects/{id}/intake`.
6. On error, show error via toast system.
7. Include a "Cancel" button that navigates back to `/projects`.
8. Register `/projects/new` route in the router config.

## Verify
- `npm run build` — build passes with zero errors

## Commit
`podplay(forward): stage 042 - new project creation form`
