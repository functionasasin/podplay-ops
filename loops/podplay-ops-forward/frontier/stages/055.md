# Stage 055 — Step 5: Installer Selection

## Spec Files
- `ui-spec/wizard-intake.md`

## Instructions
1. Create `src/components/wizard/intake/InstallerSelectionStep.tsx`.
2. On mount, query the `installers` table via Supabase client to fetch available installers (id, name, location).
3. Render a dropdown/select populated with installer names and their locations as secondary text.
4. On selection, store the `installer_id` in the parent wizard form state.
5. Handle loading state (spinner while fetching) and empty state (message if no installers found).
6. A "Next" button advances to the next step (disabled until an installer is selected).

## Verify
- `npm run build` — build passes

## Commit
`podplay(forward): stage 055 - installer selection (intake step 5)`
