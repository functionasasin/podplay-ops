# Stage 021 — Tests: Seed Data Completeness

## Spec Files
- `data-model/seed-data.md`

## Instructions
1. Write `apps/podplay/src/__tests__/db/seed-completeness.test.ts`
2. Test cases:
   - Assert exactly 4 distinct tiers in bom_templates (Pro, Autonomous, Autonomous+, PBK)
   - Assert each tier has the expected number of BOM items per the spec
   - Assert 15 deployment phases exist in checklist templates
   - Count total checklist steps across all 15 phases, assert matches spec total
   - Assert each phase has at least 1 step
   - Assert default settings row exists with key 'tax_rate' and expected value
   - Assert shipping rate settings exist
   - Assert margin target settings exist for each tier
3. Use Supabase client pointed at local instance

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/db/seed-completeness.test.ts` — all tests pass

## Commit
`podplay(forward): stage 021 - seed data completeness verification tests`
