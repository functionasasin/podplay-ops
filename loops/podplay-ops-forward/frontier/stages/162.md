# Stage 162 — Desktop Verify: /projects/$id/deployment

## Spec Files
- `ui-spec/empty-states.md`
- `ui-spec/loading-states.md`

## Instructions
1. Open Playwright browser at viewport 1280x800.
2. Navigate to a project's deployment page (`/projects/$id/deployment`).
3. Take a screenshot and save to `screenshots/desktop-deployment.png`.
4. Verify: 15 phases are listed, progress bar is visible, clicking a phase shows its checklist items, no console errors.
5. If any check fails, fix the issue in the source code and re-verify.

## Verify
- `screenshots/desktop-deployment.png` exists — shows 15 deployment phases with progress bar

## Commit
`podplay(forward): stage 162 - desktop verify /projects/$id/deployment`
