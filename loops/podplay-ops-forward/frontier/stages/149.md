# Stage 149 — Confirmation Dialogs (17)

## Spec Files
- `ui-spec/confirmation-dialogs.md`

## Instructions
1. Create `src/components/ui/ConfirmDialog.tsx` wrapping shadcn AlertDialog. Props: title, body, confirmLabel, cancelLabel, destructive flag, onConfirm, onCancel.
2. Create `src/lib/confirmation-dialogs.ts` with 17 dialog configs from the spec (e.g., delete project, remove BOM item, cancel invoice, remove team member).
3. Wire `ConfirmDialog` into all 17 destructive actions across the app, replacing any direct mutations.
4. Destructive dialogs must use red/destructive button variant.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 149 - ConfirmDialog component + 17 destructive action configs`
