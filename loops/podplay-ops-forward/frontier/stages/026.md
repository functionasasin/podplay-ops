# Stage 026 — Route Guards

## Spec Files
- `ui-spec/routes.md`

## Instructions
1. Create `src/lib/auth-guard.tsx`.
2. Implement a `ProtectedRoute` component that uses `useAuth()` to check authentication state.
3. While `loading` is true, render a full-page spinner/skeleton.
4. If `user` is null (not authenticated), redirect to `/login` using `Navigate` from react-router.
5. If authenticated, render `<Outlet />` (or children).
6. Update the router config to wrap all authenticated routes with `ProtectedRoute`.
7. Leave `/login` and `/auth/callback` as public (unguarded) routes.

## Verify
- `npm run build` — build passes with zero errors

## Commit
`podplay(forward): stage 026 - route guards with ProtectedRoute component`
