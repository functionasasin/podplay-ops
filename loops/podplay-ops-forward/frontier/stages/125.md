# Stage 125 — Movement History

## Spec Files
- `ui-spec/inventory-view.md`

## Instructions
1. Create `src/components/inventory/MovementHistory.tsx`.
2. Timeline per inventory item: date, type (in/out/adjust), quantity, reference.
3. Sort newest first.
4. Reference links to source (e.g., project procurement).

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 125 - inventory movement history timeline`
