# Stage 180 — Orphan Component Sweep

## Spec Files
- `testing/test-plan.md`

## Instructions
1. List all files in `src/components/` and `src/lib/` recursively.
2. For each exported component or function, grep the entire `src/` tree for imports of that name.
3. Flag any file with zero imports from other files (orphan/dead code).
4. For each orphan: if it belongs in a route or layout per the spec, wire it in. If it is truly unused, remove it.
5. Run the build to confirm no regressions.

## Verify
- `cd apps/podplay && npm run build` — exits 0, no unused export warnings

## Commit
`podplay(forward): stage 180 - orphan component sweep, remove dead code`
