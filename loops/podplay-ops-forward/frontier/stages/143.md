# Stage 143 — Empty States (19 components)

## Spec Files
- `ui-spec/empty-states.md`

## Instructions
1. Create `src/components/ui/EmptyState.tsx` generic component accepting icon, heading, description, and CTA (label + onClick) props.
2. Create `src/lib/empty-state-configs.ts` with 19 specific configs from the spec, each keyed by view name.
3. Wire `EmptyState` into every list/table view: projects list, inventory list, BOM table, deployment phases, invoices list, expenses list, team list, catalog list, and all other applicable views.
4. Each empty state must render the correct icon, heading, description, and CTA text per the spec.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 143 - empty states generic component + 19 configs wired into all views`
