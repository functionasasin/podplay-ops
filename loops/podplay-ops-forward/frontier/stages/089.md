# Stage 089 — Smart Checklist Component

## Spec Files
- `ui-spec/wizard-deployment.md`

## Instructions
1. Create `src/components/wizard/deployment/SmartChecklist.tsx`.
2. Checkbox items with auto-fill tokens ({{DDNS_SUBDOMAIN}}, {{LOCATION_ID}}, etc.) replaced with project data.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 089 - smart checklist component`
