# Stage 028 — Tests: Layout Renders Nav Links

## Spec Files
- `ui-spec/routes.md`
- `ui-spec/dashboard.md`

## Instructions
1. Create `src/__tests__/layout.test.ts`.
2. Mock `useAuth` to return an authenticated user.
3. Render `AppLayout` inside a `MemoryRouter`.
4. Assert nav links are present for: Projects (`/projects`), Inventory (`/inventory`), Financials (`/financials`), Settings (`/settings/pricing`).
5. Assert the sidebar element renders (query by role or test-id).
6. Assert the app title "PodPlay Ops" is visible.
7. Assert a sign-out button is present.

## Verify
- `npx vitest run src/__tests__/layout.test.ts` — all tests pass

## Commit
`podplay(forward): stage 028 - layout nav link tests`
