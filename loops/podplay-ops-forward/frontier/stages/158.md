# Stage 158 — Desktop Verify: /projects (Dashboard)

## Spec Files
- `ui-spec/empty-states.md`
- `ui-spec/loading-states.md`

## Instructions
1. Open Playwright browser at viewport 1280x800.
2. Sign in (or bypass auth for local dev).
3. Navigate to `http://localhost:5175/projects`.
4. Take a screenshot and save to `screenshots/desktop-dashboard.png`.
5. Verify: AppLayout with sidebar is visible, project table or empty state with CTA is shown, filters section is present, metrics bar is visible, no console errors.
6. If any check fails, fix the issue in the source code and re-verify.

## Verify
- `screenshots/desktop-dashboard.png` exists — shows dashboard with sidebar, project list or empty state, filters, metrics

## Commit
`podplay(forward): stage 158 - desktop verify /projects dashboard`
