# Stage 001 — Vite + React 19 + TypeScript Strict Scaffold

## Spec Files
- `deployment/infrastructure.md`

## Instructions
1. `mkdir -p apps/podplay && cd apps/podplay`
2. `npm create vite@latest . -- --template react-ts`
3. Set `"strict": true` in `tsconfig.json` (and `tsconfig.app.json` if present)
4. Verify `src/main.tsx` renders the default Vite React app

## Verify
- `cd apps/podplay && npm install && npm run build` — exits 0 with no type errors

## Commit
`podplay(forward): stage 001 - Vite + React 19 + TypeScript strict scaffold`
