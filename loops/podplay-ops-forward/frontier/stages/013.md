# Stage 013 — Tests: Hardware Tables

## Spec Files
- `data-model/schema.md`

## Instructions
1. Write `apps/podplay/src/__tests__/db/hardware-tables.test.ts`
2. Test cases:
   - Insert a hardware_catalog item with all fields, select it back, verify columns
   - Insert a duplicate SKU — expect unique constraint violation
   - Create a bom_template row linked to a catalog item and tier
   - Create a project_bom_item linked to a project and catalog item
   - Verify FK constraint: attempt to delete a catalog item referenced by bom_template — expect error (RESTRICT)
   - Verify FK cascade: delete a project, expect project_bom_items to be deleted (CASCADE)
   - Verify unit_cost_override is nullable and falls back to catalog unit_cost when null
3. Clean up test data after each test

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/db/` — all tests pass

## Commit
`podplay(forward): stage 013 - hardware tables integration tests`
