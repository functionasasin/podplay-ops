# Stage 038 — Tests: Status Pills

## Spec Files
- `ui-spec/dashboard.md`
- `ui-spec/enum-labels.md`

## Instructions
1. Create `src/__tests__/dashboard/status-pills.test.ts`.
2. For each `project_status` enum value, render `ProjectList` with a mock project having that status.
3. Assert the correct display label appears in the DOM for each status.
4. Assert the correct badge className is applied for each status (check the element's class list).
5. Test the tier badge renders correctly for at least 3 different `project_tier` values.
6. Test that the go-live date is formatted (not raw ISO string).
7. Test that progress percentage is formatted (not raw decimal).

## Verify
- `npx vitest run src/__tests__/dashboard/status-pills.test.ts` — all tests pass

## Commit
`podplay(forward): stage 038 - status pill and tier badge tests`
