# Stage 030 — Tests: Enum Labels

## Spec Files
- `ui-spec/enum-labels.md`

## Instructions
1. Create `src/__tests__/enum-labels.test.ts`.
2. For each of the 21 enum types, test:
   - Every value in the enum has a defined display label (no `undefined`).
   - Every value has a defined badge className (no `undefined`).
   - Sort order covers all values (no gaps, no missing keys).
3. Spot-check at least 5 specific labels against the spec text:
   - e.g., `project_status.intake` should display as the exact label from the spec.
   - e.g., verify a badge className for a specific status matches the spec.
4. Test the generic `getEnumLabel` helper returns correct results.
5. Test the generic `getEnumBadgeClass` helper returns correct results.

## Verify
- `npx vitest run src/__tests__/enum-labels.test.ts` — all tests pass

## Commit
`podplay(forward): stage 030 - enum label tests for all 21 enum types`
