# Stage 102 — Tests: Phase Warnings

## Spec Files
- `business-logic/power-calculations.md`

## Instructions
1. Write `src/__tests__/wizard/deployment/phase-warnings.test.ts`.
2. Test threshold triggers for PoE ports, UPS runtime, and power warnings.
3. Assert warning banners appear when thresholds exceeded and are absent when within limits.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/wizard/deployment/phase-warnings.test.ts` — all tests pass

## Commit
`podplay(forward): stage 102 - tests for phase warnings`
