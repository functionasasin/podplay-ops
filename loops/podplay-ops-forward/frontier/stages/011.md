# Stage 011 — Tests: Core Tables

## Spec Files
- `data-model/schema.md`

## Instructions
1. Write `apps/podplay/src/__tests__/db/core-tables.test.ts`
2. Use Supabase client from `src/lib/supabase.ts` (pointed at local instance)
3. Test cases:
   - Insert a project row with all required fields, select it back, verify column names match spec
   - Update project status, verify updated_at changes
   - Insert an installer row, select it back
   - Insert a settings row with jsonb value, read it back
   - Test RLS: unauthenticated request is rejected (if RLS enforced)
4. Clean up test data after each test (delete inserted rows)

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/db/` — all tests pass

## Commit
`podplay(forward): stage 011 - core tables integration tests`
