# Stage 088 — Tests: Deployment Shell

## Spec Files
- `ui-spec/wizard-deployment.md`

## Instructions
1. Write `src/__tests__/wizard/deployment/shell.test.ts`.
2. Assert: 15 phases listed, progress bar at 0% for unchecked project, selecting phase shows checklist.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/wizard/deployment/shell.test.ts` — all tests pass

## Commit
`podplay(forward): stage 088 - tests for deployment shell`
