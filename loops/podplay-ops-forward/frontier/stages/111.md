# Stage 111 — Final Invoice Form

## Spec Files
- `ui-spec/wizard-financials.md`
- `business-logic/invoicing-expenses.md`

## Instructions
1. Create `src/components/wizard/financials/FinalInvoice.tsx`.
2. Auto-calculate balance = total_price - deposit_amount.
3. Gate: only render/enable after project go-live (status='completed' or go_live_date set).
4. Submit inserts invoice with type='final' into Supabase.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 111 - final invoice form with go-live gate`
