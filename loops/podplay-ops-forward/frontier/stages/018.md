# Stage 018 — Hardware Catalog Seed

## Spec Files
- `data-model/seed-data.md` (hardware catalog section)

## Instructions
1. Create `apps/podplay/supabase/migrations/00006_seed_hardware.sql`
2. Insert all 50 hardware catalog items with exact values from the spec:
   - SKU, name, vendor, category, unit_cost, description, is_active
3. Use INSERT with ON CONFLICT (sku) DO NOTHING to make the seed idempotent
4. Group inserts by category for readability (speakers, networking, displays, mounts, cabling, etc.)

## Verify
- `cd apps/podplay && npx supabase db reset` — exits 0
- `cd apps/podplay && npx supabase db query "SELECT count(*) FROM hardware_catalog"` — returns 50

## Commit
`podplay(forward): stage 018 - seed 50 hardware catalog items`
