# Stage 173 — Mobile Verify: /projects/new + intake

## Spec Files
- `ui-spec/responsive.md`
- `ui-spec/wizard-intake.md`

## Instructions
1. Open browser at viewport 375x812.
2. Navigate to `/projects/new`. Screenshot to `screenshots/mobile-new-project.png`.
3. Verify: form renders in a single column, inputs are full-width, stepper is readable.
4. Proceed to intake step. Screenshot to `screenshots/mobile-intake.png`.
5. Verify: all form fields are single column, full-width, touch targets are >= 44px.

## Verify
- `ls screenshots/mobile-new-project.png screenshots/mobile-intake.png` — both files exist
- Visual: forms single column, inputs full-width, stepper readable, touch targets >= 44px

## Commit
`podplay(forward): stage 173 - mobile verify new project and intake wizard`
