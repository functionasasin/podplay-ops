# Stage 057 — Step 6: Financial Setup

## Spec Files
- `ui-spec/wizard-intake.md`
- `ui-spec/validation-messages.md`

## Instructions
1. Create `src/components/wizard/intake/FinancialSetupStep.tsx`.
2. Fields: `target_go_live_date` (date picker input, must be a future date), `deposit_amount` (currency input, must be > 0).
3. Define a Zod schema with exact validation messages from `ui-spec/validation-messages.md` — date must be in the future, amount must be greater than zero.
4. Use react-hook-form with the Zod resolver. Display inline errors.
5. Format the currency input with dollar sign prefix and two decimal places.
6. On valid submission, store data and advance to the review step.

## Verify
- `npm run build` — build passes

## Commit
`podplay(forward): stage 057 - financial setup form (intake step 6)`
