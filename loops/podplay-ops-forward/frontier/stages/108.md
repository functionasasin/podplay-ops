# Stage 108 — Tests: Financials Tabs

## Spec Files
- `ui-spec/wizard-financials.md`

## Instructions
1. Write `src/__tests__/wizard/financials/tabs.test.ts`.
2. Assert all 4 tabs render: Invoicing, Expenses, P&L Summary, Go-Live.
3. Assert clicking each tab shows the corresponding content area.
4. Assert default active tab is Invoicing.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/wizard/financials/tabs.test.ts` — tests pass

## Commit
`podplay(forward): stage 108 - tests for financials tab navigation`
