# Stage 083 — CC Terminal Ordering

## Spec Files
- `ui-spec/wizard-procurement.md`

## Instructions
1. Create `src/components/wizard/procurement/CcTerminalOrder.tsx`.
2. Form: serial number input, status tracking (ordered/shipped/received/installed).
3. Insert into cc_terminals table.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 083 - CC terminal ordering`
