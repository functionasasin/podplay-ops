# Stage 174 — Mobile Verify: /projects/$id/procurement

## Spec Files
- `ui-spec/responsive.md`
- `ui-spec/wizard-procurement.md`

## Instructions
1. Open browser at viewport 375x812.
2. Navigate to `/projects/$id/procurement` for an existing project.
3. Screenshot to `screenshots/mobile-procurement.png`.
4. Verify: tabs are scrollable or stacked vertically.
5. Verify: BOM table has `overflow-x: auto` and scrolls horizontally without clipping page content.
6. Verify: no content is clipped or hidden behind other elements.

## Verify
- `ls screenshots/mobile-procurement.png` — file exists
- Visual: tabs scrollable or stacked, BOM table overflow-x-auto, no content clipped

## Commit
`podplay(forward): stage 174 - mobile verify procurement view`
