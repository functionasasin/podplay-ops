# Stage 146 — Tests: Loading States

## Spec Files
- `ui-spec/loading-states.md`

## Instructions
1. Write `src/__tests__/ui/loading-states.test.ts`.
2. Render each of the 10 skeleton variants and assert they produce the correct number of placeholder elements matching page layout.
3. Test 5 representative form-submit loaders: assert spinner appears, button text changes, button is disabled during loading.
4. Assert loaders restore original state after completion.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/ui/loading-states.test.ts` — all tests pass

## Commit
`podplay(forward): stage 146 - tests for 10 skeletons + 5 form-submit loaders`
