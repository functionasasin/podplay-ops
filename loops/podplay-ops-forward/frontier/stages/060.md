# Stage 060 — Tests: Review Renders All Fields

## Spec Files
- `ui-spec/wizard-intake.md`

## Instructions
1. Write `src/__tests__/wizard/intake/review.test.ts`.
2. Provide mock form data for all 6 steps (customer name, email, phone, venue address, court count, door count, camera count, front desk, wifi, tier, ISP provider, static IP, speeds, installer, go-live date, deposit).
3. Assert every field value from the mock data is rendered in the review summary.
4. Assert each section has an "Edit" link/button.
5. Assert clicking an "Edit" link calls the navigation handler with the correct step index (0 for Customer Info, 1 for Venue Config, etc.).

## Verify
- `npx vitest run src/__tests__/wizard/intake/review.test.ts` — all tests pass

## Commit
`podplay(forward): stage 060 - tests for review step`
