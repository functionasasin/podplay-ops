# Stage 051 — Step 3: Service Tier Selection

## Spec Files
- `ui-spec/wizard-intake.md`
- `ui-spec/enum-labels.md`

## Instructions
1. Create `src/components/wizard/intake/TierSelectionStep.tsx`.
2. Render 4 radio cards in a grid layout for the service tiers: Pro, Autonomous, Autonomous+, PBK.
3. Each card displays the tier name and description from `ui-spec/enum-labels.md`, plus a list of key features from the spec.
4. Only one tier can be selected at a time — use radio button behavior.
5. On selection, store the `service_tier` value in the parent wizard form state.
6. A "Next" button advances to the next step (disabled until a tier is selected).

## Verify
- `npm run build` — build passes

## Commit
`podplay(forward): stage 051 - service tier selection (intake step 3)`
