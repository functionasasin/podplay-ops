# Stage 023 — Tests: Auth Hooks

## Spec Files
- `ui-spec/routes.md`

## Instructions
1. Create `src/__tests__/auth.test.ts`.
2. Mock the Supabase client (`@supabase/supabase-js`) so `onAuthStateChange` and auth methods can be controlled.
3. Test: `useAuth` returns `loading=true` initially before auth state resolves.
4. Test: When mock Supabase auth emits a session with a user, assert `user` is populated and `loading` becomes `false`.
5. Test: Calling `signOut` clears user and session to `null`.
6. Test: `signIn` delegates to `supabase.auth.signInWithPassword` with correct args.
7. Test: `signUp` delegates to `supabase.auth.signUp` with correct args.

## Verify
- `npx vitest run src/__tests__/auth.test.ts` — all tests pass

## Commit
`podplay(forward): stage 023 - auth hook tests`
