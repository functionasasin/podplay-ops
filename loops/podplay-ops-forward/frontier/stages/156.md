# Stage 156 — Desktop Verify: /login

## Spec Files
- `ui-spec/empty-states.md`
- `ui-spec/loading-states.md`

## Instructions
1. Start the dev server: `cd apps/podplay && npm run dev`.
2. Open Playwright browser at viewport 1280x800.
3. Navigate to `http://localhost:5175/login`.
4. Take a screenshot and save to `screenshots/desktop-login.png`.
5. Verify: email + password form is present, magic link option visible, no console errors, no placeholder text (e.g., "Lorem ipsum", "TODO", "placeholder").
6. If any check fails, fix the issue in the source code and re-verify.

## Verify
- `screenshots/desktop-login.png` exists — shows login form with email + password fields, no errors

## Commit
`podplay(forward): stage 156 - desktop verify /login`
