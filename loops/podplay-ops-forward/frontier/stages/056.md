# Stage 056 — Tests: Installer Selection

## Spec Files
- `ui-spec/wizard-intake.md`

## Instructions
1. Write `src/__tests__/wizard/intake/installer-selection.test.ts`.
2. Mock the Supabase query to return a list of 3 installers with id, name, and location.
3. Assert the dropdown renders all 3 installer names.
4. Assert selecting an installer stores the correct `installer_id` in form state.
5. Assert the loading state shows a spinner before data loads.
6. Assert the empty state shows a "no installers" message when the query returns an empty array.

## Verify
- `npx vitest run src/__tests__/wizard/intake/installer-selection.test.ts` — all tests pass

## Commit
`podplay(forward): stage 056 - tests for installer selection`
