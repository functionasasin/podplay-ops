# Stage 037 — Project List Table

## Spec Files
- `ui-spec/dashboard.md`

## Instructions
1. Create `src/components/dashboard/ProjectList.tsx`.
2. Accept `projects` array as a prop.
3. Render a shadcn `Table` with columns:
   - Project name (linked to `/projects/{id}/intake`)
   - Customer name
   - Status pill — use `getEnumLabel('project_status', status)` and `getEnumBadgeClass('project_status', status)` from enum-labels
   - Tier badge — use enum-labels for `project_tier`
   - Go-live date — formatted with `formatDate`
   - Progress % — formatted with `formatPercent`
4. Empty state: if no projects, show a message with a link to `/projects/new`.
5. Replace the stub in the dashboard route with the real `ProjectList` component.

## Verify
- `npm run build` — build passes with zero errors

## Commit
`podplay(forward): stage 037 - project list table with status pills and tier badges`
