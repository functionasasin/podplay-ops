# Stage 147 — Validation Messages (140+ wired)

## Spec Files
- `ui-spec/validation-messages.md`

## Instructions
1. Create `src/lib/validation-messages.ts` exporting all 140+ validation message constants keyed by field/context (e.g., `VALIDATION.project.name.required`, `VALIDATION.intake.customer.email.format`).
2. Replace every inline error string in every Zod schema across the codebase with the corresponding constant from `validation-messages.ts`.
3. Ensure messages match the exact wording in the spec.
4. Grep the entire `src/` directory to confirm no hardcoded validation strings remain outside `validation-messages.ts`.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 147 - 140+ validation message constants wired into all Zod schemas`
