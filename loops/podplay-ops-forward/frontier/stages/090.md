# Stage 090 — Tests: Checklist Tokens

## Spec Files
- `ui-spec/wizard-deployment.md`

## Instructions
1. Write `src/__tests__/wizard/deployment/checklist-tokens.test.ts`.
2. Mock project data.
3. Assert: tokens replaced with actual values, no raw {{TOKEN}} remains.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/wizard/deployment/checklist-tokens.test.ts` — all tests pass

## Commit
`podplay(forward): stage 090 - tests for checklist tokens`
