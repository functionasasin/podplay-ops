# Stage 69 — Procurement Route Shell + Tabs

## Spec Files
- `ui-spec/wizard-procurement.md`

## Instructions
1. Create `src/routes/projects/$projectId/procurement.tsx`.
2. Wrap in ProtectedRoute + AppLayout.
3. Load project data from Supabase using the projectId param.
4. Implement a tab layout with 6 sections: BOM Review, Inventory Check, Purchase Orders, Packing List, CC Terminals, Replay Signs.
5. Each tab click shows the corresponding content area (placeholder panels for now).
6. Register the route in the router config.

## Verify
- `npm run build` — build passes with no errors

## Commit
`podplay(forward): stage 69 - procurement route shell with 6-tab layout`
