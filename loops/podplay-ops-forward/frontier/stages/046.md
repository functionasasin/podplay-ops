# Stage 046 — Tests: Intake Stepper

## Spec Files
- `ui-spec/wizard-intake.md`

## Instructions
1. Write `src/__tests__/wizard/intake-stepper.test.ts`.
2. Assert that all 7 steps render with the correct labels: Customer Info, Venue Config, Service Tier, ISP Info, Installer, Financial Setup, Review & Submit.
3. Assert clicking a step fires the navigation callback with the correct step index.
4. Assert the current step receives a highlighted/active CSS class or aria attribute.
5. Assert the stepper visually shows progress (completed steps distinguished from upcoming steps).

## Verify
- `npx vitest run src/__tests__/wizard/intake-stepper.test.ts` — all tests pass

## Commit
`podplay(forward): stage 046 - tests for intake stepper`
