# Stage 044 — Project Creation Logic

## Spec Files
- `business-logic/customer-onboarding.md`

## Instructions
1. Update `src/routes/projects/new.tsx` to implement the full creation flow from the customer-onboarding spec.
2. After inserting the project row, seed `deployment_checklist_items` for all 15 phases defined in the onboarding templates:
   - Query or hard-code the 15 phase templates with their checklist items.
   - Insert all checklist items linked to the new project ID with `status='pending'`.
3. Wrap the project insert + checklist seeding in a transaction or sequential calls with error handling.
4. If checklist seeding fails after project creation, show a warning toast but still redirect (project exists, checklist can be retried).
5. On full success, show a success toast and redirect to `/projects/{id}/intake`.
6. On project insert failure, show an error toast and stay on the form.

## Verify
- `npm run build` — build passes with zero errors
- `npx vitest run` — all existing tests still pass

## Commit
`podplay(forward): stage 044 - full project creation with checklist seeding`
