# Stage 185 — CONVERGE: Convergence Gate

## Spec Files
- `testing/test-plan.md`
- `ui-spec/routes.md`

## Instructions
1. Run `cd apps/podplay && npm run build` — must exit 0.
2. Run `cd apps/podplay && npx vitest run` — must exit 0.
3. Verify `screenshots/` contains a screenshot for every route defined in `ui-spec/routes.md`.
4. Review `current-stage.md` — confirm all 184 prior stages are marked done.
5. If ANY check fails: do NOT write converged.txt. Instead, fix the issue or create additional stages.
6. If ALL checks pass: write `loops/podplay-ops-forward/status/converged.txt` with the following content:
   - Total stages completed
   - Test count and pass rate
   - Build status
   - Routes verified (list each)
   - Date of convergence
7. This is the FINAL stage. Only write `converged.txt` if everything truly passes.

## Verify
- `cd apps/podplay && npm run build` — exits 0
- `cd apps/podplay && npx vitest run` — exits 0
- `ls screenshots/` — screenshot for every route
- `cat loops/podplay-ops-forward/status/converged.txt` — file exists with summary

## Commit
`podplay(forward): converged — all routes verified, all tests pass`
