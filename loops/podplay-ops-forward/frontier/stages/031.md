# Stage 031 — Formatter Utilities

## Spec Files
- `ui-spec/formatting.md`

## Instructions
1. Create `src/lib/formatters.ts`.
2. Implement all 13 formatter functions defined in `ui-spec/formatting.md`:
   - `formatCurrency(value)` — USD with 2 decimal places (e.g., "$1,234.56")
   - `formatDate(value)` — per spec format
   - `formatPercent(value)` — per spec format
   - `formatPhone(value)` — per spec format
   - `truncateText(value, maxLength)` — per spec format
   - `formatBadge(value)` — per spec format
   - All remaining functions as specified in the formatting spec
3. Use exact formatting rules from the spec (locale, decimal places, prefixes/suffixes).
4. Handle null/undefined inputs gracefully — return empty string or "N/A" per spec.

## Verify
- `npm run build` — build passes with zero errors

## Commit
`podplay(forward): stage 031 - all 13 formatter utilities`
