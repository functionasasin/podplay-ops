# Stage 052 — Tests: Tier Selection

## Spec Files
- `ui-spec/enum-labels.md`

## Instructions
1. Write `src/__tests__/wizard/intake/tier-selection.test.ts`.
2. Assert that exactly 4 tier cards render with labels matching `ui-spec/enum-labels.md`: Pro, Autonomous, Autonomous+, PBK.
3. Assert that selecting a tier updates the form state with the correct `service_tier` value.
4. Assert that only one tier can be selected at a time — selecting a new tier deselects the previous one.
5. Assert that the Next button is disabled when no tier is selected and enabled once one is chosen.

## Verify
- `npx vitest run src/__tests__/wizard/intake/tier-selection.test.ts` — all tests pass

## Commit
`podplay(forward): stage 052 - tests for tier selection`
