# Stage 035 — Dashboard Route (/projects)

## Spec Files
- `ui-spec/dashboard.md`
- `ui-spec/routes.md`

## Instructions
1. Create `src/routes/projects/index.tsx`.
2. Wrap in `ProtectedRoute` and render inside `AppLayout` (via router nesting).
3. On mount, query Supabase: `supabase.from('projects').select('*')`.
4. Manage loading, error, and data states.
5. While loading, show a skeleton/spinner.
6. On error, show an error message with retry option.
7. Pass fetched projects data to a `ProjectList` component (create as a stub that renders `<div>ProjectList placeholder</div>` for now).
8. Register `/projects` route in the router config as the default authenticated route.

## Verify
- `npm run build` — build passes with zero errors

## Commit
`podplay(forward): stage 035 - dashboard route with Supabase query`
