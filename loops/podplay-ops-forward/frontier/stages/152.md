# Stage 152 — Tests: Keyboard Nav

## Spec Files
- `ui-spec/keyboard-nav.md`

## Instructions
1. Write `src/__tests__/ui/keyboard-nav.test.ts`.
2. Test tab order: first Tab focuses skip-to-content link, subsequent Tabs follow logical DOM order.
3. Test Enter submits a form (render a sample form, press Enter on input, assert submit handler fires).
4. Test Escape closes a modal (open ConfirmDialog, press Escape, assert it unmounts).
5. Test focus trap: with modal open, Tab cycles within modal elements only.
6. Test skip-to-content link: clicking it moves focus to `<main>` region.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/ui/keyboard-nav.test.ts` — all tests pass

## Commit
`podplay(forward): stage 152 - tests for keyboard nav, focus trap, skip link`
