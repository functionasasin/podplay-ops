# Stage 145 — Loading States (10 skeletons, 35 loaders)

## Spec Files
- `ui-spec/loading-states.md`

## Instructions
1. Create `src/components/ui/Skeleton.tsx` with variants for 10 pages (projects list, inventory, procurement BOM, deployment, financials, intake form, catalog, team, pricing, travel settings).
2. Each skeleton variant mirrors the layout of its page with pulsing placeholder blocks.
3. Create `src/lib/loading.ts` with 35 form-submit loader configs (button text, spinner, disabled state) covering every form submission in the app.
4. Wire skeleton variants into each page's loading/suspense state.
5. Wire form-submit loaders into every form's submit button.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 145 - 10 skeleton variants + 35 form-submit loader configs`
