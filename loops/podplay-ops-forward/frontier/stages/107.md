# Stage 107 — Financials Route Shell + Tabs

## Spec Files
- `ui-spec/wizard-financials.md`

## Instructions
1. Create `src/routes/projects/$projectId/financials.tsx`.
2. Tab layout with 4 tabs: Invoicing, Expenses, P&L Summary, Go-Live.
3. Wrap in ProtectedRoute + AppLayout.
4. Load project financial data from Supabase (invoices, expenses).
5. Each tab renders a placeholder content area that will be filled in subsequent stages.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 107 - financials route shell with tab layout`
