# Stage 126 — Tests: Movement History

## Spec Files
- `ui-spec/inventory-view.md`

## Instructions
1. Write `src/__tests__/inventory/movement-history.test.ts`.
2. Assert entries sorted chronologically newest first.
3. Assert all movement types render (in, out, adjust).
4. Assert reference links are present and correct.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/inventory/movement-history.test.ts` — tests pass

## Commit
`podplay(forward): stage 126 - tests for movement history`
