# Stage 036 — Tests: Dashboard Query

## Spec Files
- `ui-spec/dashboard.md`

## Instructions
1. Create `src/__tests__/dashboard.test.ts`.
2. Mock the Supabase client so `from('projects').select('*')` returns controlled data.
3. Test: The component calls `supabase.from('projects')` on mount.
4. Test: While the query is pending, a loading state (spinner/skeleton) is rendered.
5. Test: After the query resolves with mock project rows, project data is rendered (assert ProjectList receives the data or rows appear in the DOM).
6. Test: When the query rejects with an error, an error message is displayed.

## Verify
- `npx vitest run src/__tests__/dashboard.test.ts` — all tests pass

## Commit
`podplay(forward): stage 036 - dashboard query tests`
