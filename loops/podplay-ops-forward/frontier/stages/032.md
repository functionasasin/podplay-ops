# Stage 032 — Tests: Formatters

## Spec Files
- `ui-spec/formatting.md`

## Instructions
1. Create `src/__tests__/formatters.test.ts`.
2. Test each of the 13 formatter functions with exact input/output pairs from the spec.
3. Test edge cases for every function:
   - `null` input
   - `undefined` input
   - Zero (`0`)
   - Negative numbers (for numeric formatters)
   - Empty string (for string formatters)
4. Test `formatCurrency(1234.5)` returns `"$1,234.50"` (or exact spec format).
5. Test `formatPercent(0.856)` returns the exact spec-defined output.
6. Test `truncateText` with strings shorter than, equal to, and longer than maxLength.

## Verify
- `npx vitest run src/__tests__/formatters.test.ts` — all tests pass

## Commit
`podplay(forward): stage 032 - formatter tests with edge cases`
