# Stage 144 — Tests: Empty States

## Spec Files
- `ui-spec/empty-states.md`

## Instructions
1. Write `src/__tests__/ui/empty-states.test.ts`.
2. Render each of the 19 empty state configs via the `EmptyState` component.
3. Assert each renders the correct icon, heading text, description text, and CTA button text matching the spec.
4. Assert CTA click fires the provided callback.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/ui/empty-states.test.ts` — all 19 tests pass

## Commit
`podplay(forward): stage 144 - tests for 19 empty state configs`
