# Stage 019 — Tests: Hardware Catalog Seed

## Spec Files
- `data-model/seed-data.md`

## Instructions
1. Write `apps/podplay/src/__tests__/db/seed-hardware.test.ts`
2. Test cases:
   - Assert `SELECT count(*) FROM hardware_catalog` returns exactly 50
   - Spot-check 5 specific SKUs with exact vendor and unit_cost values from spec:
     - Pick one from each major category (speakers, networking, displays, mounts, cabling)
     - Assert vendor name matches exactly
     - Assert unit_cost matches exactly (use numeric comparison, not string)
   - Assert all rows have is_active = true
   - Assert no duplicate SKUs exist
3. Use Supabase client pointed at local instance

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/db/seed-hardware.test.ts` — all tests pass

## Commit
`podplay(forward): stage 019 - hardware catalog seed verification tests`
