# Stage 114 — Tests: Expense CRUD

## Spec Files
- `ui-spec/wizard-financials.md`
- `business-logic/invoicing-expenses.md`

## Instructions
1. Write `src/__tests__/wizard/financials/expenses.test.ts`.
2. Assert add expense creates row with correct fields.
3. Assert edit expense updates values.
4. Assert delete expense removes row.
5. Assert category filter narrows displayed rows.
6. Assert total updates after add/edit/delete.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/wizard/financials/expenses.test.ts` — tests pass

## Commit
`podplay(forward): stage 114 - tests for expense CRUD operations`
