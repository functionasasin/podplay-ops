# Stage 113 — Expense Tracking

## Spec Files
- `ui-spec/wizard-financials.md`
- `business-logic/invoicing-expenses.md`

## Instructions
1. Create `src/components/wizard/financials/ExpenseTracker.tsx`.
2. Table displaying existing expenses + add form.
3. Fields: category (12-type enum dropdown), amount, payment_method, date, notes.
4. Full CRUD: add, edit inline, delete with confirmation.
5. Category filter dropdown above table.

## Verify
- `cd apps/podplay && npm run build` — build passes

## Commit
`podplay(forward): stage 113 - expense tracker with CRUD and category filter`
