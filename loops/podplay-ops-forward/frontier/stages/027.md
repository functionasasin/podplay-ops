# Stage 027 — App Shell + Sidebar Layout

## Spec Files
- `ui-spec/routes.md`
- `ui-spec/dashboard.md`

## Instructions
1. Create `src/components/layout/AppLayout.tsx`.
2. Build a sidebar layout with a fixed left sidebar and a main content area rendering `<Outlet />`.
3. Sidebar nav links grouped into sections:
   - **Projects**: /projects (Dashboard), /projects/new (New Project)
   - **Inventory**: /inventory (Inventory)
   - **Financials**: /financials (Financials)
   - **Settings**: /settings/pricing (Pricing), /settings/templates (Templates)
4. Highlight the active route link using `useLocation` or NavLink's `isActive`.
5. Use shadcn components (Sheet for mobile sidebar, ScrollArea for nav).
6. Include a header bar with app name ("PodPlay Ops") and user avatar/signout button from `useAuth()`.
7. Register `AppLayout` as the layout route element wrapping all authenticated routes.

## Verify
- `npm run build` — build passes with zero errors

## Commit
`podplay(forward): stage 027 - app shell with sidebar layout`
