# Stage 062 — Tests: Intake Submit

## Spec Files
- `business-logic/customer-onboarding.md`
- `business-logic/bom-generation.md`

## Instructions
1. Write `src/__tests__/wizard/intake/submit.test.ts`.
2. Mock Supabase client (update, insert operations).
3. Mock the BOM generation function and cost chain calculation.
4. Assert the project row is updated with all form fields from the wizard.
5. Assert BOM generation is called with the correct arguments: tier, court_count, door_count, camera_count, has_front_desk, has_pingpod_wifi.
6. Assert the project status is changed to `'procurement'`.
7. Assert a success toast is fired on successful submission.
8. Assert an error toast is fired when the Supabase update fails.

## Verify
- `npx vitest run src/__tests__/wizard/intake/submit.test.ts` — all tests pass

## Commit
`podplay(forward): stage 062 - tests for intake submit`
