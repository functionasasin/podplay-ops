# Stage 043 — Tests: Project Creation

## Spec Files
- `ui-spec/dashboard.md`
- `business-logic/customer-onboarding.md`

## Instructions
1. Create `src/__tests__/projects/create.test.ts`.
2. Mock the Supabase client.
3. Test: Form renders all required fields (project_name, customer_name) and optional field (venue_name).
4. Test: Submitting with empty required fields shows validation errors (does not call Supabase).
5. Test: Submitting with valid data calls `supabase.from('projects').insert()` with `{ project_name, customer_name, venue_name, status: 'intake' }`.
6. Test: On successful insert, the app redirects to `/projects/{id}/intake`.
7. Test: On insert error, a toast/error message is displayed and no redirect occurs.
8. Test: Cancel button navigates back to `/projects`.

## Verify
- `npx vitest run src/__tests__/projects/create.test.ts` — all tests pass

## Commit
`podplay(forward): stage 043 - project creation tests`
