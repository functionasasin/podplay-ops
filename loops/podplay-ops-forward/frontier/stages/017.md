# Stage 017 — Tests: Financial Tables

## Spec Files
- `data-model/schema.md`

## Instructions
1. Write `apps/podplay/src/__tests__/db/financial-tables.test.ts`
2. Test cases:
   - Create an invoice for a project with type 'deposit', verify all fields
   - Create invoices of each type (deposit, final, change_order)
   - Update invoice status through lifecycle: draft -> sent -> paid
   - Create expenses across multiple categories from the 12-type enum
   - Create an expense without project_id (general expense), verify it works
   - Create a cc_terminal row, update status from ordered -> deployed
   - Create a replay_sign row, verify quantity and status tracking
   - Verify FK cascade: delete a project, confirm all invoices/expenses/terminals/signs are deleted
   - Verify enum constraints: insert with invalid status — expect error
3. Clean up test data after each test

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/db/` — all tests pass

## Commit
`podplay(forward): stage 017 - financial tables integration tests`
