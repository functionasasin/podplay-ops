# Stage 040 — Tests: Filters

## Spec Files
- `ui-spec/dashboard.md`

## Instructions
1. Create `src/__tests__/dashboard/filters.test.ts`.
2. Render the dashboard with 5 mock projects across different statuses and tiers.
3. Test: Selecting a status filter narrows the list to only projects with that status.
4. Test: Selecting a tier filter narrows the list to only projects with that tier.
5. Test: Typing in the text search input matches project name (case-insensitive).
6. Test: Typing in the text search input matches customer name (case-insensitive).
7. Test: Combining status filter + text search applies both filters (AND logic).
8. Test: Selecting "All" in a dropdown resets that filter.
9. Test: The filtered count label updates correctly.

## Verify
- `npx vitest run src/__tests__/dashboard/filters.test.ts` — all tests pass

## Commit
`podplay(forward): stage 040 - dashboard filter tests`
