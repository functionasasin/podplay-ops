# Stage 047 — Step 1: Customer Info Form

## Spec Files
- `ui-spec/wizard-intake.md`
- `ui-spec/validation-messages.md`

## Instructions
1. Create `src/components/wizard/intake/CustomerInfoStep.tsx`.
2. Fields: `customer_name` (required text input), `contact_email` (required, email format validation), `contact_phone` (optional text input).
3. Define a Zod schema for these fields with exact validation messages from `ui-spec/validation-messages.md` for the customer fields.
4. Use react-hook-form with the Zod resolver. Display inline error messages below each field on validation failure.
5. On valid submission, store data in the parent wizard form state and advance to the next step.

## Verify
- `npm run build` — build passes

## Commit
`podplay(forward): stage 047 - customer info form (intake step 1)`
