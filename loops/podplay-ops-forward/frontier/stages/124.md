# Stage 124 — Tests: Stock Levels

## Spec Files
- `ui-spec/inventory-view.md`

## Instructions
1. Write `src/__tests__/inventory/stock-levels.test.ts`.
2. Assert rows are grouped by category.
3. Assert low-stock rows have red highlight when available <= reorder point.
4. Assert available = on_hand - allocated for each row.

## Verify
- `cd apps/podplay && npx vitest run src/__tests__/inventory/stock-levels.test.ts` — tests pass

## Commit
`podplay(forward): stage 124 - tests for stock levels table`
