# Analysis Log

| # | Timestamp | Aspect | Duration | Key Findings |
|---|-----------|--------|----------|--------------|
| 1 | 2026-02-28 | `meta-instagram-messaging-api` | 1 run | Messenger Platform API; requires Business/Creator + FB Page link; Advanced Access app review required (2–10 days); webhook object=instagram; IGSID-based sender identity; 24h outbound window; 200 DM/hr rate limit; no group DMs; voice/Reels unsupported |
| 2 | 2026-02-28 | `meta-graph-api-conversations` | 1 run | Complementary read layer to Messenger Platform; `GET /{page-id}/conversations?platform=instagram`; cursor-paginated; same `instagram_manage_messages` Advanced Access required; 30-day inactivity cutoff for Requests folder; NOT viable as primary ingestion (rate limits + latency); best for initial account sync and webhook recovery/backfill; new Instagram Login API (July 2024) may remove FB Page requirement but DM endpoint support unclear; not deprecated |
| 3 | 2026-02-28 | `meta-webhooks-realtime` | 1 run | Object=instagram, fields=messages/messaging_postbacks/message_echoes/etc; two-phase verification (hub.challenge GET + HMAC-SHA256 per POST); at-least-once delivery, 5-10s timeout, async processing required; deduplicate by `mid`; filter is_echo; ephemeral media URLs must be downloaded immediately; FB Page linkage required; Dev mode: testers only until App Review; minimum viable: messages + message_echoes fields; new components: webhook endpoint, idempotency store, page subscription manager, media downloader, Temporal trigger |
| 4 | 2026-02-28 | `third-party-manychat` | 1 run | Meta-certified partner wrapping Messenger Platform API; Pro plan required ($15–$2,000+/mo); External Request action forwards `last_input_text` (text only) + contact data to Cheerful webhook; no thread history, no IGSID, no `mid`; flow-gated (only fires if DM matches a configured trigger); known bug: URL-in-DM breaks JSON payload; creator onboarding friction (must connect to ManyChat); not viable for production — useful only as prototype before Meta App Review approval; Direct Meta API preferred |
| 5 | 2026-02-28 | `third-party-messagebird-bird` | 1 run | API-first omnichannel platform; Meta Business Partner (no Cheerful App Review needed); Channels API provides full inbound webhook delivery per message (no flow-gating); exposes IGSID equivalent (`contact.identifierValue`), Bird conversation ID, message ID; bidirectional send/receive; message history via Conversations API; $0.005/message (prepaid, no subscription); 24h window + Business account still apply (Meta constraints); strongest third-party relay option — medium effort, production-grade reliability; Bird ID abstraction is migration consideration if switching to direct Meta API later |
| 6 | 2026-02-28 | `third-party-composio` | 1 run | Cheerful already uses Composio (outbound actions, MCP adapter pattern); 36 Instagram actions available including List/Get DM conversations and messages; **0 Instagram triggers** — no inbound DM events; cannot deliver real-time ingestion; polling workaround feasible via Temporal cron but minutes-latency; Meta App Review still required regardless; Composio's auth abstraction incompatible with direct Meta webhook subscription; not viable as primary inbound path; marginal value for outbound replies only |
| 7 | 2026-02-28 | `third-party-others` | 1 run | Surveyed Sendbird, Twilio, Zendesk/Sunshine Conversations, Intercom, Freshdesk, Chatwoot, Trengo; ALL follow ticket/inbox-destination pattern — not designed as programmatic relay; Twilio has no native IG DM support; Sunshine Conversations has Instagram channel API (legacy Smooch) but priced as Zendesk Suite ($55–115/agent/mo); Chatwoot (OSS) requires same Meta App Review + self-hosted ops burden; none expose per-DM webhook forwarding to external systems with full sender metadata; key conclusion: third-party relay space narrows to Bird only — all others are support desk products with inverted architecture for Cheerful's needs |
| 8 | 2026-02-28 | `unofficial-approaches` | 1 run | Three categories: (1) instagrapi — private API library, full DM read/write via `/api/v1/direct_v2/inbox/`, polling-only (no webhooks), high TOS/ban/legal risk, fragile (breaks on Meta updates); (2) Apify DM actors — cloud session-based, outbound-first (30-40 DMs/day limits), no real-time inbound; Cheerful's existing Apify usage is public data (categorically different risk); (3) Browser automation — not viable (headless detection, fragile, slow); (4) Unipile — managed session-based relay (NOT a Meta Partner), provides real-time inbound webhooks unlike other unofficial approaches, €49/mo for 10 accounts, DMA legal framing untested; narrow legitimate use: prototype-only before Meta App Review approval; production use exposes Cheerful's brand customers to account ban risk |
| 9 | 2026-02-28 | `current-thread-model` | 1 run | Dual-path thread identity (gmail_thread_id / email_thread_id) joined in campaign_thread via CHECK constraint; event-sourced append-only state (GmailThreadStatus enum shared between Gmail+SMTP); 7 states including WAITING_FOR_INBOUND; largest gaps for DM: (1) creator matching is email-address-only — no IG handle→creator resolution; (2) gmail_thread_llm_draft table is already cross-channel but email-specific fields (draft_subject, gmail_draft_id); (3) 24-hour DM window has no email equivalent; (4) EmailThreadMessageView DTO has sender_email not sender_handle; (5) XML thread format uses <subject>/<cc>/<bcc> fields irrelevant to DMs; third parallel path (ig_dm_*) is the natural extension of the existing pattern |
| 10 | 2026-02-28 | `current-email-pipeline` | 1 run | Two polling pipelines (Gmail History API via OAuth; SMTP via IMAP UID-based incremental sync) converge on shared ThreadProcessingCoordinatorWorkflow via Candidate object discriminator (gmail_account_id vs smtp_account_id); ingest stages are channel-specific (different Temporal activities, tables, API clients); coordinator + campaign association + AI drafting are channel-agnostic; Instagram DM path would differ structurally: webhook-first (not polling-first), no attachment extraction, no RFC 2822 thread chaining; 5 new components needed: webhook FastAPI endpoint, ig_dm_ingest_activity, ig_dm thread state activity, ig_dm_* tables, IgDmSendReplyWorkflow; Candidate object already supports addition of third discriminator (ig_dm_account_id) |
| 11 | 2026-02-28 | `current-creator-identity` | 1 run | Two-layer model: global `creator` table keyed by (platform, handle) + per-campaign `campaign_creator` CRM keyed by email — no FK between them; IGSID is NOT stored anywhere; handles stored in `social_media_handles` JSONB array (no GIN index, text-searched via ILIKE); matching priority: email → thread+name → signal candidates → LLM; significant coverage gap — many campaign_creators from CSV upload have no Instagram handle; DM sender arrives as opaque IGSID requiring Graph API call to resolve to username; campaign disambiguation unsolved (DM could belong to any active campaign); 4 resolution options: IGSID cache table, add igsid column to campaign_creator, GIN index on JSONB handles, or route via global creator registry via email |
| 12 | 2026-02-28 | `current-inbox-ui` | 1 run | Core type `GmailThread`/`GmailMessage` has email-hardcoded fields (subject, senderEmail, bodyHtml, to/cc, messageIdHeader, accountEmail); thread list: subject line row + email tooltip are email-specific, rest (avatar/name/snippet/flags/status badge) is channel-agnostic; thread detail: header shows `<email@domain.com>` + subject; reply composer is most problematic — full TO/CC editor + Reply-All + Tiptap HTML editor are email-only; Instagram DMs are plain-text-only so HTML editor must be replaced; 24-hour window has no UI equivalent; chat bubble message layout already fits DMs naturally; 3 implementation options: (A) channel discriminator + conditional rendering in existing components, (B) separate `/dm-inbox` route + new components, (C) minimal field reuse shim (fastest but ugly); 5 new UI components needed: 24h window indicator, IG channel badge, plain-text DM composer, IG account connection UI, IGSID resolution fallback display |
| 13 | 2026-02-28 | `current-ai-drafting` | 1 run | AI infrastructure layer (LlmService, EmbeddingService, ClaudeAgentService) fully channel-agnostic — zero changes needed; classification features (opt-in, workflow routing, thread flags, thread completion) channel-agnostic; draft generation features email-specific in 3 ways: ResponseDraftResult.subject field, email-framed Langfuse prompts (8 needed → 9 DM variants), and RAG table schema (email_reply_examples); RAG system is biggest adaptation: new ig_reply_examples table + DM sanitizer PII patterns + DM-specific query format + DM training ingestion path; thread context XML format needs DM variant (remove subject/to/cc, add ig_conversation_id/channel/window_expires_at); follow-up drafts cannot reach DMs after 24h window — need window-reopener template prompt; Langfuse observability wires through with only langfuse_session_id value change; cost per DM draft ~73% cheaper than email draft due to shorter context/output; campaign association for DMs determined at ingestion (IG account → campaign), not via AI classification |
| 14 | 2026-02-28 | `option-direct-meta-api` | 1 run | Full direct integration design: Meta OAuth (5-step flow → permanent page token) + Meta App Review (2–10 day blocker) + webhook handler (HMAC-SHA256, 200 immediately, BackgroundTask enqueue) + 7 new DB tables (user_ig_dm_account, ig_igsid_cache, ig_dm_message, ig_dm_conversation, ig_dm_thread_state, ig_dm_llm_draft, latest_ig_dm_message_per_conversation) + 3 modified tables (campaign_thread, campaign_sender, campaign_creator); IgDmIngestWorkflow → IgDmThreadSyncWorkflow → ThreadProcessingCoordinatorWorkflow (new IG DM branch, skip Gmail-only steps); IGSID resolution: 2-phase (cache hit → Graph API fallback), cached in ig_igsid_cache + denormalized ig_igsid on campaign_creator; campaign disambiguation: IG account → campaign mapping, handle JSONB GIN index lookup, LLM disambiguation for ambiguous multi-campaign matches; UI: channel discriminator on GmailThread type, conditional rendering in mail-list/mail-display, plain-text DmComposer replaces Tiptap HTML editor, 24h window indicator; AI: 9 new Langfuse prompts + ig_reply_examples RAG table (parallel), zero infra changes; estimate 20–28 engineering days + 6–10 weeks calendar (App Review blocking); compatible with both parallel-tables and channel-abstraction patterns |
| 15 | 2026-02-28 | `option-graph-api-polling` | 1 run | Three variants analyzed: (A) pure polling as primary — structurally mirrors Gmail/SMTP polling (AllIgDmPollWorkflow + ig_dm_poll_activity + timestamp checkpoint), viable for ≤5 accounts at 5-min intervals, unviable at scale (200 calls/hr limit per user exhausted at ~10 accounts); (B) hybrid: webhooks primary + polling for recovery — recommended mandatory component of direct Meta API option, runs every 30–60 min to backfill missed webhook events, negligible rate limit cost; (C) initial account sync — one-time poll of all conversations on account connect, required by all webhook approaches, throttled to 50 convs/min; same permissions/App Review requirements as webhooks (no auth complexity reduction); data model identical to direct Meta API (last_poll_timestamp replaces history_id checkpoint); Candidate discriminator extension (ig_dm_account_id) reuses existing pattern; media URL expiry risk with long poll intervals; pure polling saves ~3–5 dev days vs webhooks but loses real-time + scale; verdict: Variant A valid for prototyping only, Variants B+C are required additions to direct Meta API option |
| 16 | 2026-02-28 | `option-composio-relay` | 1 run | Composio relay is structurally polling-only (0 Instagram DM triggers); 36 Composio Instagram actions used for poll (LIST_DM_CONVERSATIONS + LIST_DM_MESSAGES) and outbound send (SEND_TEXT_MESSAGE); 4 new DB tables (user_ig_dm_account, ig_dm_message, ig_dm_thread_state, ig_dm_watermark) + new mutable watermark cursor pattern (diverges from event-sourced append-only model); Composio-hosted OAuth simplifies auth onboarding vs self-managed Meta OAuth (saves ~1 week); same Meta App Review requirement applies (no Composio shortcut); rate limits: free tier saturates at ~2 users × 50 conversations × 5-min poll; paid tier ($) needed at scale; Composio adds SPOF for Instagram channel (email unaffected); polling architecture hard to upgrade to real-time without rework; strongest use case is outbound send (SEND_TEXT_MESSAGE) reusing existing composio_adapter.py; total effort ~6 weeks vs ~7 weeks for direct Meta API; key trade-off: simpler OAuth vs polling latency + vendor SPOF + real-time upgrade cost |
| 17 | 2026-02-28 | `option-third-party-relay` | 1 run | Bird (formerly MessageBird) selected as the sole viable third-party relay; all other surveyed platforms (Sendbird, Twilio, Zendesk, Intercom, Freshdesk, Chatwoot, Trengo) rejected as destination support desks, not relays; ManyChat rejected for flow-gated forwarding + no thread IDs + text-only + creator onboarding friction; Bird architecture: one Cheerful Bird workspace, per-creator Instagram channel; Bird manages Meta webhook subscription (no Cheerful App Review needed); inbound: Bird webhook → FastAPI → Temporal IgDmIngestWorkflow; outbound: ThreadProcessingCoordinatorWorkflow → IgDmSendReplyActivity → Bird Channels API → Meta; thread key: bird_conversation_id; IGSID still exposed via contact.identifierValue (native); 5 new tables (ig_dm_account, ig_dm_thread, ig_dm_message, ig_dm_thread_state, ig_identity) + 3 modified (campaign_thread, campaign_sender, thread_flag); GIN index on campaign_creator.social_media_handles still required; 24-hour window + Professional account requirement unchanged (Meta constraints); cost: $0.005/message vs free for direct Meta API; migration path preserved (store sender_igsid natively, re-key if later migrating to direct Meta); effort: Medium (faster than direct Meta due to no App Review); Bird's multi-channel future (WhatsApp, SMS) is a bonus if Cheerful expands channels later |
| 18 | 2026-02-28 | `option-channel-abstraction` | 1 run | Architecture pattern (orthogonal to API choice); solves naming drift (`GmailThreadStatus`/`GmailThread` used for all channels); 3 DB sub-options: 1A polymorphic `thread` table (loses FK integrity, requires data migration), 1B union view over parallel tables (zero migration risk, query abstraction only); Python `ChannelAdapter` Protocol wraps existing Gmail/SMTP code + implements new `InstagramDmAdapter`; coordinator replaces `if gmail_account_id/elif smtp_account_id` branching with adapter registry; frontend: `GmailThread` → discriminated union `Thread = EmailThread \| IgDmThread` (TypeScript narrows in components); 3 migration strategies: Strategy 1 Big Bang (+8–12 days, full rename), Strategy 2 Façade (+3–5 days, wrap existing code), Strategy 3 Incremental (+1–2 days, naming only + parallel path); combinable with all API options; recommended if 4th channel (WhatsApp/LinkedIn) is on roadmap; Strategy 3 only if IG DMs is isolated; not worth full Strategy 1 unless kitchen-sink draft table is causing active bugs |
| 19 | 2026-02-28 | `option-parallel-tables` | 1 run | Architecture pattern (orthogonal to API choice); follows SMTP precedent exactly — add `user_ig_dm_account`, `ig_dm_message`, `ig_dm_thread_state`, `ig_igsid_cache`, `latest_ig_dm_message_per_thread`; extend `campaign_thread`/`campaign_sender`/`thread_flag` with 3-way mutual-exclusivity CHECK constraints; `gmail_thread_llm_draft` dilemma: Option A extend with ig columns (naming debt) vs Option B new `ig_dm_llm_draft` table (clean but more proliferation); `Candidate` object gets `ig_dm_account_id` nullable field + `ig_conversation_id`; `ThreadProcessingCoordinatorWorkflow` gets third `elif` branch; webhook-driven ingest (vs polling for Gmail/SMTP) — FastAPI webhook endpoint → BackgroundTask → Temporal IgDmIngestWorkflow; `InstagramDmService` parallel to GmailService/SmtpEmailService; GIN index on `campaign_creator.social_media_handles` + `ig_igsid_cache` for creator resolution; frontend: extend `GmailThread` type with `channel` discriminator + optional DM fields + conditional rendering + new `DmComposer`; 0 additional overhead vs abstraction (+3–5 days); strengths: proven precedent, zero migration risk, full FK integrity, channel isolation; weaknesses: `GmailThreadStatus` serves 3 channels, 3-way CHECK scales poorly, naming debt grows; "pay later" vs channel abstraction's "pay now" |
