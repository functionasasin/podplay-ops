# Analysis Log

| # | Timestamp | Aspect | Duration | Key Findings |
|---|-----------|--------|----------|--------------|
| 1 | 2026-02-24 | engine-input-root | 1 iteration | 6 top-level fields (all required in JSON). `will` is `Option<Will>` (null=intestate). `family_tree`/`donations` are `Vec` (can be `[]`). `config` required but has logical defaults. No runtime validation beyond serde — frontend must validate more. Frac serializes as `"n/d"` string, NOT `{numer, denom}`. |
| 2 | 2026-02-24 | money | 1 iteration | Single-field struct `{centavos: number\|string}`. Custom serde: i64 as JSON number, BigInt overflow as string. Frontend displays pesos (÷100) with ₱ prefix, comma thousands, conditional centavos. Non-negative enforced by frontend (engine has no explicit check). Used in 5 input contexts + 7 output fields. `formatPeso()`, `pesosToCentavos()`, `centavosToPesos()` helper functions specified. |
| 3 | 2026-02-24 | engine-config | 1 iteration | 2-field struct: `retroactive_ra_11642` (bool, default false, NOT consumed by any pipeline step — forward-looking flag for RA 11642 adoption retroactivity), `max_pipeline_restarts` (i32, default 10, consumed by Step 9 vacancy resolution as restart guard). Both fields required in JSON despite Rust Default impl. All 90+ test cases use defaults only. Frontend: expose in Advanced Settings collapsed section; add "no effect" note on retroactive flag; enforce max_pipeline_restarts ∈ [1, 100]. |
| 4 | 2026-02-24 | decedent | 1 iteration | 11-field struct. 5 fields actively drive computation: `is_illegitimate` (T14/T15 scenario selection, Art. 903), 4 articulo mortis fields (`marriage_solemnized_in_articulo_mortis`, `was_ill_at_marriage`, `illness_caused_death`, `years_of_cohabitation < 5`) reduce spouse legitime E/2→E/3 in T12 (Art. 900). 6 fields are informational/not consumed: `id`, `name`, `date_of_death`, `is_married` (spouse presence from family_tree), `date_of_marriage`, `has_legal_separation` (exclusion via Person.is_guilty_party). No MarriageType enum exists. Cascading visibility: marriage→articulo mortis→illness→death cause. |
| 5 | 2026-02-24 | person | 1 iteration | 14-field struct — the core family tree entity. Every field consumed by step1_classify.rs→Heir mapping. Key conditional visibility: relationship drives which sub-fields appear (IC→filiation, Adopted→adoption sub-form, Spouse→legal-sep guilt, Ascendant→line, Sibling→blood_type, Unworthy→condoned). Grandchildren use relationship=LegitimateChild+degree=2 (case 15). Stranger skipped by engine (no Heir). Dead persons still classified for representation. Max 1 SurvivingSpouse enforced by frontend. blood_type defaults to Full in engine if null (step7:894) but frontend should require explicit. children[] IDs are referential to other Persons. degree range 1–5 (Art. 1010). |
| 6 | 2026-02-24 | relationship-enum | 1 iteration | 11-variant PascalCase string enum. 7 compulsory (Art. 887): LegitimateChild/LegitimatedChild/AdoptedChild→LegitimateChildGroup; IllegitimateChild→IllegitimateChildGroup; SurvivingSpouse→SurvivingSpouseGroup; LegitimateParent+LegitimateAscendant→LegitimateAscendantGroup. 3 non-compulsory intestate: Sibling/NephewNiece/OtherCollateral→CollateralGroup. Stranger skipped entirely (returns None in step1). Key conditional sub-forms: AdoptedChild→adoption form; IllegitimateChild→filiation; SurvivingSpouse→legal-sep guilt; Parent/Ascendant→line; Sibling→blood_type required. Mutual exclusion: Ascendant group ineligible when LC-group alive+not renounced (step1:105–120). Pipeline derives hasSiblingsOrNephews+hasOtherCollaterals from relationship for scenario selection (pipeline.rs:41–56). 5 variants (LegitimatedChild, LegitimateAscendant, NephewNiece, OtherCollateral, Stranger) have zero test case coverage. |
| 7 | 2026-02-24 | filiation-proof | 1 iteration | 6-variant PascalCase string enum (FC Art. 172). Variants map to FC articles: BirthCertificate/FinalJudgment→Art.172(1); PublicDocumentAdmission/PrivateHandwrittenAdmission→Art.172(2); OpenContinuousPossession→Art.172(3); OtherEvidence→Art.172(4). Engine ONLY checks `filiation_proved: bool` for eligibility gate (step1_classify.rs:178, Art. 887 ¶3); proof type is informational only — used by step10_finalize filiation_description() for narrative output. Conditional visibility: show only when relationship=IllegitimateChild AND filiation_proved=true. Frontend should require explicit selection (engine doesn't validate proof type). Key pairing: filiation_proved=false+type=null→ineligible; filiation_proved=true+type=any→eligible. Test cases confirm BirthCertificate and FinalJudgment are used in practice; all 3 IC test cases have filiation_proved=true with explicit type. |
| 8 | 2026-02-24 | blood-type | 1 iteration | 2-variant PascalCase string enum: "Full" (shares both parents) / "Half" (shares one parent). Used ONLY in collateral distribution sub-algorithm (step7_distribute.rs:817). Critical: Sibling with blood_type=null is SILENTLY EXCLUDED from distribution (step7:820 filter). Frontend must require blood_type when relationship=Sibling; NephewNiece optional (engine defaults to Full at step7:911). Distribution: all-same-blood→equal shares (Art. 1004); mixed→2:1 Full:Half ratio (Art. 1006). NephewNiece blood_type represents parent sibling's blood type for per-stirpes weighting. OtherCollateral ignores blood_type entirely. Conditional visibility: show for Sibling+NephewNiece only; hidden for all other relationships. Test case 11-siblings.json confirms "Full"/"Half" exact serialization. |
| 9 | 2026-02-24 | adoption | 1 iteration | 8-field struct (types.rs:333–342) + 2-variant AdoptionRegime enum ("Ra8552"/"Ra11642"). Conditionally required on Person when relationship=AdoptedChild; missing adoption record → ineligible (step1_classify.rs:189). Core eligibility gate: is_rescinded=true → ineligible (RA 8552 Sec. 20, step1:182–186). is_adoption_valid() = !is_rescinded. Adopted child maps to LegitimateChildGroup (RA 8552 Sec. 17) — identical shares to LC. Key conditional sub-fields: biological_parent_spouse shown only when is_stepparent_adoption=true (must reference SurvivingSpouse in family_tree); rescission_date shown only when is_rescinded=true. adopter+adoptee should auto-fill (decedent.id + Person.id). retroactive_ra_11642 config flag is independent of regime field (engine does not consume it). Only test coverage is Ra8552; Ra11642 has zero test coverage. Default regime: "Ra8552". |
| 10 | 2026-02-24 | will | 1 iteration | 5-field struct (types.rs:362–368): 4 Vec sub-lists (institutions/legacies/devises/disinheritances, all can be []) + date_executed (ISO-8601 string). Will is Option<Will> in EngineInput — null→Intestate (step3:55–58), Some→Testate+Step6 validation runs. date_executed NOT validated by engine against date_of_death (Art. 838 requires pre-death); frontend must enforce cross-field. ShareSpec unit variants serialize as plain strings ("EntireFreePort", etc.); Fraction variant→{"Fraction":"n/d"} (Frac is "n/d" string, NOT {numer,denom}). LegacySpec::GenericClass→["string",{centavos}] 2-tuple array. DeviseSpec::FractionalInterest→["asset-id","n/d"]. HeirReference.person_id=null for strangers. is_residuary captures leftover FP (step7:987). is_preferred→Phase1b reduction order (Art.911). Only 2 test cases have will: case 06 (EntireFreePort institution) and case 14 (FixedAmount legacy). Gap: no test coverage for Fraction/EqualWithOthers/DeviseSpec/Condition/Substitute. |
| 11 | 2026-02-24 | institution-of-heir | 1 iteration | 3 types: InstitutionOfHeir (6 fields, types.rs:371–378), HeirReference (4 fields, types.rs:391–396), ShareSpec (6-variant enum, types.rs:381–388). ShareSpec unit variants→plain strings; Fraction→{"Fraction":"n/d"}. HeirReference.person_id=null for strangers/charities (confirmed case-06). Collective institutions (is_collective=true+class_designation) have no engine resolution logic — informational only. is_residuary flag (not ShareSpec::Residuary variant) is the actual residuary trigger (step7:987); frontend must couple is_residuary=true↔share="Residuary". Art. 854 preterition: LC/parent/ascendant total omission annuls ALL institutions; IC preterition only when another IC is instituted; spouse omission never preterition (step6:294–356). Key edge cases: duplicate residuaries (engine takes last match in loop), Fraction>1 (engine doesn't reject — frontend should warn), EqualWithOthers with single institution (resolves to 0). Frontend must validate person_id references family_tree entry. |
| 12 | 2026-02-24 | legacy | 1 iteration | 2 types: Legacy (6 fields, types.rs:399–406), LegacySpec (3-variant enum, types.rs:408–413). LegacySpec variants: FixedAmount(Money)→{"FixedAmount":{centavos}}, SpecificAsset(String)→{"SpecificAsset":"id"}, GenericClass(String,Money)→{"GenericClass":["desc",{centavos}]} 2-tuple array. Key pipeline rules: SpecificAsset→Frac::zero (engine skips monetary computation, needs appraisal; warn user); is_preferred controls Art.911 reduction order (Phase1a=non-preferred first, Phase1b=preferred after non-preferred exhausted; step6:574–632); legatee.person_id=null allowed (stranger legatees — name used as distribution key); legacy with person_id matching compulsory heir defeats preterition (Art.855, even ₱1 token); legacies paid from FP after institutions, capped at remaining FP (step7:328–351). Inofficiousness reductions applied before distribution if Step6 detects excess. Step9 checks legacy.substitutes for vacancy. Confirmed JSON from case-14. |
