# TaxKlaro Loop Registry
# CI reads this to discover which loops to run.
# Status: active | paused | converged

loops:
  forward:
    description: "Full-stack TaxKlaro build: Rust engine + WASM + React frontend + Supabase + Fly.io"
    type: forward
    schedule: "0 6,18 * * *"
    max_iterations: 50
    timeout_minutes: 30
    status: active
    created: 2026-03-06
