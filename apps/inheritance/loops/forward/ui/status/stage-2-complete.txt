Stage 2 — Shared Form Components: COMPLETE

Restyled 5 shared form components with shadcn/ui design system:

1. MoneyInput — shadcn Input with ₱ prefix overlay, Label-styled heading,
   destructive/warning color tokens for error/zero states
2. DateInput — shadcn Input type="date" with Label-styled heading,
   muted-foreground hints, destructive errors
3. FractionInput — Two shadcn Input fields with "/" separator,
   shadcn Button (outline/xs) for fraction presets, destructive errors
4. PersonPicker — Native select styled with shadcn Input classes (border,
   radius, focus ring), Label-styled heading, destructive errors
5. EnumSelect — Native select styled with shadcn Input classes,
   Label-styled heading, destructive errors

Design decisions:
- PersonPicker and EnumSelect keep native <select> elements to preserve
  test compatibility (tests use userEvent.selectOptions and optgroup roles)
- All components use design token colors (text-destructive, text-warning,
  text-muted-foreground) instead of raw Tailwind colors
- Consistent spacing with space-y-2 layout
- Label text uses text-sm font-medium leading-none (matching shadcn Label)

All 1033 tests pass.
