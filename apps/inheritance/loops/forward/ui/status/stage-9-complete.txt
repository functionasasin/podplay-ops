Stage 9 — Responsive + Final Polish — COMPLETE

Changes made:
1. Responsive breakpoints: App.tsx uses max-w-3xl (768px), WizardContainer uses max-w-2xl (672px), step labels hidden on mobile — verified correct
2. Spacing audit: Fixed non-standard ml-6.5 to ml-7 and gap-2.5 to gap-2 in DecedentStep and PersonCard
3. Typography audit: Added sr-only h2 headings to EstateStep, DecedentStep, FamilyTreeStep, WillStep; changed DonationsStep h3 to h2
4. Responsive text sizing: Added text-base sm:text-lg to DistributionSection h3s and NarrativePanel; text-lg sm:text-xl to ReviewStep
5. Focus states: Added global focus-visible styles for native checkboxes and radios in index.css
6. Empty states: FamilyTreeStep and DonationsStep already have styled empty states

All 1053 tests pass. Vite build succeeds. CSS bundle: 63.45 KB.
