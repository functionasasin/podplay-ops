Stage 7 complete — responsive + polish verified

All items implemented:
1. Loading states: Loader2 spinners + Skeleton layouts on all data-loading pages
2. Empty states: EmptyState component on dashboard, cases, deadlines, clients, notes, team
3. Mobile responsive: overflow-x-auto on tables, wizard max-width, color picker flex-wrap, touch targets
4. PersonCard: off-palette badge colors replaced with design system tokens (GAP-DWC-012)
5. ReviewStep: KPI card grid + bg-accent fix (GAP-DWC-017/019)
6. DistributionSection: pie chart Legend + overflow-x-auto (GAP-DRC-005, GAP-DMR-003)
7. ClientForm + ClientList: replaced raw HTML with shadcn components

Build: clean (zero errors)
