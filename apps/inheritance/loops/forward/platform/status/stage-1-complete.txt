Stage 1 (Foundation) — COMPLETE

All items verified against spec:

1. src/lib/supabase.ts — graceful supabaseConfigured flag (no throw on missing env)
2. src/components/SetupPage.tsx — setup page shown when env vars missing
3. frontend/.env.local.example — all 3 required vars (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_APP_URL)
4. supabase/migrations/011_create_org_rpc.sql — create_organization RPC, handle_new_user trigger, updated get_shared_case
5. supabase/migrations/012_pdf_storage.sql — case_pdfs table with RLS
6. supabase/_MIGRATION_NOTES.md — documents missing migration numbers
7. src/lib/organizations.ts — createOrganization() function
8. src/lib/auth.ts — resetPassword() and updatePassword() functions

All items pre-implemented. Verified against spec §4 and §5.
