# Current Stage: Not Started

Waiting for spec at `docs/plans/inheritance-v2-spec.md` to be produced by the reverse loop (`loops/inheritance-v2-reverse/`).

Run the reverse loop first:
```bash
cd loops/inheritance-v2-reverse && ./loop.sh
```

Then run this forward loop:
```bash
cd loops/inheritance-v2-forward && ./forward-ralph.sh auto
```
